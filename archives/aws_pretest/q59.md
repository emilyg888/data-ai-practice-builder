59/75

Question
A GenAI developer must use Amazon Q Business to enhance an internal content management system (CMS) for a company. The company wants users to be able to search, query, and receive AI-assisted insights from the company's data.

The company has technical documentation in the CMS and internal knowledge bases with proprietary guidelines. The company has enterprise directory services for authentication, strict role-based access controls (RBAC), and compliance and governance policies for data access.

Which combination of steps will meet these requirements with MINIMAL operational overhead? (Select TWO.)

Correct answer: A + D

A
Configure Amazon Q Business with data source connectors to integrate the CMS and internal knowledge bases. Use AWS IAM Identity Center authentication for RBAC.

Correct. Amazon Q Business data source connectors are managed integration points that provide secure connections to external content systems. IAM Identity Center is a centralized service to manage user access and permissions across AWS accounts. This step uses built-in connectors to integrate content securely. This step uses IAM Identity Center enterprise-grade authentication and RBAC. Therefore, this step meets the compliance requirements without custom development.

Learn more about Amazon Q Business data sources.

Learn more about IAM for Amazon Q Business.

B
Configure Amazon Q Business with custom Amazon API Gateway endpoints for content access. Enable SAML-based SSO through AWS IAM Identity Center. Create AWS Lambda functions that validate access permissions before serving responses.

Incorrect. You can use API Gateway to create and manage APIs. SAML-based SSO through IAM Identity Center is suitable for this scenario. However, creating custom API endpoints and validation Lambda functions requires additional operational overhead. Amazon Q Business already provides built-in access controls and content integration.

C
Configure Amazon Q Business with enterprise Active Directory integration. Create data source filters based on security groups. Create Amazon CloudWatch alarms to monitor unauthorized access attempts to knowledge bases.

Incorrect. Active Directory integration provides enterprise authentication by using existing identity systems. CloudWatch alarms provide monitoring and alerting capabilities. However, using data source filters based on security groups does not provide sufficient granular access control. Additionally, creating CloudWatch alarms requires additional operational overhead. Amazon Q Business already provides built-in monitoring features.

D
Configure Amazon Q Business data sources with automatic content synchronization and security group mapping for access control.

Correct. Amazon Q Business automatic content synchronization is a feature that updates knowledge bases from source systems in real time. Security group mapping provides automatic access control based on existing enterprise roles. This step uses built-in capabilities to maintain current content and enforce proper access controls. This step does not require additional services or custom code.

Learn more about Amazon Q Business data sources.

Learn more about IAM for Amazon Q Business.

E
Create custom AWS Lambda functions to process and index documentation before importing the documentation into Amazon Q Business knowledge bases.

Incorrect. Lambda is a serverless compute service that runs custom code in response to events. Creating custom Lambda functions for document processing and indexing requires additional operational overhead. Amazon Q Business already includes built-in document processing capabilities.
