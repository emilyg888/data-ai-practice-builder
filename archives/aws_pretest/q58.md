58/75

Question
A company wants to enhance a customer support AI assistant with up-to-date product information. A GenAI developer must implement a custom RAG solution by using Amazon Bedrock and Amazon OpenSearch Service. The GenAI developer needs to maintain complex relationships between product variants. The GenAI developer must implement hybrid search that combines semantic matching and keyword matching.

Select and order each step from the following list to implement the RAG solution. Select each step one time. (Select and order FOUR.)

Create initial embeddings and configure automated embedding generation for product updates.
Deploy an FM and develop a prompt template with context retrieval.
Implement vector search in OpenSearch Service with hybrid search capabilities.
Set up a data ingestion pipeline to process and update product information.
Step 1:

Set up a data ingestion pipeline to process and update product information.
Step 2:

Create initial embeddings and configure automated embedding generation for product updates.
Step 3:

Implement vector search in OpenSearch Service with hybrid search capabilities.
Step 4:

Deploy an FM and develop a prompt template with context retrieval.
Report Content Errors
This use case requires a strict sequence because of component dependencies. You must test and verify the solution at each phase.

First, you must set up the data ingestion pipeline. This step establishes the foundation for all subsequent steps. You must test and verify the pipeline to ensure proper processing and formatting of the product information. This step is necessary so that you can implement subsequent components effectively.

Second, you must create initial embeddings and configure automated embedding generation. This step requires processed data from the verified pipeline. This step ensures that the initial embedding creation and ongoing updates work correctly before you proceed. The automation configuration occurs with the initial creation because of the shared common components and testing requirements.

Third, you must implement vector search. This step requires the previous components of a verified data pipeline and functional embedding generation. You must test the hybrid search capabilities with actual embeddings. You must verify the embeddings for both semantic matching and keyword matching functionality before the final phase.

Finally, you must deploy the FM and develop the prompt template. All previous components must be operational and verified to make this step possible. This step depends on all previous components being operational and verified. You must test the prompt template with the working vector search to ensure proper context retrieval and integration.

Learn more about how to build a RAG application by using Amazon Bedrock.

Learn more about vector search.

Learn more about embeddings.
