40/75

Question
A company has a large collection of HTML documents. The documents contain articles with varying lengths and complex hierarchical structures. Headers identify each article. Each article contains multiple paragraphs that can range from a few sentences to several pages in length.

A GenAI developer must build a RAG solution that preserves the relationships between articles and the articles' contained paragraphs. The solution must retrieve relevant content for user inquiries. The solution must minimize irrelevant or inaccurate responses.

Which solution will meet these requirements?

Correct answer: A

A
Create an AWS Lambda function that implements a custom hierarchical chunking strategy. Use the LangChain framework for the HTML documents. Deploy LangChain through a Lambda function layer. Create an Amazon Bedrock knowledge base with the documents. Use the Lambda function as the custom chunking configuration.

Correct. Amazon Bedrock Knowledge Bases supports custom chunking through Lambda functions. This solution can process documents with highly variable lengths. A custom Lambda function with LangChain provides a specialized chunking strategy. This solution can properly handle articles and paragraphs of any length. This solution can preserve the hierarchical relationships in the articles and paragraphs. This solution provides the control and flexibility that you need for complex document structures.

Learn more about custom chunking.

B
Create an Amazon Bedrock knowledge base with the HTML documents. Use the built-in hierarchical chunking strategy. Configure the chunking strategy with parent chunks and child chunks. Set the estimated sizes based on the average length observed in the documents.

Incorrect. Amazon Bedrock provides built-in hierarchical chunking that maintains parent-child relationships in structured documents. However, setting fixed chunk sizes based on averages is inadequate for documents with highly variable paragraph lengths. This solution can lead to inappropriate splitting of content when paragraphs are significantly longer or shorter than the average. This solution could break important contextual relationships in the document structure.

Learn more about hierarchical chunking.

C
Create an Amazon Bedrock knowledge base with the HTML documents. Use the built-in semantic chunking strategy. Configure the chunking strategy to divide the documents into chunks based on their semantic meaning and content rather than syntactic structure.

Incorrect. Amazon Bedrock provides built-in semantic chunking that automatically divides documents based on meaning and content coherence. This solution uses natural language understanding to identify logical breaks. This solution prioritizes meaning over structure. Therefore, this solution could break the required hierarchy between articles and paragraphs. This solution could lose the explicit relationships that you need for accurate information retrieval.

Learn more about semantic chunking.

D
Use Amazon Textract to extract the text from the HTML documents. Create chunk files based on the paragraph extractions from Amazon Textract. Upload the extractions to an Amazon S3 bucket. Create an Amazon Bedrock knowledge base with the chunked files. Use the no chunking option.

Incorrect. Amazon Textract is designed to extract text from image-based documents and PDFs, not from HTML documents. Additionally, creating chunks from extracted paragraphs without considering the document hierarchy will not maintain the relationships between articles and paragraphs. The no chunking option would treat each extraction as an independent unit. Therefore, you could lose the contextual relationships that you need for accurate information retrieval.

Learn more about Amazon Textract supported document types.
