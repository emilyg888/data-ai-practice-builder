# Udemy Page Scrape: Questions 2-71

- Source: https://www.udemy.com/course/ultimate-aws-certified-generative-ai-developer-professional/learn/quiz/7381571/result/1768485161?expanded=1768485161#overview
- Scraped: 2026-05-12
- Scope: Question 2 through Question 71
- Method: text copied from the loaded Chrome page

## Extracted Text

Question 2
Correct
A financial services firm is building a customer-support chat experience by using Amazon Bedrock. An Amazon API Gateway endpoint invokes an AWS Lambda function that calls a text FM and stores the prompt/response transcript in an Amazon S3 bucket for audit purposes. The firm must reduce the risk of exposing personally identifiable information (PII) in FM responses, must detect whether any PII is still being written to the S3 audit bucket, and must retain audit data for 90 days before automatically deleting it. Which solution will meet these requirements with the LEAST operational overhead?
Your answer is correct
Attach an Amazon Bedrock guardrail that masks or removes PII on both prompts and responses for the model invocation. Enable Amazon Macie to discover PII in the S3 audit bucket and send findings to Amazon EventBridge. Configure an S3 Lifecycle rule to expire audit objects after 90 days.
Explanation
Correct. Bedrock guardrails provide a managed way to filter/mask PII in FM interactions, reducing the chance that responses contain sensitive data. Macie continuously discovers PII in S3 and can generate findings for investigation/automation. An S3 Lifecycle configuration enforces automatic retention and deletion without custom jobs.
Configure AWS WAF on the API Gateway endpoint to block requests that contain patterns that resemble PII. Use AWS CloudTrail to record model invocations and configure an S3 Lifecycle rule to expire objects after 90 days.
Explanation
Incorrect. AWS WAF can help with web request filtering but is not a comprehensive PII detection/redaction control for FM prompts and responses. CloudTrail records API activity for auditing but does not inspect S3 object contents for PII or prevent PII from being included in model outputs.
Create a post-processing workflow that runs after each model invocation: store transcripts in S3, then run an Amazon Comprehend job to identify PII and write a redacted copy to a second S3 bucket. Keep both buckets for 90 days by using S3 Lifecycle rules.
Explanation
Incorrect. This approach adds operational overhead (building and operating a custom redaction pipeline) and still stores unredacted transcripts for up to 90 days, which increases privacy risk. It also does not provide a managed continuous discovery service for PII in S3 comparable to Macie.
Encrypt the S3 audit bucket with SSE-KMS and restrict access to the bucket by using an IAM policy with least privilege. Configure an S3 Lifecycle rule to expire objects after 90 days.
Explanation
Incorrect. Encryption and access control protect data at rest and limit who can view it, but they do not detect PII in stored objects or prevent the FM from returning PII in responses.
Overall explanation
A low-operations privacy-preserving design combines managed controls at the interaction layer, storage discovery, and retention enforcement. Bedrock guardrails can filter or mask PII in prompts and responses to reduce the likelihood of sensitive information being returned to users. Amazon Macie then provides automated discovery of PII in the S3 audit bucket so the security team can detect and respond if sensitive data is still being stored. Finally, an S3 Lifecycle configuration implements the required 90-day retention policy by automatically expiring audit objects without custom scheduling or maintenance. Further reading (AWS): - Remove PII from conversations by using sensitive information filters - Amazon Bedrock (AWS Documentation) - Processing Macie findings with Amazon EventBridge - Amazon Macie (AWS Documentation) - Expiring objects - Amazon Simple Storage Service (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 3
Correct
A retail bank has multiple development teams building internal assistants that use Amazon Bedrock FMs for customer support, HR, and compliance workflows. The bank’s risk team requires a single governance approach so that prompt templates and inference settings are centrally controlled with versioning and approvals, all FM interactions are auditable for internal reviews, and organization-wide policy controls are enforced consistently across all applications. Which solution will meet these requirements with the LEAST operational overhead?
Store prompt templates in Amazon S3 and require each team to deploy updates through their own CI/CD pipeline with peer code reviews. Configure each application to log prompt inputs and outputs to Amazon CloudWatch Logs.
Explanation
Incorrect. Although S3 and CI/CD can distribute templates, governance is fragmented across multiple pipelines and review processes, which reduces consistent oversight and increases operational overhead. This approach also lacks a centralized prompt governance system with standardized approval and lifecycle controls for prompt artifacts.
Your answer is correct
Use Amazon Bedrock Prompt Management to centrally store and version approved prompt templates (including variants). Apply Amazon Bedrock Guardrails to enforce the bank’s policy controls, and use AWS CloudTrail and Amazon CloudWatch Logs to provide auditability of prompt and model usage across teams.
Explanation
Correct. Bedrock Prompt Management provides a centralized, governed mechanism for prompt reuse and versioning across multiple applications. Guardrails provide consistent policy enforcement for inputs and outputs, and CloudTrail/CloudWatch Logs provide the audit trail needed for oversight and compliance reviews without building a custom governance platform.
Store prompts in AWS Secrets Manager and grant read access only to production roles. Use Amazon CloudWatch metrics to track the number of model invocations and configure alarms when usage spikes.
Explanation
Incorrect. Secrets Manager is designed for sensitive secrets and rotation, not prompt governance, versioned prompt lifecycle management, or organization-wide approvals. CloudWatch metrics provide usage visibility but do not provide the centralized governance and policy enforcement required for consistent oversight of FM implementations.
Register each team’s prompts as artifacts in Amazon SageMaker Model Registry and require manual approval before the teams can deploy their applications. Rely on SageMaker deployment guardrails to enforce controls on prompt updates.
Explanation
Incorrect. SageMaker Model Registry is intended for model versioning and deployment workflows, not for centrally governing Bedrock prompts across multiple applications. SageMaker deployment guardrails control model rollout behavior, not organization-wide prompt governance and enforcement for Bedrock-based FM interactions.
Overall explanation
An organizational governance system for FMs needs centralized control over the artifacts that drive behavior (prompts and configurations), consistent enforcement of policies, and auditability for oversight. Centralized prompt governance through Amazon Bedrock Prompt Management standardizes prompt reuse, versioning, and controlled rollout across teams. Amazon Bedrock Guardrails adds consistent policy enforcement for both inputs and outputs, reducing the chance of teams implementing uneven safety controls. AWS CloudTrail and Amazon CloudWatch Logs provide the evidence trail required for governance and compliance audits by recording access and usage patterns, enabling consistent oversight without custom-built governance infrastructure. Further reading (AWS): - Construct and store reusable prompts with Prompt management in Amazon Bedrock (AWS Documentation) - Detect and filter harmful content by using Amazon Bedrock Guardrails (AWS Documentation) - Implementing least privilege access for Amazon Bedrock (AWS Blog)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 4
Correct
A fintech development team is building a customer-facing chat assistant that invokes an Amazon Bedrock text model through an AWS Lambda function. The assistant must block harmful user inputs (such as profanity and requests about disallowed topics) before they influence model behavior, and the team wants the simplest approach that can be applied consistently across the application. Which solution will meet these requirements with the LEAST operational overhead?
Your answer is correct
Create an Amazon Bedrock guardrail with topic filtering and word filtering. Attach the guardrail to the model invocation so that blocked inputs return a standard blocked message.
Explanation
Correct. Amazon Bedrock guardrails provide managed input filtering capabilities (such as topic and word filtering) that can be applied directly during model invocation. This approach centralizes content safety controls, enforces consistent behavior across the application, and avoids building and operating a separate moderation pipeline.
Use an Amazon API Gateway request validator with a JSON Schema model to enforce a strict request format. Add a system prompt telling the model to refuse any request that contains unsafe content.
Explanation
Incorrect. Schema validation ensures the request is structurally correct, not that the content is safe. Relying on a system prompt alone is not a robust content safety control because user inputs can still attempt to override instructions (prompt injection) and produce unsafe behavior.
Configure AWS WAF on Amazon API Gateway with regex pattern matching to detect harmful prompts and block requests that match those patterns before invoking the Lambda function.
Explanation
Incorrect. AWS WAF can help block common web exploits and can match simple patterns, but it is not designed as a comprehensive content safety layer for GenAI prompts. It cannot reliably classify disallowed topics or nuanced harmful content in natural language the way a model-focused safety control can.
Store approved prompts in Amazon Bedrock Prompt Management and require a manual approval workflow for all prompts before the application can send them to the model.
Explanation
Incorrect. Prompt Management governs reusable prompt templates and versions, but it does not provide real-time moderation of end-user inputs. A manual approval workflow is also not practical for interactive, customer-facing chat requests and adds significant operational burden.
Overall explanation
A managed input safety control that directly filters user prompts at invocation time is the most straightforward way to prevent harmful inputs from shaping model behavior. Bedrock guardrails are purpose-built for GenAI content filtering and can enforce policies like topic and word blocking while returning a consistent blocked response. Network-layer protections and schema validation can be useful complementary controls, but they do not provide reliable natural-language content moderation. Governance features for prompt templates help standardize developer-authored prompts, but they do not solve real-time user input safety requirements. Further reading (AWS): - Include a guardrail with the Converse API - Amazon Bedrock (AWS Documentation) - Block denied topics to help remove harmful content - Amazon Bedrock (AWS Documentation) - Remove a specific list of words and phrases from conversations with word filters - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 5
Incorrect
A fintech startup is building a GenAI feature that is used by two clients: a mobile app and an internal microservice running on Amazon ECS. For short, interactive prompts, users must receive a response immediately. For long-running requests (such as summarizing multi-page documents), the startup is willing to let users retrieve the result later. The startup also wants a single, stable API in front of the solution that validates required JSON fields before the request is processed, with the LEAST operational overhead. Which solution will meet these requirements?
Store prompt templates and model IDs in AWS AppConfig. Have the mobile app and the ECS service read the configuration at startup and then invoke the Amazon Bedrock Runtime API directly. Use Amazon SNS to trigger multiple Lambda functions to summarize different sections of each document in parallel and combine the results in the client.
Explanation
Incorrect. AppConfig helps with dynamic configuration, but it does not provide a unified API surface with request validation. SNS fanout and client-side aggregation increases complexity and shifts orchestration responsibilities to clients instead of using a straightforward asynchronous queue-based processing pattern.
Use Amazon Cognito to issue temporary credentials to the mobile app and the ECS service. Have both clients call the Amazon Bedrock Runtime (bedrock-runtime) API directly for all prompts, including long-running document summarization. Perform JSON field validation in each client application before invoking the model.
Explanation
Incorrect. This does not provide a single stable API layer with centrally managed request validation; each client must implement validation logic independently. It also does not provide an asynchronous processing mechanism for long-running requests, which can lead to poor user experience and operational challenges during bursts.
Your answer is incorrect
Create an Application Load Balancer (ALB) in front of an Amazon ECS service that validates requests and invokes the Amazon Bedrock Runtime API. For long-running requests, start an AWS Step Functions workflow that calls Bedrock and returns the final response when the workflow completes.
Explanation
Incorrect. This approach adds operational overhead by introducing and operating an ECS service for request validation and orchestration logic. It also does not align with the requirement to let users retrieve results later; returning the final response only after the workflow completes is still a synchronous interaction pattern for the long-running path.
Correct answer
Create an Amazon API Gateway REST API with a request validator and JSON schema model. For synchronous requests, invoke an AWS Lambda function that calls the Amazon Bedrock Runtime (bedrock-runtime) API. For long-running requests, the Lambda function places a message on an Amazon SQS queue and returns a job ID. A second Lambda function polls the queue, invokes the Bedrock Runtime API, and stores the result for later retrieval.
Explanation
Correct. API Gateway provides a single front door for multiple client types and can validate request payloads with a request validator and schema. Using Bedrock Runtime APIs from Lambda supports synchronous inference for interactive use cases. For long-running work, SQS decouples ingestion from processing, buffers bursts, and enables reliable asynchronous processing with a separate consumer Lambda that invokes Bedrock and stores results for later retrieval.
Overall explanation
The most flexible pattern for model interactions is to present a consistent, validated API surface to all callers while choosing synchronous or asynchronous execution based on the workload. API Gateway provides a stable endpoint and native request validation so malformed requests are rejected early. For immediate responses, a Lambda function can invoke Amazon Bedrock through the Bedrock Runtime APIs. For long-running jobs, placing work on Amazon SQS decouples ingestion from processing, absorbs traffic spikes, and lets a separate consumer invoke the model and persist results for later retrieval. Alternatives either force all calls to be synchronous, duplicate validation logic across clients, or introduce unnecessary operational complexity. Further reading (AWS): - Request validation for REST APIs in API Gateway - Amazon API Gateway (AWS Documentation) - Using Lambda with Amazon SQS - AWS Lambda (AWS Documentation) - InvokeModel - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 6
Correct
A media company is building an internal “chat with your documents” assistant by using Amazon Bedrock Knowledge Bases backed by Amazon OpenSearch Service. Source documents are stored in Amazon S3 and include multiple versions of policies written by different teams. Users frequently ask questions such as “What is the latest policy for content takedown?” and need the response to reflect the correct business unit and the most recent document. The company wants to improve retrieval precision and give the FM better context (for example, document timestamps and authorship) with the LEAST operational overhead. Which solution meets these requirements?
Regenerate embeddings with a larger vector dimensionality for the embedding model so the vector store can better distinguish older and newer versions of similar documents.
Explanation
Incorrect. Increasing embedding dimensionality can increase storage and compute cost and does not directly solve the need to reliably identify and prefer the most recent version or the correct business unit. Versioning, timestamps, and ownership are better handled as explicit metadata rather than relying on semantic differences in embeddings.
Build a custom post-processing Lambda function that invokes an FM to extract author and timestamp from each retrieved chunk at query time and then reranks the results before sending them to the user.
Explanation
Incorrect. This adds additional FM invocations and custom logic on every query, increasing cost and operational complexity. It is also less reliable than storing metadata once at ingestion time and using it consistently for retrieval and context.
Increase the chunk size and overlap settings so each chunk contains the document cover page that lists the author and publication date, ensuring the FM always receives this context in the prompt.
Explanation
Incorrect. Forcing cover-page details into every chunk increases token usage and introduces noise that can reduce retrieval quality. It also mixes metadata into the chunked content, making it harder to maintain and less reliable for precision retrieval compared to a structured metadata approach.
Your answer is correct
Define a standardized metadata schema (for example, business_unit, author, document_timestamp, topic tags). Store timestamps and authorship as S3 object metadata or as extracted fields, and configure the knowledge base to treat these fields as metadata (not content) so retrieval can use them to improve ranking and context.
Explanation
Correct. A metadata framework that separates content from metadata (such as timestamps, authorship, and domain tags) improves precision and context awareness without changing the core RAG architecture. Keeping metadata out of chunk text prevents it from being fragmented during chunking, while still making it available to retrieval and to the FM for grounding responses and selecting the most appropriate document version.
Overall explanation
To improve retrieval precision for RAG, document attributes like timestamps, authorship, business unit ownership, and domain/topic tags should be captured as explicit metadata and kept separate from chunked text. This lets the retrieval layer use consistent, structured signals to select the right documents (especially among near-duplicates and multiple versions) while also providing the FM with better context for grounded responses. Approaches that try to “hide” metadata inside chunk text, increase embedding dimensionality, or perform query-time extraction add cost, noise, or operational burden without directly addressing versioning and ownership precision. Further reading (AWS): - Include metadata in a data source to improve knowledge base query - Amazon Bedrock (AWS Documentation) - Configure and customize queries and response generation - Amazon Bedrock (AWS Documentation) - Amazon Bedrock Knowledge Bases now supports metadata filtering to improve retrieval accuracy (AWS Blog)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 7
Correct
A healthcare SaaS provider exposes an internal GenAI summarization service through Amazon API Gateway and an AWS Lambda function that invokes an Amazon Bedrock text model. The provider expects to switch between different Bedrock models over time as pricing and regional availability change. The provider must be able to change the model selection without modifying or redeploying the Lambda code, and must be able to roll out a model change gradually with the ability to roll back automatically if errors increase. Which solution will meet these requirements with the LEAST operational overhead?
Configure Amazon API Gateway stage variables to store the Bedrock modelId and use a mapping template to pass the stage variable to the Lambda function for each request. Update the stage variable when switching models.
Explanation
Incorrect. Although stage variables can externalize a value, switching models still requires updating the API Gateway stage/deployment configuration. This approach also lacks purpose-built configuration validation, progressive rollout controls, and automated rollback capabilities compared with AppConfig.
Store the target Bedrock modelId as a Lambda environment variable. Update the environment variable value whenever the provider needs to switch models, and redeploy the Lambda function.
Explanation
Incorrect. Updating environment variables requires a Lambda configuration update and redeployment of the function configuration. This does not provide controlled, gradual rollout or automatic rollback based on monitoring signals.
Your answer is correct
Use AWS AppConfig to store a configuration profile (for example, a feature flag) that contains the Bedrock modelId and inference settings. Configure the Lambda function to retrieve the AppConfig configuration at runtime and invoke Bedrock based on the configuration. Use AppConfig deployment strategies and CloudWatch alarms to roll out changes and trigger rollback.
Explanation
Correct. AppConfig is designed for dynamic configuration changes independent of code deployments. By reading model selection values at runtime, the Lambda code stays unchanged while model selection can be updated. AppConfig supports validation and progressive deployments with rollback based on CloudWatch alarms, which meets the gradual rollout and automatic rollback requirement with minimal operational effort.
Create an Amazon DynamoDB table that stores the current Bedrock modelId. Configure the Lambda function to read the modelId from DynamoDB on each invocation and call Bedrock with that value. Update the DynamoDB item to switch models.
Explanation
Incorrect. While this can enable changing the model without redeploying code, it introduces unnecessary operational complexity and does not natively provide configuration validation, gradual rollout strategies, or automatic rollback mechanisms for configuration changes.
Overall explanation
A flexible model-selection pattern separates the model choice from the application code so the team can switch FMs or providers without a code redeploy. AWS AppConfig is purpose-built for dynamic configuration management and supports validating configuration changes, deploying changes progressively, and rolling back when CloudWatch alarms indicate problems. By having the Lambda function retrieve model selection parameters from AppConfig at runtime, the API remains stable while the modelId and related settings can be changed safely and repeatedly with minimal operational overhead. Alternatives either require updating deployed components directly (Lambda or API Gateway) or use a general data store that lacks AppConfig’s deployment governance features. Further reading (AWS): - Configuring the AWS AppConfig Agent Lambda extension - AWS AppConfig (AWS Documentation) - Using predefined deployment strategies - AWS AppConfig (AWS Documentation) - Monitoring deployments for automatic rollback - AWS AppConfig (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 8
Correct
A customer support engineering team is building an agent-assist feature that uses Amazon Bedrock. For each incoming support ticket, the UI must display (1) a customer-ready response and (2) a short internal troubleshooting checklist. The team has found that generating both artifacts in a single prompt increases latency and cost. The team wants to use specialized foundation models (FMs) for each artifact and then combine the results into one JSON payload for the frontend. Which solution will meet these requirements with the LOWEST end-to-end latency?
Send each ticket to an Amazon SQS queue and use two separate AWS Lambda consumers to invoke different Amazon Bedrock models. Store results in Amazon S3 and have the frontend poll for completion.
Explanation
Incorrect. This design is asynchronous and introduces additional waiting and polling time, increasing end-to-end latency. It also shifts coordination complexity to the application layer instead of using an orchestration workflow designed for coordinated multi-model execution.
Invoke a single large FM in Amazon Bedrock and prompt it to return both the customer-ready response and the troubleshooting checklist in a single JSON response.
Explanation
Incorrect. This uses only one FM call and does not coordinate specialized FMs. It also tends to increase token usage and latency because the model must generate both outputs in one completion.
Your answer is correct
Use AWS Step Functions with a Parallel state that invokes two different Amazon Bedrock models at the same time (one optimized for concise summaries and one optimized for detailed responses). Use a final AWS Lambda task to merge both outputs into a single JSON payload.
Explanation
Correct. This implements a model coordination system: two specialized FMs run concurrently to minimize total wall-clock time, and a final aggregation step combines the outputs into one response. Step Functions provides managed orchestration and supports service integrations for invoking Bedrock models, while Lambda can implement the custom aggregation logic.
Enable Amazon Bedrock Intelligent Prompt Routing so Bedrock automatically selects the best model for each ticket and returns a single response that includes both artifacts.
Explanation
Incorrect. Prompt routing selects a model for a request but does not coordinate multiple specialized models to produce multiple artifacts concurrently, and it does not provide an aggregation step to combine outputs from multiple model invocations.
Overall explanation
The key requirement is coordinating multiple specialized FMs and combining their outputs while minimizing latency. Running the two model invocations in parallel reduces the critical path because both artifacts are generated at the same time. A final aggregation step (for example, a Lambda function) can deterministically assemble the customer-facing response and the internal checklist into a single JSON payload. Orchestrating this with Step Functions provides a managed coordination layer with built-in state management, error handling, and observability, which is well suited for multi-model “orchestrator + workers + synthesizer” patterns. Further reading (AWS): - Parallel workflow state - AWS Step Functions (AWS Documentation) - Invoke and customize Amazon Bedrock models with Step Functions - AWS Step Functions (AWS Documentation) - Orchestrate generative AI workflows with Amazon Bedrock and AWS Step Functions | Artificial Intelligence (AWS Blog)
Domain
Content Domain 2: Implementation and Integration
Question 9
Correct
A platform team at a regulated financial institution is building multiple internal applications that invoke Amazon Bedrock FMs for tasks such as summarization and drafting customer communications. The team must ensure that all applications use consistent, centrally managed prompts. Prompt changes must be reviewed and approved before they can be used in production, and auditors must be able to determine who used which prompt version and when. Which solution will meet these requirements with the LEAST operational overhead?
Your answer is correct
Use Amazon Bedrock Prompt Management to create reusable, parameterized prompts with versioning and an approval workflow. Store the prompt template repository in Amazon S3 as the source of truth. Enable AWS CloudTrail to audit Bedrock API usage and send application prompt/response access logs to Amazon CloudWatch Logs.
Explanation
Correct. Bedrock Prompt Management provides centralized prompt storage, parameterization (template variables), and version governance, including controlled promotion of prompt versions through an approval process. S3 provides a durable repository for prompt artifacts and related assets. CloudTrail gives an audit trail for API activity (who/when invoked Bedrock operations), while CloudWatch Logs provides centralized operational logging of prompt access and usage across applications for investigations and oversight.
Create an Amazon API Gateway front end that injects a standard system prompt by using request mapping templates. Enable AWS WAF to block prompt injection attempts and use access logs in API Gateway for auditing.
Explanation
Incorrect. API Gateway request mapping can standardize an injected instruction, but it does not provide centralized prompt lifecycle governance such as prompt versioning, parameterized templates, and approval workflows. WAF is a web security control and does not provide prompt governance. API Gateway logs capture API access, but do not provide Bedrock prompt governance or prompt version management across multiple applications.
Use Amazon SageMaker Model Registry to store and approve prompt templates as model package versions. Enable Amazon CloudWatch Logs for SageMaker endpoint invocation logging and use AWS CloudTrail for auditing.
Explanation
Incorrect. SageMaker Model Registry is intended for model versioning and deployment governance, not for managing and governing Bedrock prompt templates across applications. This approach introduces unnecessary components and does not directly implement prompt governance where the prompts are actually consumed (Bedrock Prompt Management), increasing operational overhead and complicating audits.
Store prompt templates as AWS Systems Manager Parameter Store parameters and inject them into AWS Lambda environment variables during deployment. Enable Amazon CloudWatch metrics for token usage to track prompt usage over time.
Explanation
Incorrect. Parameter Store and Lambda environment variables can distribute prompt text, but they do not provide a purpose-built prompt governance workflow (such as versioning and approvals) for Bedrock prompts. Token metrics track consumption, not who invoked which prompt version. This also increases operational overhead because each application must implement its own prompt retrieval, versioning, and audit mechanisms.
Overall explanation
A comprehensive prompt governance solution needs a centralized place to define prompts, parameterize them for reuse, control changes through versioning and approvals, and maintain auditability of prompt usage. Amazon Bedrock Prompt Management is designed for prompt lifecycle management and sharing across applications, while Amazon S3 can serve as a durable repository for prompt artifacts and related assets. For oversight and auditing, AWS CloudTrail provides an authoritative record of API activity, and Amazon CloudWatch Logs centralizes operational access logs from the applications that consume prompts. Approaches that rely on ad hoc storage (such as environment variables) or unrelated governance tools (such as model registries) either lack approvals/version control for prompts or require significant custom implementation effort. Further reading (AWS): - Construct and store reusable prompts with Prompt management in Amazon Bedrock (AWS Documentation) - Deploy a prompt to your application using versions in Prompt management - Amazon Bedrock (AWS Documentation) - Monitor Amazon Bedrock API calls using CloudTrail - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 10
Correct
A risk and compliance team is building an internal Q&A assistant by using Amazon Bedrock Knowledge Bases backed by an Amazon OpenSearch Service vector store. Source policy documents are stored in Amazon S3 and are frequently updated throughout the day (new versions, replacements, and deletions). Users report that answers sometimes reference outdated policy language. The team needs an automated data maintenance approach that detects document changes in near real time and keeps the vector store synchronized with the latest content with the LEAST operational overhead. Which solution meets these requirements?
Enable Amazon S3 Versioning and Amazon S3 Cross-Region Replication (CRR) for the document bucket. Configure the application to query both Regions’ vector stores and select the most recent response based on the S3 version ID.
Explanation
Incorrect. Replicating S3 objects does not automatically update embeddings or the vector store in either Region. Querying multiple vector stores adds complexity and cost, and it does not provide a direct synchronization mechanism to ensure the vector index reflects the latest document state.
Your answer is correct
Configure Amazon S3 event notifications for object create, overwrite, and delete events to Amazon EventBridge. Create an EventBridge rule that invokes an AWS Lambda function to call StartIngestionJob for the Amazon Bedrock knowledge base whenever relevant S3 changes occur.
Explanation
Correct. S3 events provide real-time change detection, and EventBridge + Lambda provides a serverless, automated synchronization workflow. Triggering a knowledge base ingestion job when documents change keeps the embeddings and vector store aligned with the current S3 content while minimizing infrastructure and operational burden.
Enable Amazon S3 server access logging to a separate logging bucket. Run an AWS Glue ETL job each night to parse the access logs, identify changed objects, and update the OpenSearch vector index.
Explanation
Incorrect. Access logs are primarily for auditing and are not a near-real-time change detection mechanism for keeping a vector store current. A nightly batch job also delays updates and introduces additional operational overhead for ETL management.
Create an Amazon EventBridge scheduled rule to call the Amazon Bedrock StartIngestionJob API for the knowledge base every 6 hours so the vector store is periodically refreshed.
Explanation
Incorrect. A scheduled refresh can help with maintenance, but running only every 6 hours does not provide near-real-time synchronization for frequently updated documents. It also performs unnecessary ingestion when no changes occurred, increasing cost and lag.
Overall explanation
Keeping a RAG vector store current requires both change detection and an automated synchronization mechanism that updates embeddings/index entries when source content changes. Using S3 events routed through EventBridge to trigger a Lambda function provides near-real-time detection of new, updated, or deleted documents and automatically initiates a knowledge base ingestion job to refresh the underlying vector store. Alternatives based on periodic batch refreshes increase staleness windows and can waste resources, while replication and access-log-driven pipelines add complexity without directly ensuring that embeddings and vector indices are updated promptly. Further reading (AWS): - StartIngestionJob - Amazon Bedrock (AWS Documentation) - Using EventBridge - Amazon Simple Storage Service (AWS Documentation) - Event bus targets in Amazon EventBridge - Amazon EventBridge (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 11
Correct
A security engineering team is reviewing an internal customer-support chatbot that uses an Amazon Bedrock agent with action groups (AWS Lambda tools) to look up account details and open support tickets. During a pilot, testers were able to craft prompts such as “ignore previous instructions” to attempt to override tool-use rules and to extract the agent’s hidden instructions. The team wants to add real-time protection against prompt injection and jailbreak attempts and also run automated adversarial tests whenever the team updates prompt templates, with the LEAST operational overhead. Which solution meets these requirements?
Fine-tune a custom foundation model to refuse requests that attempt to override instructions. Deploy the fine-tuned model and require all chatbot requests to use only the fine-tuned model.
Explanation
Incorrect. This approach adds significant cost and operational effort, and it relies on model customization rather than implementing threat detection mechanisms and automated adversarial testing workflows around the application. Additionally, model development and training activities are not the focus for this exam role.
Your answer is correct
Attach Amazon Bedrock Guardrails to the agent invocation. Add a Lambda pre-processing layer that sanitizes user input and detects common prompt-injection and jailbreak patterns (for example, with pattern matching and named entity recognition). Use AWS Step Functions to run a scheduled or pipeline-triggered adversarial prompt suite against the deployed prompts and publish failures to Amazon CloudWatch for alerting.
Explanation
Correct. Bedrock Guardrails provide a managed safety control plane for filtering and policy enforcement, and a Lambda pre-processing layer enables targeted detection and sanitization for adversarial prompt patterns before the request reaches the model or tools. Step Functions provides a serverless orchestration layer to automate adversarial testing (for example, running a curated set of jailbreak prompts against new prompt versions) and can integrate with CloudWatch for monitoring and alerting, minimizing operational overhead while adding defense-in-depth.
Store prompts in Amazon Bedrock Prompt Management with versioning and approval workflows. Enable AWS CloudTrail and Amazon CloudWatch Logs for full request auditing and investigate suspicious prompts manually.
Explanation
Incorrect. Prompt Management and audit logs improve governance and traceability, but they do not provide active, real-time detection and blocking of prompt injection and jailbreak attempts. Manual investigation does not satisfy the requirement for automated adversarial testing when prompts change.
Configure Amazon API Gateway request validation with a JSON Schema to enforce required fields and maximum request size. Enable AWS WAF managed rules for SQL injection and cross-site scripting (XSS) on the API Gateway endpoint.
Explanation
Incorrect. API Gateway schema validation and AWS WAF managed rules help protect web APIs, but they do not reliably detect or mitigate LLM-specific threats such as prompt injection, jailbreak attempts, or instruction leakage. They also do not provide an automated adversarial testing workflow for prompt updates.
Overall explanation
The best approach combines managed safety controls with application-layer defenses and continuous validation. Bedrock Guardrails can be applied at invocation time to enforce content and policy controls consistently. A Lambda pre-processing step can sanitize and classify inputs to detect prompt-injection and jailbreak patterns before the agent executes tool calls, reducing the chance of adversarial instructions reaching the model or tools. A Step Functions workflow can then automate adversarial testing by replaying a known suite of attack prompts whenever prompts are updated (or on a schedule), and it can emit results to CloudWatch for alerting and regression monitoring. The other approaches either focus on generic API protections, provide only auditing without prevention, or shift to heavy model customization instead of implementing threat detection and testing workflows. Further reading (AWS): - Prompt injection security - Amazon Bedrock (AWS Documentation) - Test your guardrail - Amazon Bedrock (AWS Documentation) - Add an action group to your agent in Amazon Bedrock - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 12
Incorrect
A SaaS provider is building an internal GenAI assistant that must call existing operational tools through Model Context Protocol (MCP) so multiple agent frameworks can reuse the same tool interface. One tool is a simple, stateless lookup that returns small JSON responses. Another tool performs CPU-intensive document processing that relies on native libraries and requires a longer-running process. The team wants a design that uses MCP consistently while keeping operational overhead LOW. Which solution meets these requirements with the LEAST operational overhead?
Correct answer
Implement the stateless lookup tool as a Lambda-based MCP server. Implement the document-processing tool as an MCP server running on Amazon ECS with AWS Fargate. Use MCP client libraries in each agent to call both MCP servers through a consistent interface.
Explanation
Correct. A Lambda-based MCP server is well suited for lightweight, stateless tool access with minimal infrastructure management. A container-based MCP server on ECS (with Fargate) is better for complex tools that need native dependencies and longer-running execution. Using MCP client libraries in the agents standardizes access patterns across frameworks and avoids building custom per-tool integrations.
Deploy both tools as MCP servers on Amazon EKS and use Kubernetes operators to manage scaling, upgrades, and networking for the MCP services.
Explanation
Incorrect. EKS can host MCP servers, but it introduces significantly more operational overhead than Lambda and ECS with Fargate (cluster lifecycle management, add-ons, networking, and upgrades). This conflicts with the requirement to minimize operational overhead.
Your answer is incorrect
Deploy a single MCP server on Amazon EC2 that hosts both tools and exposes them over HTTP streaming. Install and manage all dependencies on the EC2 instance and scale the instance vertically as demand grows.
Explanation
Incorrect. While this could provide MCP access, it increases operational overhead (instance management, patching, scaling strategy, and dependency management). It also creates a single-host design that is harder to scale and operate compared to serverless and managed container options.
Implement both tools as AWS Lambda functions that the agent invokes directly with the AWS SDK. Use shared utility code in the agent to normalize inputs and outputs instead of MCP.
Explanation
Incorrect. This bypasses MCP, so the tools are not exposed through a standardized MCP interface. It also increases ongoing integration burden because each agent framework must implement and maintain custom invocation and schema-handling logic rather than relying on MCP client libraries.
Overall explanation
A practical MCP-based extension framework typically separates lightweight, stateless tools from complex tools with heavier runtime requirements. Stateless tools map well to AWS Lambda because there are no servers to manage and scaling is automatic. Tools that need native libraries, larger dependency trees, or longer-running execution are better packaged as containers and run on Amazon ECS with AWS Fargate to avoid managing EC2 hosts. Using MCP client libraries in the agents preserves a consistent, reusable access pattern across different agent frameworks and reduces custom integration work. Further reading (AWS): - Flexibility to Framework: Building MCP Servers with Controlled Tool Orchestration (AWS Blog) - What is AWS Lambda? (AWS Documentation) - Architect for AWS Fargate for Amazon ECS (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 13
Incorrect
An online retailer is building a customer-facing chat experience that uses Amazon Bedrock LLMs. The team wants to route each user message to a low-latency model for simple requests and to a higher-quality model for complex requests. During traffic spikes, the higher-quality model occasionally experiences throttling, and the team wants requests to automatically fall back to the low-latency model when throttling or high latency is detected. The team also wants a clear audit trail of how each request was routed. Which solution will meet these requirements with the LEAST custom application logic?
Enable Amazon Bedrock cross-Region inference and create an inference profile so Bedrock automatically distributes requests. Use a single modelId in the application and rely on the inference profile to handle both complexity-based model selection and throttling fallback.
Explanation
Incorrect. Cross-Region inference helps with distributing inference across Regions, but it does not implement content-based routing across different models for “simple vs complex” requests. It also does not provide an auditable routing workflow that shows why a different model was selected.
Your answer is incorrect
Implement all routing in application code inside a single AWS Lambda function. The function calls Amazon Bedrock, checks for throttling errors, retries with exponential backoff, and then calls a different model if throttling persists. Log routing decisions to Amazon CloudWatch Logs.
Explanation
Incorrect. Although feasible, this approach requires more custom logic to implement orchestration, branching, retries, and audit semantics in code. It also provides a less structured audit trail than an orchestrated workflow that records state transitions and decisions.
Create two Amazon API Gateway endpoints (for example, /fast and /accurate). Configure the client application to call the appropriate endpoint based on request complexity. Use separate AWS Lambda integrations to invoke the corresponding Bedrock model.
Explanation
Incorrect. This pushes routing decisions to the client and does not provide automatic fallback based on throttling or latency conditions. It also increases client complexity and does not inherently provide a centralized, auditable routing workflow.
Correct answer
Use Amazon API Gateway to invoke an AWS Step Functions state machine. In the state machine, run a classification step to determine request complexity, then use Choice states to route to the appropriate Amazon Bedrock InvokeModel task. Add retry logic and a fallback branch that routes to the low-latency model when throttling or latency thresholds are exceeded, and store routing outcomes in the execution history.
Explanation
Correct. Step Functions provides a managed orchestration layer for dynamic, content-based model routing and can implement conditional branching, retries, and fallback behavior. The execution history provides an audit trail of routing decisions and outcomes with minimal custom code beyond small classification/threshold checks.
Overall explanation
A good model-routing design needs two decision points: determine the best model for the request content (simple vs complex) and determine whether operational conditions require a fallback (for example, throttling or elevated latency). A Step Functions workflow is well-suited to this because it can orchestrate a classification step, branch to specialized model invocations, apply retries and fallback paths, and preserve an end-to-end record of decisions and outcomes through execution history. Client-driven routing does not satisfy automatic fallback requirements, a single Lambda-based router increases custom code and reduces structured traceability, and cross-Region inference addresses Regional capacity—not routing between different models based on content and runtime conditions. Further reading (AWS): - Invoke and customize Amazon Bedrock models with Step Functions (AWS Documentation) - Handling errors in Step Functions workflows (AWS Documentation) - Viewing execution details in the Step Functions console (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 14
Correct
A financial services analytics team is building a document-summarization workflow by using an Amazon Bedrock text model. Each night, a new batch of customer interaction transcripts is delivered as JSON files to an Amazon S3 bucket. Some files are missing required fields (for example, transcriptText), and some contain empty strings that cause poor model responses. The team needs an automated validation workflow that can enforce data quality rules before the transcripts are sent for FM inference and that can publish pass/fail results as operational metrics for monitoring. Which solution will meet these requirements with the LEAST operational overhead?
Your answer is correct
Create an AWS Glue ETL job that reads the JSON files from Amazon S3 and evaluates an AWS Glue Data Quality ruleset (DQDL) for required fields and non-empty values. Configure the job to fail when the ruleset fails, write failed records to a quarantine S3 prefix, and publish data quality results to Amazon CloudWatch metrics for alarms and dashboards.
Explanation
Correct. AWS Glue Data Quality is designed for automated, rule-based validation and can be embedded directly into Glue jobs. The ruleset can enforce completeness and basic validity checks (such as required fields and non-empty strings). Failing the job prevents downstream FM inference on bad data, while quarantining isolates invalid records for later remediation. Emitting results to CloudWatch provides the required operational visibility with minimal custom code.
Create an AWS Lambda function that is triggered by S3 object creation events. The function checks whether the JSON is valid and then sends the data directly to the Amazon Bedrock model. Store any errors in Amazon CloudWatch Logs for later review.
Explanation
Incorrect. While Lambda can perform basic checks, implementing and maintaining comprehensive validation logic and pass/fail reporting is custom work and increases operational overhead compared to using a managed data quality capability. Logging errors to CloudWatch Logs alone does not provide a standardized, rule-based validation workflow with explicit data quality results published as metrics.
Use Amazon SageMaker Data Wrangler in SageMaker Studio to visually inspect the transcript dataset for missing values, correct the records manually, and export the cleaned data back to Amazon S3 for the summarization workflow.
Explanation
Incorrect. Data Wrangler is well suited for interactive data preparation and exploration, but relying on manual inspection and correction does not provide an automated validation workflow for recurring nightly batches. This approach also increases ongoing operational overhead and does not inherently enforce or continuously report pass/fail rule evaluations as monitoring metrics.
Run scheduled Amazon Athena queries that look for missing fields and empty strings in the transcript dataset stored in Amazon S3. Use the query results to decide whether to run the summarization workflow on the next schedule.
Explanation
Incorrect. Athena can detect data issues through queries, but this approach requires additional orchestration and does not inherently integrate pass/fail enforcement into the ingestion workflow. It also adds operational complexity to translate query results into consistent validation outcomes and monitoring signals compared to a Glue job with Glue Data Quality and CloudWatch metrics.
Overall explanation
The most operationally efficient approach is to implement validation where the batch data is already being processed and to use managed, rule-based checks. AWS Glue Data Quality can evaluate explicit rules (such as required keys and non-empty values) as part of an AWS Glue job, and it can be configured to fail processing when quality thresholds are not met so that invalid data does not reach the foundation model. Publishing the evaluation results to Amazon CloudWatch metrics enables dashboards and alarms without building a separate reporting system. Alternatives either rely on manual steps, require significant custom validation code, or add extra orchestration that increases operational overhead. Further reading (AWS): - Getting started with AWS Glue Data Quality for the Data Catalog - AWS Glue (AWS Documentation) - EvaluateDataQuality class - AWS Glue (AWS Documentation) - Data Quality Definition Language (DQDL) reference - AWS Glue (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 15
Correct
An enterprise platform team at a financial institution is building a centralized “GenAI gateway” that internal applications must use to access Amazon Bedrock models. The gateway must enforce consistent request validation and throttling, record an audit trail of model access, and be deployed through an automated CI/CD pipeline that includes security checks. If a new release causes increased errors or latency, the deployment must automatically roll back. Which solution meets these requirements with the LEAST operational overhead?
Front the gateway with an Application Load Balancer that routes to Amazon ECS tasks running a custom proxy service. Use AWS CodePipeline to build and push container images to Amazon ECR. Perform blue/green deployments in the ECS service without automated tests or security scans to keep the pipeline simple.
Explanation
Incorrect. While ECS blue/green can reduce deployment risk, omitting automated tests and security scans violates the security-check requirement. This option also adds operational overhead compared to a serverless API Gateway + Lambda approach for a gateway-style workload.
Use AWS Step Functions to coordinate model invocations and error handling. Trigger Step Functions deployments by using Amazon EventBridge schedules that run AWS CLI scripts in AWS Lambda to update resources. Use CloudWatch Logs for troubleshooting and manually redeploy the previous version if problems occur.
Explanation
Incorrect. This uses custom scripting for deployments instead of a managed CI/CD pipeline with automated security checks and rollback support. Manual redeployments increase operational overhead and do not meet the automatic rollback requirement.
Your answer is correct
Deploy the gateway as Amazon API Gateway integrated with AWS Lambda that invokes Amazon Bedrock. Use AWS CodePipeline to orchestrate deployments from source control. Run automated tests and security scans in AWS CodeBuild, deploy with AWS CodeDeploy using canary traffic shifting for the Lambda alias, and configure CloudWatch alarms to trigger automatic rollback. Enable CloudTrail and CloudWatch Logs for auditing and observability.
Explanation
Correct. API Gateway provides a centralized entry point with request validation and throttling. CodePipeline and CodeBuild provide a managed CI/CD workflow with automated testing and security scans. CodeDeploy can shift traffic gradually to a new Lambda version and automatically roll back when CloudWatch alarms detect elevated errors or latency. CloudTrail and CloudWatch Logs provide auditability and operational visibility with minimal custom operational work.
Expose Amazon Bedrock directly to internal applications. Store prompt templates in Amazon S3 with versioning enabled. Use AWS CloudTrail to audit Bedrock API calls and rely on developers to manually roll back by reverting the S3 objects when a deployment causes issues.
Explanation
Incorrect. This lacks a centralized gateway layer to consistently enforce request validation and throttling across applications. Manual rollback increases operational overhead and does not provide automated rollback based on runtime health signals.
Overall explanation
A centralized GenAI gateway is best implemented with API Gateway in front of a Lambda layer that invokes Bedrock so the organization can standardize request validation, throttling, and access patterns. A managed CI/CD pipeline uses CodePipeline for orchestration and CodeBuild to run automated tests (for example, contract tests for request/response formats and prompt regression tests) and security scans before deployment. For safe releases, CodeDeploy can perform canary traffic shifting for a Lambda alias and automatically roll back when CloudWatch alarms detect increased error rates or latency. CloudTrail and CloudWatch Logs provide the audit trail and observability required for secure and compliant enterprise consumption patterns. Further reading (AWS): - Throttle requests to your REST APIs for better throughput in API Gateway (AWS Documentation) - Monitor Amazon Bedrock API calls using CloudTrail (AWS Documentation) - Deploying serverless applications gradually with AWS SAM (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 16
Correct
A large enterprise is building a Retrieval Augmented Generation (RAG) application by using Amazon Bedrock. The team stores embeddings and document chunks in Amazon OpenSearch Service and has grown to tens of millions of chunks across multiple business domains (HR policies, engineering runbooks, and legal templates). Users are experiencing increased query latency during peak hours, and the OpenSearch cluster shows signs of JVM memory pressure. The team also needs to tune index settings and, if necessary, use different embedding approaches for different domains. Which design will optimize semantic search performance at scale MOST effectively?
Move older vectors to UltraWarm storage and increase the replica count to improve read scalability for semantic search queries.
Explanation
Incorrect. UltraWarm is a cost-optimization tier primarily for indices with few writes and is slower than hot storage. Increasing replicas can help read throughput, but it does not address the primary performance bottleneck for vector search caused by sharding strategy and query coordination, and it does not provide domain-specific tuning.
Keep a single OpenSearch index for all domains and increase the number of shards significantly (for example, many small 5-GB shards) to maximize parallelism during k-NN searches.
Explanation
Incorrect. For vector (semantic) search, too many shards increases cross-shard coordination overhead and can contribute to JVM memory pressure. Many small shards often reduce performance and stability rather than improving it at scale.
Store embeddings and metadata in Amazon DynamoDB and use Scan operations with FilterExpression to find the nearest vectors for each query.
Explanation
Incorrect. DynamoDB can store vectors but does not provide vector similarity search for k-nearest-neighbor retrieval. Performing scans would be inefficient and would not meet low-latency semantic retrieval requirements at scale.
Your answer is correct
Implement a multi-index strategy with separate indexes per domain and use a hierarchical indexing approach: a small top-level summary/metadata index routes queries to the appropriate domain index. Use fewer, larger shards for the domain indexes to reduce cross-shard coordination for vector search.
Explanation
Correct. Separating domains into dedicated indexes enables domain-specific tuning (including embedding approaches and index settings). A hierarchical (summary-to-detail) index pattern reduces the search space and improves routing, while fewer, larger shards reduce coordination overhead and help improve vector search performance and cluster stability at scale.
Overall explanation
High-performance semantic retrieval at scale is heavily influenced by index architecture and shard strategy. Splitting data into multiple indexes by domain allows each corpus to be tuned independently (for example, different mappings, shard sizes, and embedding strategies), which is difficult with a single monolithic index. A hierarchical approach, where a small top-level index helps route queries to a domain-specific index, reduces unnecessary searching across unrelated corpora and improves relevance and latency. For vector search, using fewer, larger shards helps reduce cross-shard coordination and can mitigate cluster stability issues such as JVM memory pressure that are commonly associated with excessive shard counts. Further reading (AWS): - Choosing the number of shards - Amazon OpenSearch Service (AWS Documentation) - AOSPERF01-BP03 Check the number of shards per GiB of heap memory - Amazon OpenSearch Service Lens (AWS Documentation) - Amazon OpenSearch Service’s vector database capabilities explained (AWS Blog)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 17
Correct
A financial services engineering team is building an internal assistant that uses Amazon Bedrock to handle complex billing questions (for example, “Explain why I was charged a late fee, recalculate it based on these dates, and draft a customer response”). The assistant must break each request into structured reasoning steps and take actions by calling tools such as AWS Lambda functions that query Amazon DynamoDB. The team also needs a complete, auditable execution history with built-in retries and timeout controls, and wants to minimize custom orchestration code. Which solution will meet these requirements with the LEAST operational overhead?
Use Amazon EventBridge rules to trigger a sequence of Lambda functions: one Lambda invokes the model to generate a plan, another Lambda queries DynamoDB, and a final Lambda calls the model again to generate the response.
Explanation
Incorrect. EventBridge is effective for event-driven integration, but it does not provide a stateful, ordered, multi-step workflow with built-in branching and step-level retries/timeouts in the way Step Functions does. Coordinating a structured reasoning loop across multiple EventBridge-triggered functions would add complexity and reduce auditability of the end-to-end execution.
Build a single AWS Lambda function that runs a custom while-loop: call the Bedrock runtime API to generate a plan, invoke DynamoDB directly for needed data, and keep iterating until the model returns a final response. Log intermediate steps to Amazon CloudWatch Logs for troubleshooting.
Explanation
Incorrect. Although this can implement an agent loop, it shifts orchestration, retries/timeouts, and state management into custom code. CloudWatch Logs provide observability, but they do not provide the same managed workflow execution history, branching control, and standardized retry/timeout semantics that Step Functions provides for multi-step orchestration.
Your answer is correct
Implement an AWS Step Functions state machine that alternates between an Amazon Bedrock InvokeModel task to decide the next step (reasoning) and Lambda tasks to perform the selected action (acting). Use Choice states for branching and termination conditions, and use Step Functions error handling and retries for tool failures.
Explanation
Correct. Step Functions is well-suited for implementing a ReAct-style loop: the model produces a structured “next action” decision, the workflow invokes the corresponding tool, then the result is fed back into the next model invocation until a stop condition is reached. Step Functions provides managed orchestration features (Choice branching, retries, timeouts, and execution history) that satisfy auditing and control requirements without requiring extensive custom orchestration code.
Create an Amazon API Gateway REST API that uses mapping templates to call Amazon Bedrock, then calls DynamoDB, and then returns a final response in a single request-response cycle without additional orchestration services.
Explanation
Incorrect. API Gateway mapping templates and request/response transformations are not designed to orchestrate multi-step, conditional reasoning and tool execution. This approach lacks a managed mechanism for iterative reasoning/acting loops, branching logic, and built-in retries across multiple steps.
Overall explanation
To give an FM the ability to break down a complex problem into structured steps, the architecture needs an explicit control plane that can repeatedly: (1) ask the model what to do next, (2) execute the chosen action with well-defined tool boundaries, and (3) feed observations back into the next reasoning step until completion. AWS Step Functions is designed for this kind of orchestration and can implement ReAct-style patterns by combining Bedrock model invocations with tool invocations, conditional branching, and termination logic. It also provides managed retries, timeout controls, and a full execution history that supports auditing. Approaches that rely on a single Lambda loop or loosely coordinated events can work functionally, but they increase custom code and reduce the quality and consistency of workflow control and traceability. Further reading (AWS): - Invoke and customize Amazon Bedrock models with Step Functions (AWS Documentation) - Handling errors in Step Functions workflows (AWS Documentation) - Choosing workflow type in Step Functions (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 18
Correct
A media company is building a generative AI feature that summarizes long articles by using Amazon Bedrock. The team regularly runs Amazon Bedrock Model Evaluations on a fixed prompt dataset to compare two candidate FMs and track metrics such as correctness, helpfulness, and logical coherence over time. Product stakeholders want a recurring, easy-to-consume report that highlights trends and provides clear model comparison visualizations without requiring engineers to manually compile results each week. Which solution will meet these requirements with the LEAST operational overhead?
Use Amazon CloudWatch Logs Insights to query Amazon Bedrock invocation logs for latency and token usage. Create Amazon CloudWatch dashboards and have engineers share exported dashboard screenshots in a weekly email.
Explanation
Incorrect. CloudWatch dashboards are useful for operational monitoring, but this approach requires manual compilation and distribution of results, which violates the requirement to avoid manual weekly reporting. It also does not inherently provide model evaluation comparison visualizations for stakeholder reporting.
Create an AWS Lambda function that parses each evaluation output file and writes summarized metrics into Amazon DynamoDB. Use Amazon SNS to email the summarized metrics to stakeholders every week.
Explanation
Incorrect. While this can automate distribution, it does not provide comprehensive, stakeholder-friendly visualization and model comparison reporting. It also introduces custom code and data modeling that is unnecessary when managed analytics and visualization services can present trends and comparisons more effectively.
Your answer is correct
Store Bedrock model evaluation outputs in Amazon S3. Use an AWS Glue crawler to create/update table definitions in the AWS Glue Data Catalog. Query the results with Amazon Athena and publish stakeholder-facing dashboards and reports in Amazon QuickSight.
Explanation
Correct. Bedrock evaluation results can be centrally stored in S3, then made queryable through the Glue Data Catalog and Athena. QuickSight is purpose-built for business analytics and provides shareable dashboards and reporting views that can clearly communicate trends and comparisons to non-engineering stakeholders. This design minimizes operational overhead by relying on managed, serverless services for storage, querying, and visualization.
Stream Amazon CloudWatch metrics for Bedrock invocations to Amazon Kinesis Data Firehose and index them into Amazon OpenSearch Service. Use OpenSearch Dashboards to create weekly model-comparison reports for stakeholders.
Explanation
Incorrect. This approach focuses on operational telemetry and requires provisioning and managing search/analytics infrastructure (OpenSearch) and ingestion pipelines. It also does not directly address reporting on Bedrock Model Evaluations results and adds unnecessary operational complexity compared to a serverless analytics + BI reporting pattern.
Overall explanation
A low-operations reporting system for FM implementations typically separates storage, analytics, and visualization. Amazon Bedrock Model Evaluations can produce structured outputs that are easy to store in Amazon S3. Registering that data through the AWS Glue Data Catalog makes it straightforward to query with Amazon Athena. Amazon QuickSight then provides the stakeholder-friendly layer for dashboards and reports, enabling clear model comparison visualizations and trend reporting without engineers manually assembling weekly summaries. Alternatives either add operational burden (building and operating ingestion/search stacks), rely on manual steps (sharing screenshots), or lack appropriate visualization and comparison capabilities (email-only summaries). Further reading (AWS): - Understand how the results of your model evaluation job are saved in Amazon S3 - Amazon Bedrock (AWS Documentation) - Using crawlers to populate the Data Catalog - AWS Glue (AWS Documentation) - Creating a dataset using Amazon Athena data - Amazon QuickSight (AWS Documentation)
Domain
Content Domain 5: Testing, Validation, and Troubleshooting
Question 19
Correct
A healthcare analytics team is building an internal chat application that uses Amazon Bedrock to answer questions by querying a reporting dataset in Amazon Athena. The team is concerned that the FM could occasionally generate inappropriate language in its answers or accidentally include sensitive information from retrieved context. The team also must ensure the application generates only read-only, deterministic SQL queries (SELECT statements) and never produces free-form SQL that could be unsafe. Which solution will meet these requirements with the LEAST operational overhead?
Place Amazon API Gateway in front of the application and enable AWS WAF managed rules to block profanity and SQL injection strings in incoming requests. Allow the FM to generate SQL and final answers without additional controls.
Explanation
Incorrect. WAF can help protect the API from malicious inbound requests, but it does not provide a robust framework for preventing harmful model outputs or for guaranteeing deterministic, read-only SQL generation. This also does not prevent the FM from producing unsafe SQL in its output.
Create a custom fine-tuned model in Amazon Bedrock that was trained to avoid harmful language and to output only SELECT statements. Invoke the custom model directly from the application without guardrails or query templating.
Explanation
Incorrect. Fine-tuning can reduce undesired behavior but does not provide a strong, deterministic control layer for SQL safety (for example, enforcing SELECT-only, schema-allowed queries) and adds cost and lifecycle overhead. Without guardrails and deterministic text-to-SQL controls, harmful outputs and unsafe SQL generation can still occur.
Post-process every FM response with Amazon Comprehend to detect toxic language and PII. If Comprehend flags the response, re-prompt the FM to rewrite the answer and to regenerate the SQL query.
Explanation
Incorrect. This approach adds custom orchestration and still relies on the FM to generate SQL directly, which is not deterministic and can still produce unsafe or malformed queries. Re-prompting also does not guarantee consistent compliance and increases latency and complexity.
Your answer is correct
Configure Amazon Bedrock Guardrails to filter and mask unsafe content in model responses. Use an AWS Lambda function to perform a text-to-SQL transformation by mapping user intent to a predefined set of parameterized SELECT-only query templates that are executed in Athena, and return only the query results to the FM for response generation.
Explanation
Correct. Bedrock Guardrails provide managed response filtering (for example, profanity/topic controls and sensitive information masking) to reduce harmful outputs. A Lambda-based text-to-SQL layer that maps intents to allowlisted, parameterized SELECT templates ensures deterministic, read-only queries and prevents free-form SQL generation, minimizing operational overhead compared to building and tuning multiple custom moderation and retry loops.
Overall explanation
The safest low-operations approach combines managed output controls with deterministic query generation. Amazon Bedrock Guardrails help prevent harmful responses by filtering or masking unsafe content in the model’s output. Separately, generating SQL through a controlled text-to-SQL transformation that maps user intent to allowlisted, parameterized SELECT templates prevents the FM from emitting arbitrary SQL and ensures the database interaction is deterministic and read-only. Approaches that rely on re-prompting, perimeter filtering, or fine-tuning alone do not provide the same deterministic guarantees and typically add more complexity or risk. Further reading (AWS): - Remove PII from conversations by using sensitive information filters - Amazon Bedrock (AWS Documentation) - Block harmful words and conversations with content filters - Amazon Bedrock (AWS Documentation) - Use parameterized queries - Amazon Athena (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 20
Correct
A fintech company’s GenAI team is building an internal assistant that generates short compliance summaries by invoking an Amazon Bedrock text model. The assistant is called synchronously from an API, and users expect responses in near real time. During predictable weekday peaks, the team receives throttling errors from Bedrock and must increase available throughput while continuing to use the same model and keeping operational overhead low. Which deployment approach will meet these requirements with the LEAST operational overhead?
Deploy a fine-tuned version of the model to an Amazon SageMaker AI real-time endpoint and update the application to invoke the SageMaker endpoint for all requests.
Explanation
Incorrect. This introduces additional operational overhead to provision and manage an endpoint and changes the deployment approach away from using the same managed Bedrock model. It also requires the team to manage endpoint scaling and lifecycle rather than simply increasing Bedrock capacity.
Your answer is correct
Purchase Amazon Bedrock provisioned throughput for the model and invoke the model by using the provisioned model ARN as the modelId during inference.
Explanation
Correct. Provisioned throughput is designed to provide consistent, higher throughput for a specific Bedrock model with minimal operational overhead. The application continues to use the Bedrock API, but targets the provisioned model ARN to obtain the reserved capacity and reduce throttling during predictable peaks.
Move summarization to Amazon Bedrock batch inference by uploading prompt files to Amazon S3 and retrieving results from S3 after the batch job completes.
Explanation
Incorrect. Batch inference is intended for asynchronous processing and returning results later. It does not meet the near real-time, synchronous response requirement for an interactive assistant.
Keep using on-demand model invocation from AWS Lambda and implement exponential backoff and jitter in the AWS SDK client for Bedrock calls.
Explanation
Incorrect. Retries can reduce transient failures but do not increase the available throughput for the model. During sustained peak demand, the application can still be throttled because on-demand capacity remains limited by account quotas and service capacity.
Overall explanation
Provisioned throughput in Amazon Bedrock is the most direct way to increase and stabilize model throughput for predictable demand while keeping operations simple. It preserves the existing Bedrock integration pattern and avoids building and managing separate hosting infrastructure. Retry strategies help handle transient errors but cannot guarantee additional capacity, and batch inference is not suitable for synchronous interactive use cases. Hosting on SageMaker AI can be appropriate for hybrid or specialized deployments, but it typically introduces more operational responsibility than reserving Bedrock throughput for a managed model. Further reading (AWS): - Increase model invocation capacity with Provisioned Throughput in Amazon Bedrock (AWS Documentation) - Use a Provisioned Throughput with an Amazon Bedrock resource (AWS Documentation) - invoke-model — AWS CLI 2.31.32 Command Reference (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 21
Correct
An online retail platform is building a customer-support assistant by using a Strands agent with an Amazon Bedrock text model. The agent can call a tool that looks up order status from an internal system by invoking an AWS Lambda function. In production, the agent intermittently fails because it calls the tool with missing or malformed parameters (for example, an empty orderId), which causes Lambda errors and breaks the conversation. The team wants to make the tool integration more reliable without changing the underlying FM or adding significant operational complexity. Which solution will meet these requirements with the LEAST operational overhead?
Switch the agent to a larger FM and add few-shot examples to the agent prompt demonstrating correct tool calls for multiple orderId formats.
Explanation
Incorrect. Better prompting and a larger model can reduce errors, but they do not guarantee reliable tool operation. This approach also increases cost and still lacks deterministic validation and structured error handling at the tool boundary.
Your answer is correct
Define the tool with the Strands API by using a standardized function signature (explicit parameter names and types). Add strict parameter validation and exception handling in the Lambda handler that returns structured error responses the agent can act on (for example, “missing orderId”), so the agent can request missing information and retry safely.
Explanation
Correct. Standardizing the tool interface (clear name, description, and typed parameters) helps the agent form correct tool calls. Adding input validation and defensive error handling inside Lambda prevents malformed requests from becoming unhandled failures and allows the application to return deterministic, machine-readable error messages. This directly improves tool reliability with minimal additional infrastructure.
Place the Lambda invocation behind an AWS Step Functions state machine with retries and a circuit breaker pattern. Configure the state machine to keep retrying until the tool succeeds.
Explanation
Incorrect. Retries and circuit breakers help with transient failures (timeouts, throttling, downstream outages), but they do not fix requests that are invalid due to missing or malformed parameters. This adds orchestration overhead while still failing repeatedly for bad inputs.
Add Amazon Bedrock Guardrails to the agent to block user prompts that mention order IDs, and rely on the FM to infer missing tool parameters from conversation history when it invokes Lambda.
Explanation
Incorrect. Guardrails are primarily for content safety controls (filtering inputs/outputs, PII masking, grounding checks) and do not provide deterministic tool parameter validation. Blocking prompts about order IDs also conflicts with the business requirement to look up order status and does not address malformed tool invocations.
Overall explanation
The most reliable way to harden an agent tool is to treat the tool boundary like an API contract: use a standardized function definition so the agent knows exactly what parameters to supply, and enforce correctness with parameter validation and robust error handling in the Lambda function. Structured, predictable error responses let the agent recover (for example, by asking the user for a missing orderId) instead of failing the entire workflow. Other approaches either focus on content moderation rather than tool correctness, add orchestration that cannot repair invalid inputs, or depend on probabilistic improvements from prompting/model size instead of deterministic validation. Further reading (AWS): - Define OpenAPI schemas for your agent's action groups in Amazon Bedrock (AWS Documentation) - Configure Lambda functions to send information that an Amazon Bedrock agent elicits from the user (AWS Documentation) - Strands Agents SDK: A technical deep dive into agent architectures and observability (AWS Blog)
Domain
Content Domain 2: Implementation and Integration
Question 22
Correct
A regional insurance provider wants to improve its claims handling workflow. Claims adjusters upload scanned PDFs and images (for example, incident photos and handwritten forms) to an Amazon S3 bucket from an internal web portal. The provider needs to extract a consistent set of fields (such as policy number, claimant name, and claim amount) into structured JSON, then update the corresponding record in an external CRM system by using an API call. The solution must be serverless and require the LEAST operational overhead as document formats evolve. Which solution will meet these requirements?
Create an Amazon Q Business application with data connectors for the S3 bucket and the CRM system. Instruct employees to ask Q Business to extract the claim fields from uploaded documents and then manually paste the results into the CRM system.
Explanation
Incorrect. Amazon Q Business is primarily an employee assistant for answering questions and summarizing information from connected data sources. This option does not implement an automated document-processing workflow or a programmatic CRM update path, and it relies on manual steps that do not meet the automation requirement.
Configure an S3 event notification to invoke an AWS Lambda function that calls the Amazon Bedrock InvokeModel API with a prompt instructing the FM to extract the required fields and output JSON. Have the same Lambda function call the CRM API to update the claim record.
Explanation
Incorrect. This approach relies on prompt-based extraction that is less controlled for consistent, production-grade field extraction and typically requires additional custom preprocessing for scanned PDFs/images. It also lacks blueprint-driven extraction, normalization, and document-processing features that reduce operational overhead as formats change.
Use Amazon Textract to perform OCR on the uploaded documents and store extracted text in Amazon S3. Run an AWS Glue ETL job to normalize the text and then use Amazon Comprehend custom entity recognition to extract the required fields. Use AWS Step Functions to orchestrate the pipeline and a Lambda function to update the CRM record.
Explanation
Incorrect. Although this can work, it introduces substantially more operational overhead: building and maintaining multi-step OCR and ETL logic, plus creating and managing a custom entity recognizer. The requirement emphasizes minimizing operational overhead as document formats change, which is better addressed by a managed document extraction approach.
Your answer is correct
Configure an S3 event notification to start an AWS Step Functions workflow. In the workflow, invoke Amazon Bedrock Data Automation asynchronously by using a project that contains blueprints for the required fields. After the job completes, have a Lambda function read the JSON output and call the CRM API to update the claim record.
Explanation
Correct. Amazon Bedrock Data Automation is designed to extract structured data from documents and images, and blueprints provide a managed way to define and evolve required fields (including normalization) with minimal custom parsing. Step Functions provides durable orchestration for an asynchronous document-processing workflow, and Lambda can implement the CRM update logic without provisioning servers.
Overall explanation
The lowest-overhead serverless design is to orchestrate an asynchronous document-processing workflow that uses a managed extraction service purpose-built for multimodal inputs and structured outputs. Amazon Bedrock Data Automation can extract structured JSON from PDFs and images and can be configured with blueprints to define and evolve the required fields. AWS Step Functions provides reliable workflow orchestration (including waiting for asynchronous completion), and AWS Lambda can apply the extracted results by calling the CRM API. Alternatives either require significant custom OCR/ETL and model management or rely on manual, non-automated processes. Further reading (AWS): - Using the Bedrock Data Automation API - Amazon Bedrock (AWS Documentation) - Custom output and blueprints - Amazon Bedrock (AWS Documentation) - Starting a Step Functions workflow in response to events - AWS Step Functions (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 23
Incorrect
An HR analytics team is building an internal assistant that drafts employee performance feedback by using Amazon Bedrock. The team stores standardized prompts in Amazon Bedrock Prompt Management. During a pilot, leadership raised concerns that the generated feedback might contain subtly different tone and recommendations for employees who are described with different demographic attributes in otherwise equivalent scenarios. The team wants to automatically evaluate and compare prompt variants for fairness, and track fairness results over time with minimal custom tooling. Which solution will meet these requirements with the LEAST operational overhead?
Route a statistically significant sample of generated feedback to Amazon Augmented AI (A2I) for human review, and store reviewers’ ratings in Amazon S3 to measure bias trends.
Explanation
Incorrect. Human review can assess bias, but it introduces substantial operational overhead (workforce management, review latency, and ongoing cost). This does not meet the goal of automated fairness evaluation with minimal custom tooling compared with using Bedrock’s native prompt experimentation and evaluation capabilities.
Your answer is incorrect
Use Amazon SageMaker Clarify to calculate pre-training bias metrics, and automatically fail the deployment if the bias metrics exceed thresholds.
Explanation
Incorrect. SageMaker Clarify is designed for bias detection and explainability in ML workflows, but it does not directly provide an end-to-end, low-overhead mechanism to A/B test Bedrock prompt variants and evaluate generated text outputs using an LLM-as-a-judge within the Bedrock prompt lifecycle.
Enable Amazon Bedrock Guardrails with strict topic and profanity filters, and configure a blocked-message response to prevent biased content from being returned to users.
Explanation
Incorrect. Guardrails provide input/output safety controls (filtering and policy enforcement), but they do not perform fairness evaluation or comparative A/B testing across prompt variants. Filtering can reduce harmful content, but it does not measure whether outputs are unbiased across demographic scenarios.
Correct answer
Create two prompt variants in Amazon Bedrock Prompt Management and orchestrate A/B execution with Amazon Bedrock Prompt Flows. Use Amazon Bedrock Model Evaluations with an LLM-as-a-judge on a balanced prompt dataset to score potential bias, and publish the resulting fairness metrics to Amazon CloudWatch dashboards for ongoing tracking.
Explanation
Correct. Prompt Management variants and Prompt Flows support systematic A/B testing without building custom orchestration. Bedrock Model Evaluations can use an LLM-as-a-judge to automatically score outputs across a representative dataset for fairness-related criteria. Publishing the evaluation results as metrics to CloudWatch enables trend monitoring and governance reporting with minimal additional infrastructure.
Overall explanation
To apply fairness evaluations for foundation model outputs, the solution needs a repeatable way to compare alternatives and quantify bias-related behavior across a representative dataset. Using prompt variants and prompt orchestration enables controlled A/B testing, while automated model evaluation with an LLM-as-a-judge provides scalable scoring of outputs for fairness criteria without building a bespoke evaluation pipeline. Publishing those scores to CloudWatch makes fairness observable over time and supports governance reporting. Guardrails focus on safety filtering rather than fairness measurement, and human-in-the-loop review adds significant operational burden. SageMaker Clarify is valuable for bias assessment in ML workflows but is not the most direct, low-overhead approach for evaluating prompt-driven Bedrock text generation behavior. Further reading (AWS): - Construct and store reusable prompts with Prompt management in Amazon Bedrock - Amazon Bedrock (AWS Documentation) - Evaluate model performance using another LLM as a judge - Amazon Bedrock (AWS Documentation) - Using Amazon CloudWatch dashboards - Amazon CloudWatch (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 24
Correct
A retail operations team is building an internal troubleshooting assistant by using Amazon Bedrock and an Amazon OpenSearch Service vector index. The assistant retrieves chunks from repair manuals and incident reports. Engineers often search by exact error codes (for example, "ERR-1042") and also by natural language descriptions of symptoms. With the current semantic (vector-only) retrieval approach, searches sometimes miss the correct chunk that contains the exact code, and the returned top results are not consistently the most relevant. Which solution will improve retrieval relevance and accuracy with the LEAST operational overhead?
Remove the retrieval layer and rely on a larger foundation model with improved prompt engineering and few-shot examples so the model can answer from its pretrained knowledge.
Explanation
Incorrect. Removing retrieval reduces grounding in the team’s internal documents and increases the likelihood of incomplete or hallucinated answers, especially for organization-specific error codes and procedures. This does not improve the accuracy of retrieved context because no retrieval is performed.
Your answer is correct
Use an OpenSearch hybrid search approach that combines keyword matching on indexed fields (for example, errorCode and productId) with vector similarity search for semantic relevance. Then use an Amazon Bedrock reranker model to reorder the top retrieved chunks before sending the context to the FM.
Explanation
Correct. Hybrid search helps ensure exact-term matches (such as error codes) are retrieved while still benefiting from semantic similarity for natural-language queries. A Bedrock reranker can then improve the ordering of the retrieved chunks without requiring the team to build and maintain a custom ranking model, improving relevance with minimal added operational burden.
Increase the embedding dimensionality and switch the knowledge base to semantic chunking so that the vector-only search returns more granular and precise matches without using any keyword indexing.
Explanation
Incorrect. Chunking strategy and embedding dimensionality can affect retrieval quality, but vector-only search can still fail to prioritize or return exact matches for error codes and other precise tokens. This does not address the core requirement of combining exact-term retrieval with semantic retrieval.
Store embeddings in Amazon DynamoDB along with the source text and build a custom Lambda-based k-nearest neighbor search function that scans items and ranks them by cosine similarity.
Explanation
Incorrect. DynamoDB is not a vector search engine for k-nearest neighbor queries; implementing similarity search by scanning and scoring items in Lambda would be inefficient at scale and adds significant custom logic and operational overhead compared to using managed vector/hybrid search capabilities.
Overall explanation
The most effective low-overhead way to improve retrieval for both exact identifiers (like error codes) and descriptive queries is to combine keyword retrieval with semantic vector retrieval, then rerank the resulting candidates for best relevance. Hybrid search improves recall and precision when exact tokens matter, while a managed reranker improves the ordering of the top results without requiring the team to design, train, and operate a bespoke ranking system or scoring pipeline. Further reading (AWS): - Configure Neural Search and Hybrid Search on OpenSearch Serverless - Amazon OpenSearch Service (AWS Documentation) - Vector search - Amazon OpenSearch Service (AWS Documentation) - Improve the relevance of query responses with a reranker model in Amazon Bedrock - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 25
Correct
A financial services engineering team has created a domain-specific LLM by applying a parameter-efficient fine-tuning technique (LoRA) in Amazon SageMaker AI. The team wants to deploy the customized model to production and frequently release improved LoRA versions. The team must maintain a clear version history and approval trail for each release, and must be able to roll back quickly if a newly deployed model increases error rates or produces unacceptable outputs. Which combination of actions will meet these requirements with the LEAST operational overhead? (Select TWO.)
Use Amazon Bedrock Prompt Management to version prompts and prompt variants for each release, and treat prompt version changes as the model deployment lifecycle.
Explanation
Incorrect. Prompt Management governs prompts, not model artifacts and model deployments. It does not provide model versioning, model approvals, or rollback mechanisms for a SageMaker-hosted customized model.
Deploy each model version to a separate AWS Lambda function version and use AWS CodeDeploy to perform canary deployments of Lambda aliases to roll back if errors increase.
Explanation
Incorrect. Lambda alias traffic shifting applies to Lambda functions, not to SageMaker real-time inference endpoints hosting large model artifacts. This approach does not directly address model deployment and lifecycle management for SageMaker-hosted models.
Store each LoRA checkpoint in Amazon S3 with bucket versioning enabled. Update the application configuration to point to the new S3 object version to switch models.
Explanation
Incorrect. S3 versioning helps track objects, but it does not provide model governance (approval status), deployment orchestration, or safe traffic shifting/rollback for inference endpoints. The team would still need to build custom controls and rollback logic.
Your selection is correct
Register each LoRA-adapted model version in Amazon SageMaker Model Registry and require an approval step before promoting a version for production deployment.
Explanation
Correct. SageMaker Model Registry provides centralized versioning and metadata for model artifacts, and supports an approval workflow to control which versions can be deployed. This creates an auditable promotion trail and simplifies lifecycle management across multiple model iterations.
Your selection is correct
Deploy the model behind a SageMaker real-time endpoint that uses deployment guardrails (canary or linear) with automatic rollback based on Amazon CloudWatch alarms.
Explanation
Correct. SageMaker deployment guardrails provide controlled traffic shifting to a new model version and can automatically roll back when CloudWatch alarms indicate failures or regressions. This minimizes downtime risk and reduces the need for custom rollback automation.
Overall explanation
A low-overhead, production-grade lifecycle for customized models requires both governance/versioning and safe release mechanisms. A model registry provides a controlled system of record for model versions (including approvals and deployment-ready artifacts). Deployment guardrails on the inference endpoint then handle progressive rollout and rollback when monitoring signals indicate a regression, avoiding custom traffic-splitting and rollback tooling. Alternatives that rely on object storage versioning, prompt versioning, or Lambda deployment features do not provide end-to-end model lifecycle management for SageMaker-hosted customized models. Further reading (AWS): - Model Registry Models, Model Versions, and Model Groups - Amazon SageMaker AI (AWS Documentation) - Update the Approval Status of a Model - Amazon SageMaker AI (AWS Documentation) - Blue/Green Deployments - Amazon SageMaker AI (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 26
Correct
A healthcare analytics team is building an internal GenAI assistant that uses Amazon Bedrock to summarize clinical case notes. The assistant retrieves supporting facts from a data lake in Amazon S3 by running Amazon Athena queries. The team must ensure that service-to-service traffic does not traverse the public internet and that the Bedrock invocation can access only approved columns (no PHI) from the data lake. The team also wants to monitor data access with minimal operational overhead. Which solution meets these requirements MOST effectively?
Place the AWS Lambda function in private subnets and use a NAT gateway for outbound access to Amazon Bedrock and Amazon Athena public endpoints. Restrict access to S3 objects by using S3 bucket policies and encrypt the bucket with SSE-KMS.
Explanation
Incorrect. A NAT gateway still routes traffic to public service endpoints over the internet, which does not meet the requirement to avoid public internet paths. SSE-KMS and bucket policies help protect data at rest and control S3 access, but they do not provide the required private connectivity to Bedrock and Athena, nor do they provide column-level controls for query results.
Use client-side encryption for all objects in Amazon S3 so that only the Lambda function can decrypt the data. Invoke Amazon Bedrock directly from Lambda over the public endpoint and rely on AWS KMS key policies to restrict which principals can use the keys.
Explanation
Incorrect. Client-side encryption and KMS key policies can protect data confidentiality, but they do not ensure that traffic avoids the public internet when calling Bedrock or Athena. This approach also does not provide Lake Formation’s table/column governance for Athena query results and typically increases operational complexity due to key handling and client-side encryption workflows.
Store all clinical notes in Amazon OpenSearch Service with fine-grained access control. Configure the application to retrieve documents from OpenSearch and then invoke Amazon Bedrock over the public endpoint. Use OpenSearch Dashboards audit logs as the primary monitoring mechanism.
Explanation
Incorrect. This design still invokes Bedrock over a public endpoint, which does not satisfy the private connectivity requirement. It also shifts the data lake access pattern away from Athena/Lake Formation governance and increases operational overhead by introducing and operating a separate search platform for the primary control objective.
Your answer is correct
Create interface VPC endpoints (AWS PrivateLink) for Amazon Bedrock and Amazon Athena, and create a gateway VPC endpoint for Amazon S3. Run the Lambda function in private subnets with no internet gateway. Use AWS Lake Formation to apply column-level permissions (data filters) to the Athena tables. Grant the Lambda execution role least-privilege permissions to query only the governed tables. Send access and application logs to Amazon CloudWatch Logs for monitoring.
Explanation
Correct. VPC endpoints keep service-to-service traffic on the AWS network without public internet exposure. Lake Formation can enforce granular (including column-level) access controls for Athena tables so the application cannot retrieve PHI columns. A least-privilege IAM role constrains what the Lambda function can query and access. CloudWatch provides centralized monitoring and logging for the application’s data access patterns with minimal added infrastructure.
Overall explanation
A protected AI environment for FM deployments typically combines private network connectivity, least-privilege access controls, and governed data access. Using VPC endpoints (PrivateLink) allows the application components in private subnets to call managed services without traversing the public internet. Lake Formation can enforce fine-grained permissions for Athena-accessed data in S3, including restricting sensitive columns such as PHI. IAM policies on the Lambda execution role restrict what queries and data resources the application can access. Finally, CloudWatch Logs provides centralized monitoring of the application’s access activity with low operational overhead. Further reading (AWS): - Use interface VPC endpoints (AWS PrivateLink) to create a private connection between your VPC and Amazon Bedrock (AWS Documentation) - Connect to Amazon Athena using an interface VPC endpoint (AWS Documentation) - Managing data filters - AWS Lake Formation (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 27
Incorrect
A media analytics team is building a Retrieval Augmented Generation (RAG) assistant by using Amazon Bedrock. Millions of internal articles are stored in Amazon S3 and are tagged by topic (for example, sports, finance, and entertainment). The team wants to segment the corpus by topic to improve retrieval precision. The team also wants the search tier to accept natural language queries and generate embeddings automatically so the application does not need to compute embeddings before querying the vector store. Which solution meets these requirements with the LEAST application-side implementation effort?
Correct answer
Store the documents in Amazon S3. Create an Amazon OpenSearch Service domain with separate indices per topic. Configure the OpenSearch Neural plugin to call an Amazon Bedrock embedding model in an ingest pipeline and use neural queries so OpenSearch generates query embeddings automatically.
Explanation
Correct. This design uses OpenSearch as the vector database and applies topic-based segmentation by isolating content into per-topic indices. The Neural plugin allows OpenSearch to generate embeddings during ingestion and at query time by invoking an embedding model in Amazon Bedrock, which removes the need for the application to compute embeddings before search.
Your answer is incorrect
Create an Amazon Bedrock Knowledge Base with the S3 bucket as the data source and an Amazon OpenSearch Serverless collection as the managed vector store. Use hierarchical chunking and let the Knowledge Base handle ingestion and retrieval for the application.
Explanation
Incorrect. Although a Knowledge Base can simplify RAG ingestion and retrieval, it does not directly satisfy the requirement for the search tier itself (OpenSearch) to accept natural language queries and generate embeddings automatically for ad hoc search use cases. The requirement is specifically to offload query-embedding generation into the search layer.
Store documents in Amazon S3 and store embeddings in Amazon RDS for PostgreSQL as a JSON column. Use SQL to calculate similarity scores in queries and filter by topic tags to return the top matches.
Explanation
Incorrect. Storing embeddings as JSON and implementing similarity calculations manually increases application and database complexity and is not an optimized vector database architecture. This approach also misses purpose-built vector indexing/search capabilities needed for efficient semantic retrieval at scale.
Store document text and topic tags in Amazon DynamoDB. Generate embeddings in the application with an Amazon Bedrock embedding model and store embeddings as DynamoDB attributes. Use DynamoDB Scan operations and compute cosine similarity in the application to find the closest matches.
Explanation
Incorrect. DynamoDB is not designed for efficient vector similarity search in this pattern. Scanning and computing similarity in the application does not provide efficient semantic retrieval at scale and significantly increases application-side complexity and latency.
Overall explanation
The best approach is to use Amazon OpenSearch Service as the vector store with topic-based segmentation (separate indices per topic) and integrate embedding generation directly into OpenSearch through the Neural plugin. This lets OpenSearch create embeddings for documents during ingestion and create embeddings for user queries at search time by invoking an Amazon Bedrock embedding model, enabling efficient semantic retrieval without requiring the application to generate embeddings before each query. Other approaches either don’t offload embedding generation into the search tier or require inefficient retrieval patterns and substantial custom implementation. Further reading (AWS): - Semantic search in Amazon OpenSearch Service - Amazon OpenSearch Service (AWS Documentation) - Amazon Bedrock templates - Amazon OpenSearch Service (AWS Documentation) - Power neural search with AI/ML connectors in Amazon OpenSearch Service (AWS Blog)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 28
Correct
A healthcare provider operates hospitals in multiple European countries. Patient records must remain inside each hospital’s on-premises data center to satisfy data residency requirements. The provider wants to use an Amazon Bedrock FM to generate short discharge summaries, but the solution must ensure that raw patient records do not traverse the public internet and that only approved, de-identified text is sent to the FM. Which solution meets these requirements with the LEAST operational overhead?
Expose an Amazon API Gateway Regional endpoint that invokes Amazon Bedrock directly. Configure TLS for encryption in transit and rely on IAM authentication so hospitals can send patient records securely to the FM.
Explanation
Incorrect. Encryption in transit and IAM authentication do not satisfy the requirement that raw patient records remain on premises, and this design does not ensure traffic avoids the public internet end-to-end. It also sends sensitive records directly to the FM without enforcing local preprocessing and de-identification in the hospital environment.
Replicate the hospitals’ patient records from on premises into an Amazon S3 bucket in an AWS Region. Invoke Amazon Bedrock from an AWS Lambda function that reads the records from Amazon S3 and generates discharge summaries.
Explanation
Incorrect. Replicating raw patient records into Amazon S3 violates the requirement that patient records remain in each hospital’s on-premises data center. This approach also increases compliance risk because the full sensitive dataset is moved into the cloud for processing.
Deploy the application on AWS Wavelength Zones near each hospital to reduce latency. Store patient records in an Amazon S3 bucket and call Amazon Bedrock from AWS App Runner running in the Wavelength Zone.
Explanation
Incorrect. AWS Wavelength is designed for edge deployments and low-latency access patterns, but it does not address the requirement that patient records remain in each hospital’s on-premises data center. Storing patient records in Amazon S3 moves the data into AWS and can violate data residency constraints.
Your answer is correct
Deploy the application’s ingestion and preprocessing components on AWS Outposts in each hospital. Store patient records locally on Outposts and de-identify the text on Outposts. Use an Amazon VPC interface endpoint (AWS PrivateLink) to invoke the Amazon Bedrock Runtime API with only the de-identified text.
Explanation
Correct. AWS Outposts enables running AWS compute and storage on premises for local data integration and residency. De-identifying on Outposts ensures only approved data is sent to the FM. Using a VPC interface endpoint (PrivateLink) provides private connectivity to the Bedrock Runtime API so requests do not traverse the public internet, meeting the secure routing requirement with minimal operational overhead because the provider does not manage model infrastructure.
Overall explanation
The key design objective is a cross-environment architecture where sensitive data stays local to satisfy residency rules, while still allowing controlled FM access. Running ingestion and preprocessing on AWS Outposts keeps patient records in the hospital data center and enables local enforcement of de-identification before any model invocation. Private connectivity to Amazon Bedrock through a VPC interface endpoint (AWS PrivateLink) helps ensure secure routing without traversing the public internet. Approaches that move raw records to Amazon S3 or send records directly to Bedrock fail the residency constraint, and edge deployments alone do not solve on-premises data integration and control requirements. Further reading (AWS): - Use interface VPC endpoints (AWS PrivateLink) to create a private connection between your VPC and Amazon Bedrock - Amazon Bedrock (AWS Documentation) - What is AWS Outposts? - AWS Outposts (AWS Documentation) - Ensure Workload Resiliency and Comply with Data Residency Requirements with AWS Outposts | AWS Architecture Blog (AWS Blog)
Domain
Content Domain 2: Implementation and Integration
Question 29
Incorrect
A global HR SaaS provider is building a semantic search feature for internal policy documents by using a RAG architecture. The team will embed millions of document chunks and store the vectors in a vector database for similarity search. The solution must keep vector storage costs low while maintaining search relevance for the organization’s terminology, and the embedding generation process must efficiently handle nightly ingestion of large document batches. Which combination of actions will meet these requirements MOST cost-effectively? (Select TWO.)
Configure the embedding model to use the maximum available vector dimension to improve accuracy. Invoke the embedding model from the application for each chunk individually to avoid batching complexity.
Explanation
Incorrect. Always maximizing dimensionality can significantly increase storage and indexing costs and is not guaranteed to improve retrieval enough to justify the cost. Generating embeddings one chunk at a time increases invocation overhead and slows nightly ingestion compared to batching.
Correct selection
Run a proof of concept that generates embeddings for a representative set of documents and queries by using multiple Amazon Bedrock embedding models (for example, Amazon Titan embeddings and an alternative embedding model). Compare retrieval quality metrics (such as top-k relevance) and select the model that best fits the domain and language needs.
Explanation
Correct. Embedding models can differ in how well they represent domain-specific concepts and multilingual content. Evaluating retrieval performance on representative queries is an effective way to select an embedding model that best aligns with the business use case.
Your selection is correct
Use an Amazon Titan embedding model and configure a smaller embedding vector dimension after validating that retrieval relevance remains acceptable for the policy-document domain. Use AWS Lambda to batch-generate embeddings for new chunks before writing them to the vector store.
Explanation
Correct. Smaller embedding dimensionality reduces storage and indexing costs for large corpora, but should be validated to ensure it still captures the semantic detail needed for the domain. Batch generation with Lambda reduces per-item invocation overhead and improves ingestion efficiency for nightly loads.
Your selection is incorrect
Use Amazon Comprehend to classify each document into topics and store only the topic labels. Use keyword search on the labels instead of generating embeddings to reduce cost.
Explanation
Incorrect. Topic classification and keyword search do not provide the semantic similarity retrieval required for embedding-based vector search, and this approach would not meet the semantic search requirement for RAG-based retrieval.
Store embeddings and chunk metadata in Amazon DynamoDB and use PartiQL queries to perform semantic similarity searches directly in DynamoDB.
Explanation
Incorrect. DynamoDB can store embedding vectors as attributes but does not provide native vector similarity search for k-nearest-neighbor retrieval, making it unsuitable as the primary semantic vector search engine for a RAG retriever.
Overall explanation
To keep costs low at scale, the embedding strategy should reduce the size and number of vectors stored while preserving retrieval quality. Configuring an embedding model with an appropriately smaller vector dimension can materially reduce vector storage and indexing costs, but it must be validated against real queries to avoid harming relevance. Because different embedding models can behave differently across domains and languages, testing multiple Bedrock embedding model options on representative data is a reliable way to choose the best fit. Finally, batching embedding generation with Lambda improves ingestion throughput and reduces per-item overhead during large nightly loads. Further reading (AWS): - Amazon Titan Text Embeddings models - Amazon Bedrock (AWS Documentation) - Get started with Amazon Titan Text Embeddings V2: A new state-of-the-art embeddings model on Amazon Bedrock | Artificial Intelligence (AWS Blog) - Using Lambda with Amazon SQS - AWS Lambda (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 30
Incorrect
A compliance engineering team is building an internal summarization service that uses an Amazon Bedrock text FM to produce 1-paragraph summaries of long policy documents. The team needs an evaluation approach that can be rerun for every prompt template change to detect regressions. The approach must assess the quality of summaries across multiple dimensions, including relevance to the source content, factual accuracy, consistency across runs, and fluency, while keeping the evaluation process largely automated. Which approach will meet these requirements with the LEAST manual effort?
Correct answer
Store a prompt dataset in Amazon S3 that includes source documents and reference summaries. Run Amazon Bedrock Model Evaluations using an LLM-as-a-judge configuration to score each generated summary on relevance, correctness (factual accuracy), consistency, and fluency. Compare scores across prompt template versions before deployment.
Explanation
Correct. An automated evaluation workflow that uses a prompt dataset and an evaluator (judge) model can score outputs against multiple quality dimensions beyond traditional ML metrics. This approach supports repeatable regression testing for each prompt change and minimizes manual effort while still providing structured, multi-metric quality signals.
Create a benchmark dataset of source documents and use ROUGE and BLEU scores to compare model-generated summaries to reference summaries. Use the combined score as the single quality signal for go/no-go decisions.
Explanation
Incorrect. ROUGE and BLEU measure overlap and n-gram precision/recall-style similarity and can be helpful for certain summarization/translation comparisons, but they do not reliably capture factual accuracy (hallucination), consistency across runs, or fluency. Relying on them alone does not provide the comprehensive, multi-dimensional assessment requested.
Track Amazon CloudWatch metrics for the summarization endpoint, including InputTokenCount, OutputTokenCount, and model latency. Treat lower token usage and lower latency as indicators of higher summary quality.
Explanation
Incorrect. Token counts and latency are useful operational metrics, but they do not evaluate output quality dimensions such as relevance, factual accuracy, consistency, or fluency. Optimizing only for speed or token usage can reduce quality and does not provide a comprehensive assessment framework.
Your answer is incorrect
Enable Amazon SageMaker Model Monitor on the summarization workload to detect data drift and feature attribution drift. Block deployments when drift exceeds a predefined threshold.
Explanation
Incorrect. SageMaker Model Monitor is designed to monitor deployed model/data characteristics (for example, drift) rather than to directly evaluate the semantic quality of LLM-generated text for relevance, factual accuracy, consistency, and fluency. Drift signals alone cannot serve as a comprehensive text quality evaluation framework for FM outputs.
Overall explanation
A comprehensive FM output assessment framework needs explicit quality-oriented metrics (such as relevance, factual accuracy/correctness, consistency, and fluency) and must be repeatable for regression testing when prompts or configurations change. An automated evaluation workflow that uses a curated prompt dataset with reference outputs, and applies an LLM-as-a-judge evaluator to score multiple dimensions, provides actionable quality scores at scale with minimal manual effort. Operational metrics like latency and token counts support cost/performance optimization but do not measure output quality, and traditional n-gram overlap metrics alone are not sufficient to capture hallucinations, consistency, and fluency for generative outputs. Further reading (AWS): - Evaluate model performance using another LLM as a judge - Amazon Bedrock (AWS Documentation) - Create a model evaluation job that uses an LLM as a judge - Amazon Bedrock (AWS Documentation) - Use metrics to understand model performance - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 5: Testing, Validation, and Troubleshooting
Question 31
Incorrect
A platform engineering team is building an internal “Ops Copilot” that autonomously troubleshoots application incidents. The team uses AWS Agent Squad to route each user request to one of several specialized Strands agents (for example, an incident triage agent and a runbook agent). Users expect the assistant to remember conversation context within a session and also retain longer-term preferences (for example, preferred service names and escalation rules) across multiple sessions. The team wants a serverless approach that minimizes the amount of custom state-management code they must build and operate. Which solution meets these requirements with the LEAST operational overhead?
Deploy the Strands agents on Amazon ECS with AWS Fargate and store state locally in the container file system so agents can reuse memory between requests. Use AWS Agent Squad only for routing decisions.
Explanation
Incorrect. Container-local storage is not a durable or shared state store for a multi-agent, horizontally scaled system; tasks can be replaced at any time. This approach also introduces additional container operations compared to a managed, serverless agent runtime with built-in memory services.
Use Amazon Bedrock Knowledge Bases as the system of record for both short-term and long-term memory by writing each conversation turn into the knowledge base and retrieving relevant history for each request.
Explanation
Incorrect. Knowledge bases are designed for retrieval augmentation over curated documents and indexed content, not for maintaining structured conversational state and preferences with session semantics. Using a knowledge base for chat memory typically adds latency and requires additional ingestion and retrieval design that does not directly provide session and memory-record abstractions.
Your answer is incorrect
Store per-session conversation history in Amazon DynamoDB and store long-term preferences as separate DynamoDB items. Pass the session identifier between the routed agents so each agent can fetch and update the state as needed.
Explanation
Incorrect. DynamoDB can store chat history and preferences, but the team must design session/event schemas, implement read/write patterns, manage summarization or pruning, and coordinate consistency across multiple agents. This increases custom state-management logic and operational responsibility compared to a managed agent memory capability.
Correct answer
Deploy the routed Strands agents by using Amazon Bedrock AgentCore Agent Runtime and use AgentCore Memory to manage session context and long-term memory records for user preferences that the agents can read and update across sessions.
Explanation
Correct. AgentCore provides serverless endpoints for agents and includes managed memory capabilities. AgentCore Memory supports session-oriented context (events within a session) and longer-term memory records (such as preferences and summaries), reducing the need to build and operate custom state stores and coordination logic across multiple specialized agents.
Overall explanation
The core challenge is maintaining both short-term conversational state (what was said earlier in the current interaction) and durable long-term memory (user preferences and extracted insights) while coordinating that state across multiple specialized agents selected by a router. A managed agent runtime with built-in memory reduces custom engineering and operational burden because it provides purpose-built abstractions for sessions and long-term memory records, and it avoids building and maintaining bespoke storage schemas, pruning logic, and cross-agent synchronization. Alternatives such as using a general-purpose database, container-local storage, or a retrieval knowledge base can work in limited scenarios, but they either do not provide session semantics and durable multi-agent state management, or they significantly increase the amount of custom code and operations required. Further reading (AWS): - Use isolated sessions for agents - Amazon Bedrock AgentCore (AWS Documentation) - Memory types - Amazon Bedrock AgentCore (AWS Documentation) - Compare long-term memory with Retrieval-Augmented Generation - Amazon Bedrock AgentCore (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 32
Correct
A fintech company is building a customer self-service chatbot that runs behind Amazon API Gateway and AWS Lambda. The chatbot uses Amazon Bedrock to answer questions such as “What were my last 5 card transactions?” by querying an Amazon Aurora PostgreSQL database. The company is concerned that abusive user messages could cause the chatbot to respond with profane or otherwise harmful language. The company also wants to avoid any fabricated transaction amounts and ensure that data-backed answers are deterministic and auditable. Which solution will meet these requirements with the LEAST operational overhead?
Deploy a custom toxicity classification model in Amazon SageMaker AI and use SageMaker Model Monitor and SageMaker Clarify to detect unsafe outputs and block responses when toxicity or bias metrics exceed a threshold.
Explanation
Incorrect. This introduces significant operational overhead (model deployment, monitoring jobs, and ongoing tuning) and is not the most direct approach for preventing harmful LLM outputs. Clarify focuses on bias and explainability, not primary output moderation for conversational responses, and this does not inherently make database-backed answers deterministic.
Your answer is correct
Configure Amazon Bedrock Guardrails to filter model responses (for example, profanity and topic filters) and return a blocked message when content violates policy. For transaction questions, implement a text-to-SQL flow where the model produces a SQL query that is restricted to read-only, parameterized patterns executed by a Lambda function against Aurora, and format the response only from the SQL result set.
Explanation
Correct. Bedrock Guardrails provide managed output filtering to prevent harmful language in responses. Using a text-to-SQL approach with restricted, read-only query patterns and returning answers from the executed SQL results makes transaction answers deterministic and auditable, reducing hallucinations and preventing fabricated amounts while keeping the architecture managed and relatively simple to operate.
Use AWS Lambda to call Amazon Comprehend to detect negative sentiment in the generated response and remove any sentences that appear toxic. Then return the remaining text to the user without changing how the model answers transaction questions.
Explanation
Incorrect. Sentiment detection is not the same as content safety enforcement, and deleting sentences post-generation can produce incomplete or misleading answers. This also does not ensure deterministic, auditable transaction responses because the model is still generating the content rather than returning results grounded in the database query.
Rely on prompt instructions that tell the model to be polite and to not use offensive language. Set the model temperature to 0 to reduce variability, and let the model answer transaction questions from its own reasoning.
Explanation
Incorrect. Prompt-only controls and lower temperature can reduce randomness but cannot reliably prevent harmful outputs, and the model can still fabricate transaction details because it is not deterministically grounded to the database results.
Overall explanation
A robust content safety framework for a customer-facing chatbot should combine managed output controls with deterministic data access patterns. Guardrails help enforce policy on the model’s generated responses by filtering profanities and disallowed topics and by returning a consistent blocked message when violations occur. For questions that require exact values (such as financial transactions), a text-to-SQL approach reduces the risk of hallucinated numbers by forcing the system to retrieve the answer from the database and to generate the final response only from the query results. Approaches that rely only on prompting or post-hoc sentiment checks are less reliable for preventing harmful outputs and do not ensure deterministic, auditable data-backed answers. Further reading (AWS): - Detect and filter harmful content by using Amazon Bedrock Guardrails - Amazon Bedrock (AWS Documentation) - Use the ApplyGuardrail API in your application - Amazon Bedrock (AWS Documentation) - Build an AI-powered text-to-SQL chatbot using Amazon Bedrock, Amazon MemoryDB, and Amazon RDS | AWS Database Blog (AWS Blog)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 33
Correct
A financial services innovation team wants to pilot an internal “AI search” assistant that answers questions about employee policy PDFs stored in Amazon S3. The team needs a technical proof of concept within 2 weeks, wants to avoid managing servers, and must produce initial measurements of per-question latency and token-related cost before committing to a production rollout. Which combination of actions will meet these requirements with the LEAST operational overhead? (Select TWO.)
Your selection is correct
Create an Amazon Bedrock Knowledge Base that ingests the policy PDFs from Amazon S3 with managed chunking and an embedding model. Implement a simple API backed by AWS Lambda that calls RetrieveAndGenerate to answer pilot user questions.
Explanation
Correct. Bedrock Knowledge Bases provide a managed RAG implementation that can be stood up quickly for a proof of concept, with minimal infrastructure to manage. Using Lambda to call RetrieveAndGenerate enables a lightweight pilot experience without building a custom retrieval pipeline.
Your selection is correct
Instrument the proof of concept to estimate and track token usage and latency by using the Amazon Bedrock CountTokens API and Amazon CloudWatch metrics (such as input and output token counts, invocation count, and model latency).
Explanation
Correct. For a proof of concept, the team needs early performance and cost signals. CountTokens provides a way to estimate request size without performing inference, and CloudWatch metrics provide ongoing visibility into latency and token consumption so the team can quantify per-question cost and performance characteristics.
Provision an Amazon OpenSearch Service cluster on dedicated instances. Build a custom ingestion pipeline with AWS Glue ETL jobs to chunk documents, generate embeddings, and manage indexing and updates.
Explanation
Incorrect. Although this can work, it adds significant operational overhead (cluster sizing, scaling, index management, and ETL pipeline maintenance) and is not the fastest approach for a 2-week proof of concept when a managed Bedrock Knowledge Base can provide retrieval and ingestion capabilities.
Deploy a large language model to an Amazon SageMaker AI real-time inference endpoint and front it with an Application Load Balancer. Run load tests to measure latency and then integrate it with the S3 documents directly in prompts.
Explanation
Incorrect. This requires managing endpoint capacity and deployment concerns that are unnecessary for a rapid Bedrock-based proof of concept. It also risks very large prompts (higher token costs and context window pressure) because documents are being inserted directly into prompts instead of using managed retrieval.
Fine-tune an Amazon Titan text model in Amazon Bedrock by creating labeled prompt-completion pairs from the policy documents in Amazon S3, then deploy the custom model for the pilot.
Explanation
Incorrect. Fine-tuning increases time, cost, and operational effort compared to a RAG-based pilot. For a rapid proof of concept over documents that already exist in S3, using a managed RAG workflow is faster and better aligned to validating feasibility and business value.
Overall explanation
A low-overhead proof of concept should rely on managed GenAI building blocks and lightweight measurement. Using a Bedrock Knowledge Base with documents in S3 provides a fast “chat with your documents” style RAG implementation without building and operating a custom retrieval pipeline. Pairing the pilot with token and latency measurement through CountTokens and CloudWatch produces the core feasibility signals (cost per request and responsiveness) needed to decide whether to proceed to a full production deployment. Approaches that require fine-tuning, managing search clusters and ETL pipelines, or deploying and operating custom model endpoints add time and operational burden that conflicts with the rapid proof-of-concept goal. Further reading (AWS): - Create a knowledge base by connecting to a data source in Amazon Bedrock Knowledge Bases (AWS Documentation) - RetrieveAndGenerate - Amazon Bedrock (AWS Documentation) - Monitoring the performance of Amazon Bedrock - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 34
Correct
A customer support SaaS provider is building an internal assistant that uses Amazon Bedrock to draft replies to incoming support tickets. Tickets are ingested from email and chat and often include noisy text such as email signatures, legal disclaimers, and inconsistent formatting. The assistant’s outputs are inconsistent because key details such as product names and case identifiers are not always clearly presented in the prompt. The team wants to improve response quality and consistency by enhancing the input text before invoking the FM, without building a custom model. Which solution will meet these requirements with the LEAST operational overhead?
Your answer is correct
Add an AWS Lambda preprocessing step that calls Amazon Comprehend to extract key entities (such as product names and case identifiers) and normalize or redact noisy/sensitive content. Then use an Amazon Bedrock text model to reformat the cleaned ticket into a standardized template (for example, consistent sections/fields) before invoking the primary FM to draft the reply.
Explanation
Correct. This approach improves input data quality by extracting structured signal (entities) and normalizing the text before generation. Using Lambda for preprocessing keeps the integration lightweight, Amazon Comprehend provides consistent entity extraction, and Amazon Bedrock can reformat the cleaned content into a predictable structure, improving response quality and consistency without custom model training.
Store the raw tickets in Amazon DynamoDB and include the entire ticket history in every prompt. Set the model temperature to 0 to reduce randomness and improve consistency.
Explanation
Incorrect. Lowering temperature can reduce randomness, but it does not address poor input quality. Including entire histories increases token usage and can amplify noise (signatures/disclaimers), which can reduce response quality.
Apply Amazon Bedrock Guardrails with PII filtering and topic filters, and pass the raw ticket text directly to the model to ensure safe and compliant outputs.
Explanation
Incorrect. Guardrails help filter unsafe content and can mask or remove sensitive data, but they do not reliably normalize the ticket structure or extract key entities to improve input clarity and consistency.
Create a fine-tuned Amazon Bedrock custom model by using labeled prompt-completion pairs from historical tickets so the model learns to ignore signatures and always extract product and case identifiers.
Explanation
Incorrect. Fine-tuning is model customization and introduces significant cost and lifecycle management overhead. The requirement is to improve results without building a custom model.
Overall explanation
When an FM receives inconsistent, noisy prompts, output quality and consistency often degrade because important details are buried in irrelevant text. A low-overhead way to address this is to add a preprocessing layer: use AWS Lambda to orchestrate input cleaning, use Amazon Comprehend to extract key entities and help normalize/redact problematic content, and then use an Amazon Bedrock model to rewrite the cleaned ticket into a standard structure. The primary FM then receives a clearer, more consistent prompt, which improves response quality without the additional complexity and cost of model customization. Further reading (AWS): - DetectEntities - Amazon Comprehend API Reference (AWS Documentation) - InvokeModel - Amazon Bedrock (AWS Documentation) - What is AWS Lambda? - AWS Lambda (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 35
Correct
A fintech engineering team is building an internal policy assistant that uses Retrieval Augmented Generation (RAG) with Amazon Bedrock. Policy documents are stored in Amazon S3. The team needs semantic search over the documents to provide relevant context to the foundation model (FM). The team wants a solution that requires the LEAST operational overhead to deploy and operate. Which solution will meet these requirements?
Your answer is correct
Create an Amazon Bedrock Knowledge Base that uses the S3 bucket as a data source. Select an embedding model (for example, Amazon Titan Embeddings) and use a managed vector store option backed by Amazon OpenSearch Service. Configure chunking, then use the knowledge base RetrieveAndGenerate capability to augment FM prompts.
Explanation
Correct. Amazon Bedrock Knowledge Bases provide a managed approach to deploying semantic search for RAG: the service handles ingestion from S3, chunking, embedding generation with a selected embedding model, and storage/indexing in the configured vector store. This minimizes custom code and ongoing operational work while enabling semantic retrieval for FM augmentation.
Provision an Amazon OpenSearch Service domain and create a k-NN vector index. Build a custom ingestion pipeline that uses AWS Lambda to invoke an embedding model in Amazon Bedrock, store embeddings in OpenSearch documents, and expose a custom retrieval API for the RAG application.
Explanation
Incorrect. This approach can enable semantic search, but it requires building and operating the ingestion pipeline, embedding generation logic, index mappings, and a retrieval API. That increases operational overhead compared with using a managed knowledge base.
Create an Amazon Aurora PostgreSQL cluster with the pgvector extension enabled. Store each document chunk and embedding vector in tables. Implement similarity queries in SQL and build an application layer that injects the retrieved rows into prompts sent to Amazon Bedrock.
Explanation
Incorrect. Aurora PostgreSQL with pgvector can support vector similarity search, but the team would still need to design schemas, implement chunking/embedding pipelines, tune and operate the database for retrieval workloads, and build the retrieval integration logic. This does not meet the goal of least operational overhead.
Store document chunks and embedding vectors as items in Amazon DynamoDB. Use DynamoDB Query operations and filter expressions to retrieve the nearest-neighbor vectors for each user query and pass the results to Amazon Bedrock.
Explanation
Incorrect. DynamoDB can store vectors as attributes, but it does not provide native vector similarity search (k-NN) for semantic retrieval. Query and filter expressions cannot perform vector distance calculations needed for semantic search.
Overall explanation
The key requirement is to deploy and configure semantic retrieval with minimal operational burden. A managed knowledge base in Amazon Bedrock is purpose-built for RAG: it connects to an S3 data source, chunks content, generates embeddings with a chosen embedding model, and stores/indexes those embeddings in a managed vector store for semantic search. Alternatives that directly use OpenSearch or Aurora can achieve vector search, but they require the team to build and maintain ingestion pipelines, embedding generation, index/schema design, and retrieval integration. DynamoDB is not suitable as a semantic vector search engine because it lacks native vector similarity search capabilities. Further reading (AWS): - Build a knowledge base by connecting to a data source - Amazon Bedrock (AWS Documentation) - Connect to Amazon S3 for your knowledge base - Amazon Bedrock (AWS Documentation) - Amazon Titan Text Embeddings models - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 36
Incorrect
A logistics company operates a legacy on-premises shipment tracking system that can send HTTPS webhooks but cannot use AWS SDKs. The company wants to add an Amazon Bedrock FM step that summarizes shipment exception notes and stores the summaries for downstream applications. During shift changes, exception events arrive in large bursts. The on-premises system must receive an immediate acknowledgment and must not be tightly coupled to Bedrock availability. Which solution will meet these requirements with the LEAST operational overhead?
Configure an Amazon API Gateway REST API that invokes an AWS Lambda function synchronously. In the Lambda function, call the Amazon Bedrock InvokeModel API and return the summary in the API response to the on-premises system.
Explanation
Incorrect. Synchronous invocation couples the webhook response time to Bedrock latency and availability. Bursts can cause throttling and timeouts, and the on-premises system cannot wait for the full FM inference to complete.
Your answer is incorrect
Store exception notes in an Amazon S3 bucket from the on-premises system. Configure Amazon EventBridge to detect new objects and invoke an AWS Step Functions workflow that calls Amazon Bedrock and writes results back to the on-premises database.
Explanation
Incorrect. This requires the legacy system to integrate directly with S3 uploads and adds workflow orchestration and a write-back path to on-premises systems, increasing integration complexity and operational overhead compared to a simple webhook ingestion pattern.
Correct answer
Configure an Amazon API Gateway REST API with an AWS service integration to Amazon SQS. Have the on-premises system send webhooks to the API. Configure an AWS Lambda function to poll the SQS queue, invoke the Amazon Bedrock model, and store the summaries in Amazon DynamoDB for downstream consumption.
Explanation
Correct. API Gateway provides an HTTPS integration point for the legacy system and can immediately acknowledge requests. SQS decouples ingestion from processing and buffers burst traffic. Lambda scales to process messages asynchronously and can invoke Bedrock without coupling the legacy system to inference performance or availability. DynamoDB provides a simple, scalable store for downstream applications to retrieve summaries.
Deploy a containerized service on Amazon ECS behind a Network Load Balancer to receive webhooks, call Amazon Bedrock directly from the service, and store results in Amazon OpenSearch Service.
Explanation
Incorrect. Running and scaling a container service introduces additional operational overhead (cluster/service management, scaling policies, patching, and deployment). It also still risks coupling webhook processing to inference unless additional buffering is implemented, and OpenSearch is unnecessary if the primary need is durable asynchronous processing and simple storage of summaries.
Overall explanation
The key enterprise connectivity requirement is to integrate a legacy HTTPS webhook producer while keeping the FM invocation loosely coupled and resilient to burst traffic. Using an HTTPS front door with API Gateway allows the legacy system to send standard web requests and receive immediate acknowledgment. Placing SQS between ingestion and inference provides durable buffering and decoupling, so downstream processing can scale independently and tolerate temporary slowdowns or throttling. A Lambda consumer can then invoke the Bedrock model asynchronously and store outputs for downstream systems in a managed data store such as DynamoDB, minimizing the amount of infrastructure to operate. Further reading (AWS): - Integrate Amazon API Gateway with Amazon SQS to handle asynchronous REST APIs - AWS Prescriptive Guidance (AWS Documentation) - Using Lambda with Amazon SQS - AWS Lambda (AWS Documentation) - InvokeModel - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 37
Correct
A security operations team is building a Retrieval Augmented Generation (RAG) assistant by using Amazon Bedrock. The team stores runbooks and incident reports in Amazon OpenSearch Service as a vector store. Engineers often search by exact identifiers (for example, CVE IDs and command names), but the assistant frequently retrieves semantically similar documents that do not contain the exact identifier. The team also observes increased retrieval latency as the OpenSearch index has grown. Which solution will improve retrieval relevance and retrieval latency with the LEAST operational overhead?
Your answer is correct
In Amazon OpenSearch Service, implement hybrid search that combines vector similarity with keyword matching on identifier fields. Preprocess queries to extract exact identifiers into keyword clauses or metadata filters, and reindex the corpus into fewer, larger shards to reduce cross-shard coordination overhead.
Explanation
Correct. Hybrid search improves relevance by rewarding exact keyword matches (for identifiers) while still leveraging semantic similarity for natural-language queries. Simple query preprocessing (extracting identifiers into keyword clauses/filters) increases precision for identifier-driven lookups. Reindexing into fewer, larger shards reduces the amount of cross-shard fan-out and coordination work during vector and hybrid queries, improving latency. This approach stays within OpenSearch and does not add new services or complex model pipelines.
Tune the OpenSearch approximate nearest neighbor (ANN) index for higher recall by increasing HNSW parameters (such as ef_search and M) so that more candidates are evaluated during vector search.
Explanation
Incorrect. Increasing HNSW search parameters can increase recall, but it generally increases query latency and memory usage, which conflicts with the goal of reducing retrieval latency. It also does not directly address precision for exact identifier lookups, which are better handled with keyword matching and filtering.
Migrate embeddings from OpenSearch to Amazon S3 Vectors and query S3 Vectors directly for the nearest neighbors to reduce cost and simplify the architecture.
Explanation
Incorrect. S3 Vectors is positioned as a lower-cost and simpler vector store, but it is not optimized for performance-critical, low-latency retrieval compared with OpenSearch-based approaches. It also does not directly solve the need to incorporate keyword-based matching and custom scoring for exact identifiers.
Configure an Amazon Bedrock reranker model to rerank the top 50 retrieved chunks from OpenSearch based on semantic relevance to the query before sending context to the FM.
Explanation
Incorrect. Reranking can improve ordering of already-retrieved results, but it adds additional model inference work and typically increases end-to-end latency. It also cannot reliably fix cases where the initial retrieval set does not include documents containing the exact identifier.
Overall explanation
Identifier-heavy queries (like CVE IDs and command names) are best served by combining semantic retrieval with exact matching. Hybrid search improves relevance by blending vector similarity with keyword scoring, and simple query preprocessing can reliably extract identifiers into keyword clauses or metadata filters. To address speed, index optimization in OpenSearch—such as reindexing into fewer, larger shards—reduces coordination overhead and improves query latency as the corpus grows. Other approaches either add extra inference latency, trade speed for recall, or prioritize cost over low-latency retrieval. Further reading (AWS): - Hybrid Search with Amazon OpenSearch Service (AWS Blog) - k-Nearest Neighbor (k-NN) search in Amazon OpenSearch Service - Amazon OpenSearch Service (AWS Documentation) - Choosing the number of shards - Amazon OpenSearch Service (AWS Documentation)
Domain
Content Domain 4: Operational Efficiency and Optimization fo
Question 38
Incorrect
A fintech company is launching a public, GenAI-powered customer support chatbot that uses Amazon Bedrock to answer account questions. Security reviewers are concerned about prompt injection attempts (for example, users trying to override instructions or request hidden system prompts) and about accidental leakage of personally identifiable information (PII) in both user inputs and model outputs. The company wants a defense-in-depth approach that adds comprehensive protection against FM misuse with the LEAST operational overhead. Which solution meets these requirements?
Your answer is incorrect
Use Amazon Bedrock Guardrails only, configured to block profanity and remove PII. Rely on the guardrail blocked-message response to prevent misuse, and return the model response directly to callers.
Explanation
Incorrect. Guardrails are an important layer, but using only guardrails is not defense in depth. Additional layers (such as input pre-processing and post-processing validation/redaction) help reduce the impact of missed detections, integration mistakes, or novel prompt injection patterns.
Correct answer
Implement a serverless safety pipeline where Amazon API Gateway invokes a Lambda function that uses Amazon Comprehend to detect and mask PII in user prompts before invoking Amazon Bedrock with Bedrock Guardrails enabled. After the model responds, the Lambda function performs post-processing validation and additional PII redaction before returning the response through API Gateway response mapping templates that enforce a safe, filtered response shape.
Explanation
Correct. This design layers multiple controls: Comprehend provides pre-processing filters (including PII detection/masking), Bedrock Guardrails provide model-based safety controls for prompts and responses, Lambda enables custom post-processing validation/redaction for anything that slips through, and API Gateway can apply response transformations to enforce a safe response contract. This matches a defense-in-depth pattern while remaining largely managed and serverless.
Fine-tune a custom model in Amazon Bedrock by using past chat transcripts so the model learns the company’s policies and avoids unsafe outputs. Deploy the fine-tuned model without additional pre-processing or post-processing because the model has been customized.
Explanation
Incorrect. Customization (fine-tuning or continued pre-training) does not guarantee compliance with safety requirements and does not replace layered runtime controls. It also increases cost and operational effort compared to managed guardrails and lightweight pre/post-processing.
Place Amazon API Gateway in front of the chatbot. Use AWS WAF managed rules to block SQL injection and common web exploits, and then invoke Amazon Bedrock directly from the application with no additional controls.
Explanation
Incorrect. AWS WAF protects the HTTP interface, but it does not provide LLM-specific protections such as PII detection/redaction, prompt injection handling, or output moderation. Invoking the model without additional pre-processing, guardrails, and post-processing does not provide defense in depth for FM misuse.
Overall explanation
A defense-in-depth safety design uses multiple independent layers to reduce the probability and impact of unsafe inputs and outputs. A practical pattern is to filter and normalize inputs before model invocation (for example, detect and mask PII and suspicious content), apply model-native safety controls during inference (guardrails for prompt/response filtering and grounding-style checks where applicable), and validate or redact again after inference before returning data to end users. Implementing these layers with serverless components (Lambda, API Gateway) and managed services (Comprehend, Bedrock Guardrails) provides comprehensive protection with minimal infrastructure management, while approaches that rely on a single control layer (only WAF or only guardrails) or on model customization alone do not provide equivalent runtime protection against FM misuse. Further reading (AWS): - Detect and filter harmful content by using Amazon Bedrock Guardrails (AWS Documentation) - Personally identifiable information (PII) - Amazon Comprehend (AWS Documentation) - Mapping template transformations for REST APIs in API Gateway (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 39
Correct
An insurance company is building an internal GenAI assistant that drafts claim denial letters by using an Amazon Bedrock FM. Company policy requires a licensed adjuster to review and approve (or edit) each draft before the letter is sent to a customer. The company also wants to capture the adjuster’s rating and final approved text for later analysis. Review times can vary from minutes to hours. Which solution will meet these requirements with the LEAST operational overhead?
Send each prompt to an Amazon SQS queue. Have adjusters read messages directly from the queue, write approvals back to a second queue, and have a Lambda consumer send the final letter to the customer.
Explanation
Incorrect. This approach places workflow coordination responsibilities on the adjusters and requires additional custom coordination logic to correlate requests with approvals and to manage edits and ratings. It also lacks a centralized, auditable review/approval workflow without building extra orchestration and data models.
Invoke the FM from an AWS Lambda function behind Amazon API Gateway. Email the draft to the adjuster and poll the email inbox from Lambda until an approval reply is received. Store approvals in Amazon S3.
Explanation
Incorrect. Polling an email inbox introduces significant custom operational logic, error handling, and state management. It is also difficult to reliably correlate replies to the original request and to handle long-running review times without building additional infrastructure.
Use Amazon Bedrock Guardrails to enforce compliance and safety policies for the denial letters, and automatically send the FM output to customers without human review. Store all prompts and responses in Amazon CloudWatch Logs for auditing.
Explanation
Incorrect. Guardrails can help filter content and improve safety, but they do not satisfy a requirement for human review and approval. Logging prompts and responses supports auditing but does not provide a collaborative approval step or capture explicit reviewer feedback and edits.
Your answer is correct
Use Amazon API Gateway to accept the request and start an AWS Step Functions state machine. The state machine invokes the Amazon Bedrock FM to create a draft, stores the draft and status in Amazon DynamoDB, notifies adjusters (for example, through Amazon SNS), and waits for the adjuster to submit an approval/edit and rating through an API Gateway endpoint. The state machine then continues to send the approved letter and records the final output and reviewer feedback in DynamoDB.
Explanation
Correct. AWS Step Functions provides a managed orchestration layer for review and approval workflows, including the ability to wait for human actions. API Gateway provides a simple, controlled interface for collecting human feedback and decisions. DynamoDB is well-suited for tracking draft status, storing reviewer inputs, and enabling later analysis, all with minimal operational overhead compared to custom polling or manual queue-based coordination.
Overall explanation
A human-in-the-loop design needs an explicit approval step, durable storage of the draft and the reviewer’s edits/ratings, and orchestration that can span variable human response times. A managed workflow service can coordinate the end-to-end process: generate the draft with the FM, route it to a reviewer for approval, and continue only after the review is complete. Exposing a dedicated feedback endpoint simplifies collecting structured reviewer decisions and ratings, and storing this information in a low-maintenance database enables traceability and later evaluation. Alternatives that rely on email polling or forcing reviewers to interact directly with queues increase custom code, coordination complexity, and operational burden, while relying only on safety controls does not fulfill the mandatory human review requirement. Further reading (AWS): - Deploying a workflow that waits for human approval in Step Functions - AWS Step Functions (AWS Documentation) - Discover service integration patterns in Step Functions - AWS Step Functions (AWS Documentation) - Building Generative AI prompt chaining workflows with human in the loop | Artificial Intelligence (AWS Blog)
Domain
Content Domain 2: Implementation and Integration
Question 40
Correct
A manufacturing company is building an internal GenAI assistant on AWS by using Amazon Bedrock and a RAG architecture. Engineering specifications are stored in a SharePoint Online document library, and design decision records are maintained in an Atlassian Confluence wiki. The customer support team maintains troubleshooting articles in Salesforce. For compliance reasons, a copy of the Salesforce articles must also be stored in Amazon S3. The company wants to integrate these sources into a single retrieval layer with the LEAST operational overhead. Which combination of actions will meet these requirements? (Select TWO.)
Your selection is correct
Create an Amazon Bedrock Knowledge Base and configure Confluence and SharePoint as data sources. Use an embedding model (for example, Amazon Titan embeddings) and a managed vector store integration to support semantic retrieval for the RAG application.
Explanation
Correct. Amazon Bedrock Knowledge Bases provide managed connectors for common enterprise content systems such as Confluence and SharePoint, and they manage ingestion, chunking/embedding, and retrieval integration for RAG with minimal custom integration code.
Build a custom ingestion pipeline that exports content from Confluence, SharePoint, and Salesforce, generates embeddings by calling the Amazon Bedrock Runtime API from application code, and indexes vectors directly into Amazon OpenSearch Service.
Explanation
Incorrect. Although OpenSearch can be used as a vector store, this option requires building and operating custom connectors, embedding pipelines, and indexing workflows. It does not meet the requirement for the least operational overhead compared with managed Knowledge Base integrations and managed SaaS data transfer.
Create AWS Lambda functions to scrape Confluence and SharePoint content into Amazon DynamoDB tables. At query time, run DynamoDB scans and add the returned items directly into the FM prompt as context.
Explanation
Incorrect. DynamoDB scans are inefficient for retrieval and do not provide semantic search over embeddings. This approach also increases operational overhead by requiring custom scraping, storage logic, and retrieval logic.
Use AWS Glue crawlers to discover Confluence, SharePoint, and Salesforce schemas and store metadata in the AWS Glue Data Catalog. Query the sources with Amazon Athena at runtime to retrieve relevant passages for RAG.
Explanation
Incorrect. Glue crawlers and Athena are designed for querying data in S3 and supported data sources using SQL and schema-on-read patterns, not as a direct integration mechanism for enterprise wiki/document systems for semantic retrieval. This also creates a brittle runtime dependency on ad hoc SQL retrieval rather than vector-based retrieval.
Your selection is correct
Use Amazon AppFlow to replicate Salesforce knowledge articles into an Amazon S3 bucket on a schedule or on demand. Configure the Amazon Bedrock Knowledge Base to ingest the S3 content so the RAG application can retrieve from it.
Explanation
Correct. AppFlow is a managed SaaS-to-AWS integration service that can move Salesforce data into S3, satisfying the compliance requirement to keep a copy in S3. Using S3 as a Knowledge Base data source then provides managed ingestion into the retrieval layer.
Overall explanation
The lowest-operations approach is to use managed integration components rather than building custom crawlers and ingestion pipelines. Amazon Bedrock Knowledge Bases can connect directly to enterprise knowledge sources such as Confluence and SharePoint and provide a managed path from source documents to semantic retrieval for RAG. For Salesforce content, Amazon AppFlow provides a managed way to replicate SaaS data into Amazon S3 to satisfy the requirement to keep an S3 copy, and the Knowledge Base can then ingest from S3 to make that content available to the RAG application. The other approaches either rely on non-semantic retrieval methods, use services that are not designed for this type of enterprise-content integration, or require significant custom pipeline implementation and ongoing maintenance. Further reading (AWS): - Connect a data source to your knowledge base - Amazon Bedrock (AWS Documentation) - Connect to Amazon S3 for your knowledge base - Amazon Bedrock (AWS Documentation) - Salesforce connector for Amazon AppFlow - Amazon AppFlow (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 41
Correct
A fintech company is building a loan pre-qualification assistant. The assistant uses an AWS Step Functions state machine to orchestrate an agent-like loop: it invokes an Amazon Bedrock FM to decide the next action and, when needed, invokes an AWS Lambda function that calls a third-party credit bureau API. During intermittent credit bureau outages, the FM keeps requesting the tool call, which causes repeated loop iterations and increased token usage. The company needs to stop the workflow after a small number of tool failures, temporarily disable credit bureau calls for a cooldown period to prevent repeated failures, and ensure the tool Lambda function cannot access resources beyond a specific DynamoDB table and a single secret in AWS Secrets Manager. Which solution will meet these requirements with the LEAST operational overhead?
Implement a circuit breaker inside the Lambda function by using an in-memory variable to skip credit bureau calls for 10 minutes after a failure. Invoke the Amazon Bedrock FM directly from the Lambda function and stop the loop by returning an error to the caller. Use an IAM permissions boundary on the IAM user who invokes the state machine to restrict access.
Explanation
Incorrect. An in-memory variable in Lambda is not a reliable circuit breaker because execution environments are reused unpredictably and do not provide durable shared state across concurrent invocations. Adding a permissions boundary to the calling IAM user does not restrict what the Lambda execution role can access. This option also removes Step Functions’ ability to enforce deterministic stopping conditions at the workflow level.
Configure Step Functions retries with exponential backoff on the Lambda task and set a maximum retry count of 3. Keep the state machine looping until the model returns a final answer. Grant the Lambda function AmazonDynamoDBFullAccess and SecretsManagerReadWrite to simplify access management.
Explanation
Incorrect. Limiting retries on a single task does not prevent the overall model-driven loop from repeatedly requesting the tool across multiple iterations. This approach also does not implement a circuit breaker and uses overly broad IAM permissions that violate the requirement to enforce resource boundaries.
Your answer is correct
Use Step Functions to implement a loop counter and stopping condition with a Choice state that ends the workflow after a maximum number of tool failures. Implement a circuit breaker by storing an “open/closed” flag with a TTL in DynamoDB that the state machine checks before calling the tool. Configure the tool Lambda with a short timeout and least-privilege IAM permissions scoped to the required DynamoDB table and the specific Secrets Manager secret.
Explanation
Correct. Step Functions can enforce explicit stopping conditions by tracking failure counts in state input and branching with a Choice state. A DynamoDB-backed circuit breaker persists across executions and can implement a cooldown period by using TTL. A short Lambda timeout prevents long-running tool calls, and least-privilege IAM policies on the Lambda execution role enforce resource boundaries.
Place the third-party credit bureau API behind Amazon API Gateway and configure throttling with a usage plan. Add AWS WAF rate-based rules to block repeated requests during outages. Leave the Step Functions workflow and Lambda IAM permissions unchanged to minimize changes.
Explanation
Incorrect. Throttling and rate-based blocking can reduce request volume but do not provide deterministic stopping conditions for the agent loop, and they can increase failure rates without implementing a controlled fallback path. This option also does not enforce least-privilege access for the tool Lambda function.
Overall explanation
A safeguarded agentic workflow needs explicit controls that are independent of the FM’s behavior. Implementing stopping conditions in Step Functions prevents unbounded loops when the model keeps requesting a failing tool. A circuit breaker requires durable shared state so the system can “fail fast” during an outage and recover automatically after a cooldown; DynamoDB is well-suited for persisting the breaker state and can use TTL to automatically expire an “open” state. Finally, controlling what the tool can do is enforced by scoping the Lambda execution role to least privilege (only the required DynamoDB table operations and a specific secret), which ensures the FM cannot indirectly access other resources through the tool. Further reading (AWS): - Using the circuit breaker pattern with AWS Step Functions and Amazon DynamoDB (AWS Blog) - IAM policy to read, write, update, and delete access on a DynamoDB table (AWS Documentation) - Use Secrets Manager secrets in Lambda functions (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 42
Correct
An insurance technology team is building an interactive claims assistant. An AWS Lambda function reads a user’s conversation history from Amazon DynamoDB and streams responses from an Amazon Bedrock text model by using the ConverseStream API. The same Lambda function also invokes a custom text-classification model deployed on an Amazon SageMaker AI real-time endpoint to label each new user message before it is stored. After a refactor, Bedrock returns a validation error indicating required fields are missing, and SageMaker returns a JSON parsing error. The team wants to correct the input formatting for both inference calls with the LEAST additional development effort. Which solution will meet these requirements?
Change the Lambda function to concatenate the full conversation history into a single plain-text prompt and invoke the Bedrock InvokeModelWithResponseStream API. Send the same plain-text payload to the SageMaker endpoint for classification.
Explanation
Incorrect. Bedrock streaming can be done with other APIs, but sending a single concatenated string does not address the model-specific request schema expected by ConverseStream. Additionally, SageMaker endpoints typically expect a specific structured request body (commonly JSON) defined by the model’s inference code, and SageMaker will not automatically parse an arbitrary plain-text payload into the expected schema.
Invoke the Bedrock control plane endpoint (bedrock) instead of bedrock-runtime and pass the conversation history as a JSON string. Keep the SageMaker request unchanged.
Explanation
Incorrect. The bedrock endpoint is for management operations, not for runtime inference. Inference requests must be sent to the runtime endpoint (bedrock-runtime). This also does not fix the SageMaker parsing issue.
Base64-encode the conversation history and pass it as an inputImage field to a multimodal Amazon Titan embedding model to standardize the request format across Bedrock and SageMaker.
Explanation
Incorrect. This uses an embedding model interface that is not intended for chat generation and does not match the required ConverseStream schema. It also does not address the SageMaker endpoint’s expected input format for the classification model.
Your answer is correct
In the Lambda function, format the Bedrock request body as a ConverseStream messages structure that includes a messages array with role and content fields for each turn. Separately, send a SageMaker InvokeEndpoint request with a JSON body that matches the SageMaker model’s expected input schema (for example, a single field that contains the latest user text) and set the request Content-Type to application/json.
Explanation
Correct. ConverseStream requires the conversation to be represented as structured JSON with a messages array where each message contains a role (for example, user or assistant) and content (for example, text). SageMaker endpoints do not transform inputs automatically; the caller must serialize the payload in the exact format required by the deployed model’s inference handler (commonly JSON) and provide the appropriate content type. Creating two correctly formatted request payloads aligns with each service’s model-specific inference requirements with minimal code changes.
Overall explanation
The key fix is to format inference inputs according to each target’s required schema. For Amazon Bedrock conversational inference, the Converse/ConverseStream APIs expect a structured JSON conversation format that explicitly represents each turn with role and content fields. For Amazon SageMaker AI endpoints, the model container defines the request schema, and SageMaker does not automatically adapt a Bedrock-style conversation payload; the application must construct the exact JSON shape (and content type) that the endpoint expects. Using separate, model-specific request bodies resolves both validation and parsing errors without redesigning the application. Further reading (AWS): - ConverseStream - Amazon Bedrock (AWS Documentation) - Using the Converse API - Amazon Bedrock (AWS Documentation) - InvokeEndpoint - Amazon SageMaker (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 43
Correct
An online retailer’s GenAI team is building a product discovery feature on Amazon Bedrock. Customers will search either by uploading a product photo or by entering a short text description. The backend will perform semantic similarity search in a vector store so the same search experience works for both photos and text queries. Which Amazon Bedrock foundation model choice will meet these requirements with the LEAST operational overhead?
Use Anthropic Claude to answer user queries conversationally and include the entire product catalog in the prompt so the model can find the best matching items without vector search.
Explanation
Incorrect. This does not align with the technical requirement to use semantic similarity search in a vector store. Putting a large catalog into prompts is not scalable and is constrained by context window limits and cost. Claude is a conversational LLM rather than an embeddings model for multimodal vector retrieval.
Your answer is correct
Use an Amazon Titan multimodal embeddings model to generate embeddings from both the product image and associated text so they can be compared in the same vector space.
Explanation
Correct. The Amazon Titan multimodal embeddings model is designed to embed images, text, or both into vector representations that can be stored and queried in a vector database. This directly supports “search by image” and “search by text” using a single retrieval mechanism, without adding additional captioning or translation components.
Use Stable Diffusion to generate a text caption for each product image, then use an Amazon Titan text embeddings model to embed the generated captions and user text queries for similarity search.
Explanation
Incorrect. Stable Diffusion is an image generation model, not an image captioning or embedding model. Even if captions were produced by another component, a multi-step caption-then-embed approach adds extra processing, cost, and error modes (caption quality becomes a dependency) compared with using a multimodal embeddings model directly.
Use an Amazon Titan text embeddings model for all content and store only the product descriptions (not images) in the vector store to simplify ingestion.
Explanation
Incorrect. A text-only embeddings model cannot embed images, so the system would not support photo-based queries as required. Dropping images simplifies ingestion but fails the core business requirement of unified search across photos and text.
Overall explanation
The key technical requirement is to support semantic similarity search when the query might be either an image or text. This requires an embeddings model that can produce comparable vectors for both modalities. A Titan multimodal embeddings model provides this capability directly, allowing the application to embed product images and user photo queries using the same embedding approach used for text. The alternative options either use models that are not intended for embeddings, avoid vector search entirely, or rely on text-only embeddings that cannot represent images. Further reading (AWS): - Amazon Titan Multimodal Embeddings G1 model - Amazon Bedrock (AWS Documentation) - Amazon Titan Image Generator, Multimodal Embeddings, and Text models are now available in Amazon Bedrock | AWS News Blog (AWS Blog) - Easily build semantic image search using Amazon Titan | Artificial Intelligence (AWS Blog)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 44
Correct
A regulated digital bank is launching a GenAI feature that uses an FM to generate customer-facing explanations for lending decisions. During audits, the bank must demonstrate which model version produced each explanation, which approved data sources were used to generate the response, and provide an immutable record of the decision workflow for later review. The team wants to meet these compliance requirements with the LEAST operational overhead. Which solution should the team implement?
Publish all model requests and responses to an Amazon OpenSearch Service domain and create OpenSearch Dashboards for auditors. Document model behavior in a shared wiki and require developers to manually update the wiki after each model change.
Explanation
Incorrect. OpenSearch can store and search logs, but it does not provide standardized model documentation or automated lineage capabilities on its own. Relying on manual wiki updates for model documentation increases operational overhead and introduces compliance risk due to inconsistent and error-prone processes.
Store prompts, model IDs, and retrieved document IDs in an Amazon DynamoDB table for each request. Write a custom AWS Lambda function that periodically exports these records to Amazon S3 with a JSON schema for auditors.
Explanation
Incorrect. This approach adds significant operational overhead because the team must design and maintain a custom logging and governance solution. It also does not establish standardized model documentation (model cards) or automate data lineage tracking across ingestion and transformation workflows.
Enable AWS CloudTrail across all Regions and store CloudTrail logs in an Amazon S3 bucket. Use Amazon Athena to query the CloudTrail logs when auditors request evidence about model usage.
Explanation
Incorrect. CloudTrail provides an audit trail of API activity, but it does not provide a complete compliance framework for FM outputs. It does not capture application-level decision context (such as retrieved sources, prompt templates, or response payloads) or provide standardized model documentation and data lineage.
Your answer is correct
Create programmatic model cards in Amazon SageMaker AI for each approved model version and deployment. Use AWS Glue Data Catalog and metadata tagging to register and attribute approved data sources and transformations for lineage. Configure the application to write comprehensive decision logs (for example, model identifier, prompt template version, retrieved source identifiers, and response metadata) to Amazon CloudWatch Logs.
Explanation
Correct. This approach directly addresses regulatory needs with AWS-native governance components: SageMaker AI model cards document model intent, limitations, and versioning; AWS Glue enables lineage and systematic data source attribution through cataloging and metadata tagging; and CloudWatch Logs provides centralized, queryable decision logs that capture the complete decision workflow beyond what API audit logs provide.
Overall explanation
A practical compliance framework for FM deployments usually needs three complementary capabilities: standardized model documentation, traceability of data sources and transformations, and durable decision logs for audit review. Programmatic model cards provide an auditable record of model purpose and versioned deployment context. A data catalog with metadata tagging and lineage allows the organization to prove which governed data sources were used (and how they were processed) in support of outputs. Centralized decision logs capture the application-level context (such as prompt template version and retrieved source identifiers) that is typically required to reconstruct and defend decisions during audits. Using managed, AWS-native services for these tasks minimizes operational burden compared to building custom governance systems or relying on manual documentation. Further reading (AWS): - Amazon SageMaker Model Cards - Amazon SageMaker AI (AWS Documentation) - Assigning LF-Tags to Data Catalog resources - AWS Lake Formation (AWS Documentation) - What is Amazon CloudWatch Logs? - Amazon CloudWatch Logs (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 45
Correct
A media analytics team uses Amazon Bedrock to generate short summaries for hundreds of thousands of customer call transcripts every night. A Lambda function currently reads each transcript from Amazon S3 and invokes the model one request at a time. The team is frequently throttled during the batch window and the job does not finish by morning. The summaries can be generated asynchronously, and the output must be stored in Amazon S3 for downstream processing. Which solution will increase throughput for this workload MOST cost-effectively?
Put Amazon API Gateway in front of the Lambda function and enable API Gateway caching to reduce repeated model invocations for similar transcripts.
Explanation
Incorrect. API caching is effective when identical requests repeat and responses can be reused. In transcript summarization, inputs are typically unique, so caching provides little benefit and does not address Bedrock throttling or throughput constraints.
Purchase Amazon Bedrock provisioned throughput for the model and continue invoking the model synchronously from the existing Lambda function for each transcript.
Explanation
Incorrect. Provisioned throughput can increase available capacity, but it is generally an expensive way to address a nightly, asynchronous batch use case. It does not reduce the number of model invocations or per-request overhead, and it can lead to paying for capacity that is idle outside the batch window.
Your answer is correct
Use Amazon Bedrock batch inference by writing the prompts to an input file in Amazon S3, submitting a batch inference job, and storing the batch output in Amazon S3 for downstream processing.
Explanation
Correct. Batch inference is designed for high-throughput, asynchronous processing by submitting many prompts together and writing results back to Amazon S3. This reduces orchestration overhead from per-item invocations and is well suited for nightly jobs where results are not required in real time.
Publish one SQS message per transcript and increase AWS Lambda reserved concurrency so more messages are processed in parallel. Add exponential backoff retries when Bedrock returns throttling errors.
Explanation
Incorrect. Increasing concurrency can improve parallelism, but it also increases the chance of hitting Amazon Bedrock throughput limits during peak processing. This approach still performs a very large number of individual model invocations with per-request overhead and requires active management of concurrency, retries, and failure handling.
Overall explanation
For large, offline GenAI workloads, optimizing throughput is primarily about reducing per-request overhead and efficiently managing how many model invocations are executed. Amazon Bedrock batch inference is intended for this pattern: prompts are placed in Amazon S3, a batch job processes them at scale, and the results are delivered back to Amazon S3 for later consumption. Approaches that rely on scaling Lambda concurrency or adding an API layer still generate a large number of individual invocations, which can amplify throttling and increase operational complexity. Provisioned throughput can help for sustained, predictable real-time demand, but it is usually not the most cost-effective choice for a nightly asynchronous batch pipeline. Further reading (AWS): - Process multiple prompts with batch inference - Amazon Bedrock (AWS Documentation) - Format and upload your batch inference data - Amazon Bedrock (AWS Documentation) - Create a batch inference job - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 4: Operational Efficiency and Optimization fo
Question 46
Correct
A compliance engineering team is building an incident-report summarization feature by using an Amazon Bedrock text model. The summaries must be consistent in tone and avoid speculative language. During testing, the same report sometimes produces different wording and occasionally adds unsupported details. The team must continue using the same model and wants a data-driven way to validate changes before updating production. Which approach will MOST effectively improve output quality for this use case?
Create an Amazon Bedrock custom model by fine-tuning the base model with labeled prompt-completion pairs that contain ideal summaries. Deploy the custom model and route all summarization traffic to the new custom model.
Explanation
Incorrect. Fine-tuning can improve behavior, but it changes the model (custom model) and introduces additional lifecycle management, time, and cost. The scenario specifically calls for continuing to use the same model and focuses on improving results through configuration and validation rather than customization.
Your answer is correct
Adjust the model’s inference parameters to reduce randomness (for example, lower temperature and tune top-p or top-k based on the desired determinism). Use Amazon Bedrock Model Evaluations with a representative prompt dataset and reference summaries to compare the parameter configurations and select the best-performing configuration for production.
Explanation
Correct. Lowering temperature and appropriately tuning top-p/top-k can make outputs more deterministic and reduce creative variation. Bedrock Model Evaluations provides a structured A/B-style comparison using a prompt dataset (and optional reference responses) so the team can quantitatively validate quality improvements before rollout.
Enable Amazon Bedrock prompt caching for the summarization prompt prefix and increase maxTokens to reduce truncation. Keep the rest of the application unchanged and monitor user complaints after deployment.
Explanation
Incorrect. Prompt caching and maxTokens primarily address latency/cost and output length limits, not hallucinations or variability in phrasing and speculative content. This option also lacks a pre-deployment evaluation method.
Add an Amazon Bedrock Knowledge Base and configure a reranker model so the summarization model always receives more context. Keep the inference parameters unchanged and rely on the additional context to reduce output variability.
Explanation
Incorrect. Adding retrieval and reranking changes the architecture and targets grounding via external context, not model parameter tuning. It increases system complexity and does not directly address the need to tune generation behavior for consistent, non-speculative summaries while keeping the same model.
Overall explanation
To reduce variation and speculative language without changing the underlying model, the most direct control is model-specific inference configuration. Lower temperature generally reduces randomness, while top-p or top-k tuning constrains token sampling to make responses more consistent. Because the team needs a data-driven validation method before production changes, running an evaluation with a representative prompt dataset and reference summaries provides measurable evidence of whether the new parameter configuration improves quality for the specific summarization use case. Approaches that focus on caching/throughput do not improve response quality, and adding retrieval or fine-tuning either changes the architecture or violates the requirement to keep using the same model. Further reading (AWS): - Influence response generation with inference parameters - Amazon Bedrock (AWS Documentation) - Starting an automatic model evaluation job in Amazon Bedrock - Amazon Bedrock (AWS Documentation) - Use prompt datasets for model evaluation in Amazon Bedrock - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 4: Operational Efficiency and Optimization fo
Question 47
Incorrect
A SaaS provider runs an interactive RAG assistant for internal help desk agents. The request path is Amazon API Gateway to AWS Lambda, where the function performs a vector similarity search in Amazon OpenSearch Service and then calls an Amazon Bedrock FM by using the Converse API. Users report that responses feel slow, and profiling shows frequent connection setup overhead between Lambda and downstream services and high query fan-out across many OpenSearch shards during vector searches. The provider wants to reduce p95 end-to-end latency for the chat experience without changing the FM or the underlying document corpus and with the LEAST operational overhead. Which combination of actions should the provider take? (Select TWO.)
Correct selection
Reindex the OpenSearch vector index with fewer, larger shards sized for semantic search workloads to reduce cross-shard coordination during k-NN queries.
Explanation
Correct. Vector search can be slowed by coordinating many shard-level searches and merging results. Using fewer, larger shards (appropriate shard sizing) reduces fan-out and coordination overhead, improving retrieval latency without changing the FM or the document set.
Correct selection
Refactor the Lambda function to reuse HTTP/AWS SDK clients across invocations (for example, initialize clients outside the handler) and configure connection pooling/keep-alive for calls to OpenSearch and Amazon Bedrock.
Explanation
Correct. Reusing clients allows Lambda execution environments to keep connections open across invocations, reducing repeated TLS handshakes and connection setup time. This is a low-overhead change that improves service-to-service communication efficiency and directly reduces latency for GenAI request chains.
Your selection is incorrect
Enable Amazon Bedrock global cross-Region inference to route all model invocations to the least busy Region for faster responses.
Explanation
Incorrect. Cross-Region inference is primarily a resilience/throughput strategy and can add network latency when requests are served from a different Region. It also does not address the identified retrieval and connection setup bottlenecks.
Tune OpenSearch HNSW settings by increasing ef_search and M values to maximize recall for every query, even if memory usage increases.
Explanation
Incorrect. Increasing ef_search and M can increase query time and memory consumption. The scenario’s primary problem is latency; maximizing recall at the expense of performance typically makes p95 latency worse and does not align with the goal.
Your selection is incorrect
Turn on Amazon Bedrock prompt caching by placing the retrieved OpenSearch context into the cached prompt prefix so future queries can reuse it.
Explanation
Incorrect. Prompt caching is most effective for a stable, reusable prompt prefix (for example, instructions and few-shot examples). Retrieved RAG context is dynamic per query, so caching it as the prefix will result in low cache reuse and can increase cache write overhead without addressing OpenSearch retrieval latency.
Overall explanation
The largest latency gains come from optimizing the two proven bottlenecks in the workflow: service-to-service communication and vector retrieval execution. Reusing clients with connection pooling reduces repeated connection establishment overhead between Lambda and downstream services, lowering end-to-end latency without changing the FM or architecture. Separately, tuning OpenSearch for vector search by reducing shard fan-out (fewer, larger shards sized appropriately for semantic search) reduces coordination overhead and speeds retrieval, which is a major contributor to total RAG response time. Options that prioritize recall tuning, rely on cross-Region routing, or attempt to cache dynamic retrieved context either increase latency, fail to address the true bottleneck, or have low effectiveness for this workload. Further reading (AWS): - AWS Lambda client not reused | Amazon Q, Detector Library (AWS Documentation) - Reuse connections with keep-alive in Node.js - AWS SDK for JavaScript (AWS Documentation) - Choosing the number of shards - Amazon OpenSearch Service (AWS Documentation)
Domain
Content Domain 4: Operational Efficiency and Optimization fo
Question 48
Correct
A SaaS provider is building a customer-support chat application that uses the Amazon Bedrock Converse API with a single FM. The application stores full conversation history in Amazon DynamoDB and uses an Amazon Bedrock Knowledge Base for RAG. As usage grows, the company’s Bedrock spend increases sharply, and some requests fail when the prompt exceeds the model’s context window. The company wants to reduce token-related costs while preserving answer quality. Which combination of actions will meet these requirements? (Select TWO.)
Increase the knowledge base chunk size so that fewer chunks are retrieved, and disable metadata-based filtering to avoid excluding potentially relevant content. Send the larger chunks to the FM to preserve answer quality.
Explanation
Incorrect. Increasing chunk size can increase the total number of tokens included per retrieved result, which can raise input token usage and worsen context-window overflow. Disabling metadata filtering can also introduce irrelevant context, reducing answer quality and increasing tokens spent on noise.
Your selection is correct
Before each model invocation, call the Amazon Bedrock CountTokens API to estimate total input size. If the request exceeds a token budget, prune older conversation turns and reduce the number of retrieved chunks from the knowledge base. Publish input and output token counts to Amazon CloudWatch for ongoing tracking.
Explanation
Correct. Using CountTokens provides a low-cost way to estimate token usage before inference. Pruning conversation history and limiting the number of retrieved RAG chunks reduces input tokens while still keeping the most relevant context. Tracking token counts in CloudWatch enables continuous optimization and quick detection of regressions in prompt size or response verbosity.
Enable Amazon Bedrock prompt caching for the application’s system prompt and few-shot examples so the cached prefix is discounted. Keep the full conversation history and the same number of retrieved RAG chunks in every request.
Explanation
Incorrect. Prompt caching can reduce costs for repeated static prompt prefixes, but it does not reduce the prompt’s token count. Keeping the full conversation history and large RAG context still risks context-window overflow and continues to send excessive dynamic tokens that cannot be cached.
Enable Amazon Bedrock provisioned throughput and use the latency-optimized performance configuration for the FM to reduce per-request token costs and prevent context-window overflow errors during peak traffic.
Explanation
Incorrect. Provisioned throughput and latency-optimized configurations address throughput and latency, not prompt size. They do not reduce the number of input or output tokens sent to the model and therefore do not directly fix context-window overflows caused by oversized prompts.
Your selection is correct
Implement prompt compression by using a smaller FM to summarize older conversation history into a short running summary that is stored in DynamoDB. Include only the summary plus the most recent turns in each request and set maxTokens to limit response size.
Explanation
Correct. Summarizing older chat history reduces the number of tokens that must be sent on every request while preserving key information needed for quality responses. Including only recent turns maintains conversational relevance. Setting maxTokens provides response size control to prevent unexpectedly large outputs from increasing cost.
Overall explanation
The most effective token-efficiency approaches reduce how many tokens are included in each request while preserving the information needed for accurate answers. Estimating tokens before invocation enables enforcing a token budget and applying deterministic reductions such as pruning conversation history and limiting retrieved RAG context. Prompt compression through summarization is another high-impact technique: it replaces long histories with a compact summary plus recent turns, and response limiting prevents large completions from driving up cost. Options focused on performance/throughput settings or on caching without reducing dynamic context do not reliably prevent context-window overflows or meaningfully lower token-driven spend for this workload. Further reading (AWS): - Monitor your token usage by counting tokens before running inference - Amazon Bedrock (AWS Documentation) - Query a knowledge base and retrieve data - Amazon Bedrock (AWS Documentation) - Build a .NET Context-Aware Generative AI Chatbot using Amazon Bedrock and LangChain | .NET on AWS Blog (AWS Blog)
Domain
Content Domain 4: Operational Efficiency and Optimization fo
Question 49
Correct
A media streaming provider uses an API Gateway endpoint backed by an AWS Lambda function to call an Amazon Bedrock FM that generates podcast episode summaries. The team wants to reduce cost by switching to a smaller FM and tuning inference parameters (for example, temperature and max token limits). Before rollout, the team must verify summary quality does not regress, quantify the latency-to-quality and cost-to-quality tradeoffs, and introduce the new configuration to production traffic gradually with an easy rollback path. Which combination of steps should the team take to meet these requirements with the LEAST operational overhead? (Select TWO.)
Create an Amazon Bedrock custom model by fine-tuning the FM on past transcripts so the model learns the provider’s preferred style. Promote the fine-tuned model to production after a small set of spot checks by editors.
Explanation
Incorrect. Fine-tuning increases cost and complexity and does not directly address systematic comparison of multiple configurations. Spot checks are not a repeatable evaluation system, and this approach shifts the problem from selecting an optimal configuration to building and maintaining a customized model.
Your selection is correct
Deploy two AWS Lambda versions behind a single alias: one version calling the existing FM configuration and one version calling the candidate configuration. Use AWS CodeDeploy canary deployments to shift a small percentage of production traffic to the candidate version and automatically roll back based on Amazon CloudWatch alarms for error rate and latency.
Explanation
Correct. A canary deployment provides controlled exposure of the new configuration to live traffic and a straightforward rollback mechanism. Monitoring CloudWatch alarms during the canary validates operational behavior (latency and errors) under real workload conditions without requiring complex orchestration.
Enable detailed AWS Lambda logs and export the logs to Amazon S3. Use Amazon CloudWatch Logs Insights to manually review a small sample of model responses and estimate cost by averaging Lambda duration and memory usage.
Explanation
Incorrect. Manual log sampling is not a systematic evaluation method and does not reliably compare multiple candidate configurations. Lambda duration is not a good proxy for FM inference cost (token-based) or FM latency, and this approach does not produce repeatable quality metrics.
Your selection is correct
Create a prompt dataset in Amazon S3 that includes representative transcripts (or excerpts) and reference summaries. Run Amazon Bedrock Model Evaluations across multiple candidate FMs and parameter configurations by using an LLM-as-a-judge. Use Amazon CloudWatch metrics (such as model latency and input/output token counts) collected during the runs to compute latency-to-quality and cost-to-quality ratios.
Explanation
Correct. Bedrock Model Evaluations provides a systematic way to compare models and inference settings on the same prompt dataset. Using token counts and latency metrics makes it possible to quantify price/performance (token efficiency and latency-to-quality) alongside quality scores without building a custom evaluation framework.
Use Amazon S3 Lifecycle policies to move older transcripts to lower-cost storage classes and run periodic batch summarization jobs to compare completion time across models. Select the model that finishes first to optimize performance.
Explanation
Incorrect. Storage lifecycle policies are unrelated to model evaluation. Comparing only completion time ignores summary quality and token-based cost, and periodic batch jobs do not provide a controlled canary mechanism for safe rollout to production traffic.
Overall explanation
A systematic evaluation approach should first compare candidate foundation models and inference parameter settings on a consistent prompt dataset, using an evaluation method that produces repeatable quality scores. Then, the team should validate the chosen configuration under real production conditions with a controlled rollout mechanism that supports quick rollback. Amazon Bedrock Model Evaluations enables structured, repeatable multi-model and multi-configuration testing using a prompt dataset, and it can be paired with operational metrics (token counts and latency) to calculate cost-to-quality and latency-to-quality tradeoffs. A CodeDeploy canary deployment for Lambda provides a low-ops way to gradually shift production traffic to the new configuration while monitoring alarms and rolling back automatically if issues occur. The other approaches either rely on manual review, optimize unrelated parts of the system, or introduce unnecessary customization complexity without providing systematic comparisons. Further reading (AWS): - Evaluate model performance using another LLM as a judge - Amazon Bedrock (AWS Documentation) - Working with deployment configurations in CodeDeploy - AWS CodeDeploy (AWS Documentation) - Configure advanced options for a deployment group - AWS CodeDeploy (AWS Documentation)
Domain
Content Domain 5: Testing, Validation, and Troubleshooting
Question 50
Correct
A financial news provider is building a GenAI feature that generates short, compliant market summaries for traders by invoking an Amazon Bedrock text model from an AWS Lambda function. Traffic is highly predictable: every weekday at market open, request volume increases by about 10x for 45 minutes. During this window, users experience increased latency and occasional throttling errors from the model. The provider must keep all inference in a single AWS Region and wants consistent throughput with minimal operational overhead. Which solution will MOST effectively maximize throughput and resource utilization for this workload?
Deploy the model behind a SageMaker AI real-time endpoint with AWS Auto Scaling and invoke the endpoint from AWS Lambda. Use CloudWatch to scale the endpoint during market open based on CPU and request metrics.
Explanation
Incorrect. This increases operational overhead and changes the integration pattern away from managed Bedrock model invocation. It also introduces additional deployment and scaling complexity that is unnecessary when the requirement is primarily to increase Bedrock model throughput during predictable spikes.
Write each prompt to Amazon S3 and use Amazon Bedrock batch inference to process prompts asynchronously. Store the generated summaries back in Amazon S3 and notify users when processing completes by using Amazon EventBridge.
Explanation
Incorrect. Batch inference is intended for asynchronous, throughput-oriented processing where users can wait for results. It does not meet the near-real-time interactive requirement implied by a trader-facing feature during market open, and it changes the user experience to a delayed, file-based workflow.
Your answer is correct
Use Amazon Bedrock provisioned throughput for the selected model sized from expected requests-per-minute and tokens-per-minute. Update the application to invoke the provisioned model ARN, and use Amazon CloudWatch metrics (invocation count, latency, throttles, token counts) to monitor utilization and adjust capacity as needed.
Explanation
Correct. Provisioned throughput is designed to provide predictable, reserved model capacity for higher sustained throughput during known peaks. Sizing based on expected RPM/TPM is a capacity-planning approach, and CloudWatch utilization monitoring helps validate the provisioned level and tune it over time. This approach keeps the same Bedrock integration pattern and remains within a single Region.
Enable Amazon Bedrock cross-Region inference and configure the application to use an inference profile so Bedrock can route requests to the least busy Region. Keep the existing on-demand invocation pattern and rely on SDK retries with exponential backoff.
Explanation
Incorrect. This violates the requirement that inference must remain in a single Region. While exponential backoff helps resiliency, it does not provide dedicated capacity and can still result in unacceptable throttling and latency during predictable peaks.
Overall explanation
For predictable, recurring traffic spikes, the most direct way to increase model throughput while keeping a simple architecture is to reserve capacity for the exact Bedrock model being used. Provisioned throughput provides dedicated throughput for the model, and capacity planning based on expected request volume and token consumption helps select an appropriate provisioned level. CloudWatch monitoring then validates whether the provisioned capacity is sufficient (for example, by watching throttles, latency, and token counts) and informs iterative tuning. Asynchronous batch processing changes the interaction pattern, cross-Region inference conflicts with single-Region constraints, and moving to a hosted endpoint adds operational complexity that is avoidable for this use case. Further reading (AWS): - Increase model invocation capacity with Provisioned Throughput in Amazon Bedrock (AWS Documentation) - Use a Provisioned Throughput with an Amazon Bedrock resource (AWS Documentation) - Model Invocations - Amazon CloudWatch (AWS Documentation)
Domain
Content Domain 4: Operational Efficiency and Optimization fo
Question 51
Incorrect
A fintech team is deploying an open-source LLM behind an Amazon SageMaker AI real-time inference endpoint by using a custom container image in Amazon ECR. The model artifacts in Amazon S3 are very large, and the container must download and load the weights into GPU memory during startup. During deployment, the endpoint repeatedly fails with container health check errors. Logs show the model is still downloading and initializing when the health check fails. Which change will allow the team to deploy the LLM successfully while keeping the same real-time endpoint architecture with the LEAST operational overhead?
Your answer is incorrect
Replace the real-time endpoint with a SageMaker Asynchronous Inference endpoint and have the application poll Amazon S3 for results.
Explanation
Incorrect. Asynchronous inference is designed for workloads that can tolerate longer end-to-end latency and typically returns results via an output location such as Amazon S3. This changes the interaction pattern and does not meet the requirement to keep a real-time endpoint architecture.
Correct answer
Update the endpoint configuration to increase the container startup health check timeout and the model artifact download timeout so the container has enough time to download and load the model before being declared unhealthy.
Explanation
Correct. Large language models often have very large weight files and longer initialization times than traditional ML models, especially when downloading artifacts from Amazon S3 and loading them into GPU memory. Increasing the container health check timeout and download timeout accommodates these LLM-specific startup characteristics without changing the architecture or adding additional services to operate.
Increase the size of the GPU instance type for the endpoint so the container can load the model faster during startup.
Explanation
Incorrect. A larger instance can help with memory pressure, but it does not directly address the root cause shown in the logs: the container fails health checks while it is still downloading and initializing the model. This option increases cost and may still fail if the health check and download timeout settings are not adjusted for large model startup times.
Move the model inference into an AWS Lambda function that downloads the model from Amazon S3 on each invocation and runs inference in the function runtime.
Explanation
Incorrect. Lambda is not suitable for hosting large LLM artifacts that must be loaded into GPU memory, and downloading large model weights on every invocation would create excessive latency and operational risk. It also does not align with the container-based LLM deployment approach used for SageMaker AI endpoints.
Overall explanation
Container-based LLM deployments frequently fail for reasons that are uncommon in traditional ML endpoints: large artifacts take longer to download, model initialization can be slower due to GPU memory setup, and the container may not pass health checks within default time limits. The most direct, low-overhead fix is to adjust the endpoint’s startup health check and model download timeout settings so the container can complete model loading before SageMaker evaluates it as unhealthy. Alternatives either change the required real-time interaction model, add significant cost without guaranteeing success, or use a compute environment that is not appropriate for large LLM inference. Further reading (AWS): - SageMaker AI endpoint parameters for large model inference - Amazon SageMaker AI (AWS Documentation) - ProductionVariant - Amazon SageMaker (AWS Documentation) - Custom Inference Code with Hosting Services - Amazon SageMaker AI (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 52
Correct
A retail platform is building a GenAI feature that helps sellers create product listing text by using Amazon Bedrock. Most requests are routine (for example, short rewrites and basic summaries), but a smaller percentage require more advanced reasoning and higher-quality copy. The team wants to reduce ongoing inference costs while still producing high-quality output for complex requests, and the solution must avoid managing GPU infrastructure. Which deployment approach is the MOST cost-effective way to meet these requirements?
Your answer is correct
Implement API-based model cascading in Amazon Bedrock: route routine listing requests to a smaller pre-trained model and automatically escalate complex requests to a larger model (for example, by using intelligent prompt routing or application-side routing logic).
Explanation
Correct. Model cascading balances performance and resource requirements by using lower-cost, smaller models for routine requests and reserving larger models for complex prompts that need higher quality. Because inference remains in Amazon Bedrock, the team avoids managing GPU infrastructure while still optimizing ongoing cost.
Fine-tune a large Amazon Bedrock custom model so it can handle both routine and complex listing requests with shorter prompts, then use that single custom model for all invocations.
Explanation
Incorrect. Fine-tuning can improve task alignment, but it can be expensive and does not provide the same cost control as routing routine requests to a smaller pre-trained model. It also does not implement a cascading strategy to reserve higher-capability inference for the minority of complex requests.
Deploy an open-source LLM on a GPU-backed Amazon SageMaker AI real-time endpoint and use the endpoint for all listing-generation requests to control performance.
Explanation
Incorrect. This introduces GPU infrastructure management and operational overhead (endpoint sizing, scaling, model server operations). It also does not reduce costs for routine prompts because every request still uses the same heavyweight deployment.
Use a single large text model in Amazon Bedrock for all requests. Purchase provisioned throughput to ensure consistent performance during peak traffic.
Explanation
Incorrect. Provisioned throughput can improve consistent capacity, but using a large model for every request does not balance resource usage with request complexity and will keep inference costs high for routine queries.
Overall explanation
A cost-optimized GenAI deployment often comes from matching model capability to request complexity. Routine tasks (rewrites, short summaries) typically do not require the most capable and expensive model. A cascading approach invokes a smaller, cheaper pre-trained model for the common case and escalates only when the request is complex or when additional capability is needed. Keeping the workflow within Amazon Bedrock preserves a managed, serverless inference experience and avoids the operational burden of hosting and scaling GPU-backed endpoints. Strategies that use a single large model for everything (even with provisioned throughput) or that require custom hosting do not provide the same cost-to-quality balance for mixed workloads. Further reading (AWS): - Understanding intelligent prompt routing in Amazon Bedrock (AWS Documentation) - Use Amazon Bedrock Intelligent Prompt Routing for cost and latency benefits (AWS Blog) - Amazon Bedrock pricing (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 53
Correct
A fintech support engineering team is building a customer chat assistant by using an FM in Amazon Bedrock. The assistant must consistently behave like a regulated customer support agent across multiple applications, must return responses in a fixed JSON structure that downstream systems can parse, and must prevent the FM from returning personally identifiable information (PII) or discussing restricted topics. Which solution will meet these requirements with the LEAST operational overhead?
Store prompt text templates in an Amazon S3 bucket and have each application load and assemble prompts at runtime. Use AWS Lambda to call Amazon Comprehend for PII detection and redaction before and after each model invocation.
Explanation
Incorrect. Although this can work, it increases operational overhead by requiring custom code to fetch, assemble, and version prompts, plus additional Lambda-based pre/post-processing logic and scaling considerations. It also duplicates governance responsibilities across multiple applications instead of using managed prompt governance and guardrails.
Configure Amazon API Gateway request validation with a JSON Schema model and reject any Bedrock request that does not include the required output fields. Rely on the FM to comply with the schema and avoid restricted topics.
Explanation
Incorrect. API Gateway request validation applies to incoming API requests to the application, not to enforcing an FM’s generated output structure or content safety. This approach does not provide a control mechanism for restricting unsafe model outputs such as PII or restricted topics.
Your answer is correct
Create a reusable, parameterized prompt template in Amazon Bedrock Prompt Management that defines the agent role and includes explicit JSON output instructions (schema and example). Attach Amazon Bedrock Guardrails with topic filtering and PII masking, and invoke the managed prompt from all applications.
Explanation
Correct. Bedrock Prompt Management centralizes and versions the instruction framework (including consistent role definitions and response formatting expectations) so multiple applications can use the same governed template. Bedrock Guardrails add managed safety controls, including topic filtering and PII masking/removal, reducing the need for custom moderation code. Together, they control both FM behavior and output with minimal custom infrastructure.
Fine-tune a Bedrock custom model on company support transcripts that always follow the JSON format and never includes PII. Deploy the fine-tuned model and remove all prompt instructions to reduce token usage.
Explanation
Incorrect. Model customization is significantly higher operational effort and cost than using managed prompts and guardrails, and it does not provide strong, configurable runtime governance controls for restricted topics and PII handling. Additionally, removing prompt instructions reduces controllability and makes behavior harder to govern across applications.
Overall explanation
The best approach is to centralize the instruction framework and governance so every application gets consistent behavior without duplicating logic. Amazon Bedrock Prompt Management provides a reusable prompt template to enforce a standard role and a consistent response format (such as a required JSON structure). Amazon Bedrock Guardrails add managed safety controls—such as topic filtering and PII masking/removal—so the system can block or sanitize unsafe outputs without building and operating custom moderation pipelines. Alternatives either rely on unmanaged prompt files and custom code, try to enforce constraints in the wrong layer (API request validation), or introduce unnecessary operational complexity through model customization. Further reading (AWS): - Construct and store reusable prompts with Prompt management in Amazon Bedrock (AWS Documentation) - Detect and filter harmful content by using Amazon Bedrock Guardrails (AWS Documentation) - Remove PII from conversations by using sensitive information filters (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 54
Correct
A product enablement team is building an internal web portal that helps employees draft customer emails by using an Amazon Bedrock foundation model (FM). The team wants to deliver an accessible web UI quickly, standardize backend integration for future clients by using an API-first approach, and allow non-developers to adjust the prompt workflow (including branching and reusable prompt components) without redeploying application code. Which solution will meet these requirements with the LEAST operational overhead?
Deploy a containerized web application on Amazon ECS behind an Application Load Balancer. Use AWS Step Functions to orchestrate a multi-step prompt chain that invokes the FM and stores intermediate results in Amazon DynamoDB.
Explanation
Incorrect. This introduces additional infrastructure to manage (ECS cluster/services and load balancing) and uses Step Functions for orchestration rather than a purpose-built no-code prompt workflow capability. While Step Functions can orchestrate calls, updating the prompt workflow would typically require developer-driven changes to state machine definitions and associated code/configuration, increasing operational overhead compared to using Amazon Bedrock Flows for prompt chaining.
Host a static single-page application in Amazon S3 behind Amazon CloudFront, and call the Amazon Bedrock runtime APIs directly from the browser by using Amazon Cognito identity pools. Hardcode the full prompt sequence in the client so business users can edit prompts in the web page source.
Explanation
Incorrect. This approach pushes prompt logic to the client, which increases governance and maintenance risk (for example, prompt leakage and inconsistent prompt versions across users). It also does not provide a no-code workflow builder for chaining prompts and branching logic. Although the UI could be delivered quickly, ongoing prompt management and workflow updates would be operationally burdensome and error-prone.
Deploy an Amazon Q Business application and connect it to internal data sources. Configure Amazon Q Business admin controls to shape responses and use it as the primary employee-facing interface for email drafting.
Explanation
Incorrect. Amazon Q Business is a managed assistant with its own UI and governance controls, but it does not satisfy the requirement for an API-first interface based on an OpenAPI specification that can be reused by multiple client applications. It also does not provide the same prompt workflow construction model (branching prompt chains as a custom application component) described in the requirements.
Your answer is correct
Build the web UI with AWS Amplify and use Amplify libraries for authentication. Define the backend as an Amazon API Gateway REST API created from an OpenAPI specification. Invoke an Amazon Bedrock Flow from an AWS Lambda integration behind the API so prompt chains and conditions can be updated in the Flow without application redeployments.
Explanation
Correct. AWS Amplify accelerates delivery of a production-ready, accessible web interface and commonly used front-end capabilities (such as authentication). An OpenAPI-driven Amazon API Gateway REST API provides an API-first contract that can be shared across teams and used to generate/validate clients consistently. Amazon Bedrock Flows provide a managed, no-code/low-code way to chain prompts and models with conditional connections, so non-developers can update the workflow without changing and redeploying the UI or API code. Lambda provides a simple integration layer to invoke Bedrock and apply any necessary request/response handling.
Overall explanation
The lowest-overhead solution combines a rapid front-end delivery mechanism with a standardized API contract and a managed, no-code workflow layer for prompt chaining. AWS Amplify accelerates building and hosting the web interface and common client capabilities. An OpenAPI-defined Amazon API Gateway interface provides an API-first contract that other clients can adopt consistently. Amazon Bedrock Flows allow teams to create and change multi-step prompt workflows (including conditional branching and reusable prompt components) without continuous code deployments, reducing ongoing operational effort compared to hardcoding prompts in the client or building custom orchestration and infrastructure. Further reading (AWS): - Create and design a flow in Amazon Bedrock (AWS Documentation) - Develop REST APIs using OpenAPI in API Gateway - Amazon API Gateway (AWS Documentation) - Deploy a Next.js app to Amplify Hosting - AWS Amplify Hosting (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 55
Correct
A fintech customer support engineering team is building an internal GenAI assistant to help agents answer questions about the latest policies and procedures. The documents are stored in Amazon S3 and an internal Atlassian Confluence wiki, and the content changes frequently. The team wants the assistant’s answers to be grounded in the approved documents to reduce hallucinations and to avoid having to retrain a model whenever content is updated. Which architecture will meet these requirements with the LEAST operational overhead?
Store the policy documents and their embedding vectors as items in Amazon DynamoDB. For each user query, use AWS Lambda to scan the table, calculate cosine similarity in code, select the top matches, and send those matches to an Amazon Bedrock model.
Explanation
Incorrect. DynamoDB is not designed for semantic vector similarity search; scanning and scoring items in Lambda does not scale and creates significant custom operational burden. This design increases latency, cost, and complexity compared to using a managed knowledge base and vector retrieval solution.
Use AWS Lambda to fetch the full text of the relevant policy documents from Amazon S3 and Confluence and append the documents to each user prompt before calling an Amazon Bedrock text model.
Explanation
Incorrect. This approach requires custom retrieval and prompt-building logic and is likely to hit context window limits and increase token costs. It also does not provide a managed retrieval layer that is optimized for semantic relevance, which can reduce answer quality and increase troubleshooting effort.
Your answer is correct
Create an Amazon Bedrock Knowledge Base that connects to Amazon S3 and Confluence as data sources. Configure an embedding model (such as Amazon Titan embeddings) and a managed vector store. Use the bedrock-agent-runtime RetrieveAndGenerate capability to produce responses and apply Amazon Bedrock Guardrails with contextual grounding checks.
Explanation
Correct. A Bedrock Knowledge Base provides a managed RAG architecture that ingests documents from supported data sources (including S3 and Confluence), chunks content, generates embeddings, and performs semantic retrieval to augment prompts. RetrieveAndGenerate uses the retrieved context to ground responses, and Guardrails with contextual grounding checks help further reduce hallucinations. Updates can be handled by synchronizing the knowledge base rather than retraining an FM, minimizing operational overhead.
Create an Amazon Bedrock custom model by fine-tuning a text FM with prompt-completion training pairs stored in Amazon S3. Invoke the fine-tuned model by using the Amazon Bedrock Runtime API for all agent questions.
Explanation
Incorrect. Fine-tuning (custom models) requires creating and managing training datasets and running customization jobs. It also does not align well with frequently changing documents because the model would require additional customization jobs to incorporate updates, which increases operational overhead and delays content freshness.
Overall explanation
A managed RAG architecture is the best match when information changes frequently and the goal is to keep responses grounded in approved sources without retraining. Amazon Bedrock Knowledge Bases provide built-in ingestion from supported repositories, embedding generation, chunking, and semantic retrieval, and they integrate directly with a generation step (RetrieveAndGenerate). Adding Bedrock Guardrails with contextual grounding checks further enforces that responses stay aligned with retrieved context, reducing hallucinations while keeping the architecture simple to operate. In contrast, fine-tuning introduces a model customization lifecycle that must be repeated to keep up with document changes, and building custom retrieval/prompt stuffing or DIY similarity search increases complexity and maintenance effort. Further reading (AWS): - Turning data into a knowledge base - Amazon Bedrock (AWS Documentation) - Query a knowledge base and generate responses based off the retrieved data - Amazon Bedrock (AWS Documentation) - Use contextual grounding check to filter hallucinations in responses - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 56
Correct
A compliance engineering team at a fintech is building a Retrieval Augmented Generation (RAG) assistant by using Amazon Bedrock Knowledge Bases. The team ingests long HR and benefits policy PDFs. Users often ask narrow questions such as eligibility exceptions and edge cases that appear in short paragraphs. With the current ingestion settings, the retrieval step frequently returns broad passages that bury the relevant clause, and the model sometimes answers without enough surrounding context to justify the result. The team wants to improve retrieval precision while still providing sufficient context for grounded answers with the LEAST ingestion-time operational overhead and cost. Which document segmentation approach should the team use?
Implement fixed-size chunking in a Lambda preprocessor with very large chunks and high overlap to ensure relevant clauses always appear with nearby context.
Explanation
Incorrect. Large fixed chunks and high overlap increase duplicated content across chunks, which inflates storage and embedding generation work and can reduce retrieval precision by returning overly broad passages. This also introduces extra custom processing to build, deploy, and maintain.
Configure the knowledge base to use semantic chunking so a foundation model determines chunk boundaries based on meaning rather than token counts.
Explanation
Incorrect. Semantic chunking can improve chunk boundaries, but it incurs additional cost because an FM is invoked during ingestion to create semantically derived chunks. This conflicts with the requirement to minimize ingestion-time cost and operational overhead.
Disable chunking so that each PDF is treated as a single chunk during ingestion, relying on the FM’s large context window to handle long documents.
Explanation
Incorrect. Treating an entire document as one chunk typically reduces retrieval precision because a single embedding must represent many topics and sections. It can also increase the amount of irrelevant context passed to the FM, which worsens grounding and increases token usage.
Your answer is correct
Configure the knowledge base to use hierarchical chunking so the retriever indexes smaller child chunks for precise matching and then returns the corresponding larger parent chunks to provide additional context.
Explanation
Correct. Hierarchical chunking is designed to balance precision and context for RAG. Smaller child chunks improve semantic match quality for pinpoint clauses, and returning parent chunks provides the broader surrounding policy text needed for grounded, defensible answers. This is a managed capability of Bedrock Knowledge Bases and avoids building and operating a custom chunking pipeline.
Overall explanation
Hierarchical chunking is purpose-built for RAG document segmentation when answers depend on small, specific passages but still require surrounding context. By indexing smaller child chunks, the retrieval step can match user questions to the most relevant fine-grained content. By returning the associated parent chunk, the system supplies additional surrounding text for grounding and justification. Alternatives either degrade retrieval precision by embedding entire documents, add ingestion-time FM cost through semantic chunking, or introduce extra custom processing and duplication through large fixed-size chunks with overlap. Further reading (AWS): - How content chunking works for knowledge bases - Amazon Bedrock (AWS Documentation) - Amazon Bedrock Knowledge Bases now supports advanced parsing, chunking, and query reformulation giving greater control of accuracy in RAG based applications (AWS Blog) - Use a custom transformation Lambda function to define how your data is ingested - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 57
Correct
A financial services firm is building an internal policy assistant that answers employee questions by using an Amazon Bedrock text model. The source policy documents are stored in Amazon S3. During testing, users report that the assistant sometimes invents policy details that are not present in the source documents. The firm needs a solution that grounds responses in the policy corpus, produces a machine-readable confidence signal that the application can use to return “insufficient evidence” when answers are not supported, and returns results in a consistent JSON structure for downstream processing. The firm wants the solution with the LEAST operational overhead. Which approach meets these requirements?
Your answer is correct
Create an Amazon Bedrock Knowledge Base over the policy documents in Amazon S3. Use a retrieve-and-generate pattern so the FM answers only with retrieved context. Enable Amazon Bedrock Guardrails contextual grounding checks to score grounding and relevance, and enforce a JSON response structure (for example, with a response format template) that includes fields such as answer, citations, and confidence.
Explanation
Correct. A Knowledge Base provides managed retrieval to ground responses in the firm’s policy documents, which reduces hallucinations by supplying authoritative context at generation time. Guardrails contextual grounding checks provide an automated way to assess whether the response is supported by the retrieved context and whether it is relevant to the query, enabling the application to return “insufficient evidence” when grounding is low. Enforcing a JSON structure ensures consistent downstream parsing and validation.
Create a Bedrock custom model by fine-tuning a supported FM on the policy documents stored in Amazon S3. Set temperature to 0 for inference and store the final prompt template in Amazon Bedrock Prompt Management.
Explanation
Incorrect. Fine-tuning can help align style and domain language, but it does not provide retrieval-based grounding against the current document corpus at inference time. It also does not inherently produce an evidence-based confidence signal that the application can use to detect unsupported answers, and it adds operational and cost overhead compared to managed retrieval-based grounding.
Use an AWS Lambda function to generate Amazon Titan embeddings for each policy document and store the vectors in Amazon DynamoDB. For each query, compute the query embedding and use a DynamoDB Query operation to retrieve the nearest vectors for prompt augmentation.
Explanation
Incorrect. Amazon DynamoDB can store vectors as attributes, but it does not provide native vector similarity search to retrieve nearest neighbors. This approach would require additional services or custom similarity infrastructure to perform semantic search, increasing operational overhead and failing the stated design as written.
Use Amazon Comprehend to extract entities from the user question and from the model’s draft response. Reject the response when it contains entities that are not present in a pre-approved entity dictionary derived from the policy documents.
Explanation
Incorrect. Entity allow-listing can reduce certain classes of errors, but it does not verify factual correctness or ensure that claims are grounded in the source documents. It also does not provide an evidence-based confidence measure and can lead to false positives/negatives (for example, policies might mention valid entities not present in the dictionary).
Overall explanation
To reduce hallucinations, the most reliable pattern is to ground the FM on authoritative content at inference time and to verify that the response aligns with that content. Using an Amazon Bedrock Knowledge Base implements managed retrieval over the policy corpus so the model can answer using retrieved context instead of guessing. Adding Amazon Bedrock Guardrails contextual grounding checks provides an automated grounding/relevance signal that the application can interpret as a confidence indicator and use to return an “insufficient evidence” response when needed. Finally, enforcing a consistent JSON output format enables deterministic downstream processing and validation, which improves reliability for production workflows. Options that rely on fine-tuning, entity filtering, or unsupported vector search patterns do not provide the same end-to-end grounding and verification with minimal operational overhead. Further reading (AWS): - Retrieve data and generate AI responses with Amazon Bedrock Knowledge Bases (AWS Documentation) - RetrieveAndGenerate - Amazon Bedrock (AWS Documentation) - Use contextual grounding check to filter hallucinations in responses - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 58
Correct
A healthcare provider is building an internal “policy assistant” that answers employee questions by using Amazon Bedrock Agents with an Amazon Bedrock Knowledge Base that is populated from documents in Amazon S3. The compliance team requires the application to be transparent by showing users which policy sources were used for each answer and by providing auditors with a trace of how the agent reached the response (including knowledge base retrievals and action group calls). The operations team also wants a per-response confidence signal recorded in Amazon CloudWatch so they can investigate low-confidence answers. Which solution will meet these requirements with the LEAST operational overhead?
Store the full prompt, retrieved context chunks, and action group parameters for each request in Amazon DynamoDB. Return the stored prompt and context to end users so they can verify the model’s reasoning and use the stored context size as a confidence metric in CloudWatch.
Explanation
Incorrect. Returning full prompts and retrieved context can leak sensitive internal instructions and data and is not an appropriate transparency mechanism. Context size is not a reliable confidence signal, and persisting full prompts and context for every request increases data handling risk and operational burden.
Your answer is correct
Enable Amazon Bedrock agent tracing for the agent and log the returned trace payloads. Configure the application to include source attribution in the response by presenting the retrieved document metadata (for example, document title and S3 URI) as evidence. Publish a per-response confidence score to Amazon CloudWatch by emitting a custom metric derived from an automated grounding or relevance score collected during the request processing.
Explanation
Correct. Amazon Bedrock agent tracing provides detailed reasoning traces that include how the agent orchestrated knowledge base retrievals and action group invocations. Presenting retrieved document metadata gives user-facing source attribution without exposing system prompts or full internal context. CloudWatch can store operational confidence signals as custom metrics so teams can dashboard and alert on low-confidence responses with minimal additional infrastructure.
Orchestrate all agent steps with AWS Step Functions and implement each knowledge base lookup and action group call as separate AWS Lambda states. Display the Step Functions execution history to users as a reasoning display and use state transition success rates as the confidence signal in CloudWatch.
Explanation
Incorrect. Step Functions can provide deterministic workflow visibility, but it does not capture the agent’s reasoning trace (for example, how it interpreted the question or decided which tool to call). Showing state transitions also does not provide source attribution for the generated answer, and using workflow success rates is not a meaningful per-response confidence signal.
Enable AWS CloudTrail logging for Amazon Bedrock API calls and export the CloudTrail logs to Amazon S3. Use Amazon Athena queries over the CloudTrail logs to reconstruct which tools the agent called and to compute a confidence score based on request frequency and error rates.
Explanation
Incorrect. CloudTrail is an audit log of API activity and is not designed to provide model reasoning traces or evidence attribution for a specific answer. Reconstructing tool usage and “confidence” from CloudTrail events is indirect, brittle, and does not provide the agent’s orchestration-level trace required by auditors.
Overall explanation
The lowest-overhead way to build transparency into an agent-based GenAI application is to use native agent tracing to obtain an auditable reasoning trace, while presenting evidence by surfacing the identifiers of the retrieved policy sources that informed the answer. Operational confidence should be captured as a metric in CloudWatch so it can be visualized and alarmed on; this is best done by emitting a per-request score produced during processing (for example, a grounding/relevance-style score) rather than trying to infer confidence from infrastructure logs or workflow success rates. Alternatives that rely on audit logs, workflow histories, or returning full prompts/context either fail to provide true reasoning traces and source attribution or introduce unnecessary security and operational risk. Further reading (AWS): - Track agent's step-by-step reasoning process using trace - Amazon Bedrock (AWS Documentation) - Query a knowledge base and generate responses based off the retrieved data - Amazon Bedrock (AWS Documentation) - Publish custom metrics - Amazon CloudWatch (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 59
Incorrect
A product support team has built a public FAQ assistant that uses Amazon API Gateway and AWS Lambda to invoke an Amazon Bedrock text model. The assistant does not use user-specific context, and the team has configured the model with deterministic settings so the same question produces the same answer. Metrics show that a large percentage of requests are repeated verbatim across users, and the team wants to reduce Amazon Bedrock invocation costs and improve global response latency. Which solution will meet these requirements MOST cost-effectively?
Enable Amazon Bedrock prompt caching for the system prompt and few-shot examples. Keep the user question as the suffix so the prefix is reused across invocations.
Explanation
Incorrect. Prompt caching can reduce token processing cost and improve latency by reusing a cached prompt prefix, but it still invokes the FM for every request. It does not avoid unnecessary FM invocations when users ask the exact same question repeatedly.
Purchase Amazon Bedrock provisioned throughput for the model and increase the reserved concurrency of the Lambda function to improve performance during peak traffic.
Explanation
Incorrect. Provisioned throughput and increased Lambda concurrency can help handle higher traffic, but they do not reduce the number of FM invocations. This approach increases cost and does not address the core requirement of avoiding unnecessary invocations for repeated identical requests.
Your answer is incorrect
Implement semantic caching by storing embeddings of prompts and corresponding responses in Amazon MemoryDB. For each new prompt, generate an embedding and perform nearest-neighbor lookup. If the similarity score exceeds a threshold, return the cached response instead of invoking the FM.
Explanation
Incorrect. Although semantic caching can reduce FM invocations for semantically similar prompts, it requires an embedding-generation step for each request and careful threshold tuning to prevent incorrect cache hits. This introduces more operational complexity than edge caching for verbatim repeated questions, and it may add latency and cost overhead compared to a simple deterministic cache key.
Correct answer
Place Amazon CloudFront in front of the API. Create a deterministic request fingerprint (hash) from the normalized prompt and model configuration. Send requests as GET /ask?fingerprint=<hash> so CloudFront can cache and serve identical responses from the edge, invoking Bedrock only on a cache miss.
Explanation
Correct. Edge caching with CloudFront can avoid unnecessary FM invocations entirely for repeated requests by serving cached responses at edge locations. Using a deterministic request hash (fingerprint) ensures that only truly identical requests map to the same cache key, and including model configuration in the fingerprint prevents incorrect reuse when settings change. This directly reduces Bedrock invocation volume and improves latency for global users.
Overall explanation
Because many requests are exact repeats and the responses are deterministic, the best optimization is to avoid invoking the FM when a previous identical response already exists. Edge caching with CloudFront accomplishes this by serving cached responses from edge locations, improving latency for global users while reducing total Bedrock invocations. A deterministic request fingerprint provides a stable cache key and helps ensure cache correctness when prompts or model parameters change. Prompt caching improves token efficiency but still calls the model each time, and semantic caching adds embedding overhead and tuning complexity that is unnecessary when repetition is primarily verbatim. Provisioned throughput improves capacity but does not reduce invocation volume. Further reading (AWS): - Use various origins with CloudFront distributions (AWS Documentation) - Cache content based on query string parameters - Amazon CloudFront (AWS Documentation) - Prompt caching for faster model inference - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 4: Operational Efficiency and Optimization fo
Question 60
Correct
A healthcare technology team is building a patient-facing virtual assistant that uses a text foundation model (FM) on Amazon Bedrock to answer questions about appointment scheduling. The assistant is exposed through a public API and has recently received user messages containing profanity, harassment, and attempts to override the assistant’s instructions (prompt injection). The team also must ensure that personally identifiable information (PII) such as phone numbers and social security numbers in user messages is masked before the FM processes the request. Which solution will protect against harmful user inputs in real time with the LEAST operational overhead?
Store each user message in Amazon S3 and run an Amazon Macie discovery job to detect PII. After the discovery job completes, invoke the FM on Amazon Bedrock by using the sanitized text.
Explanation
Incorrect. Macie is designed for discovering sensitive data in S3 using scheduled or batch analysis, not for real-time request filtering on a live chat experience. This design introduces significant latency and operational complexity and does not provide direct, real-time moderation of harmful prompt content.
Create an AWS Step Functions workflow that sends each prompt to a human review queue for approval before invoking the Amazon Bedrock model. Reject prompts that contain harmful content or prompt injection attempts.
Explanation
Incorrect. Human review adds substantial latency and operational overhead and does not meet a real-time conversational requirement for a patient-facing assistant. Step Functions is useful for custom moderation workflows, but requiring human approval for every prompt is not an efficient baseline control for high-volume real-time interactions.
Use AWS WAF in front of Amazon API Gateway to block requests that contain profanity and common prompt-injection strings. Pass all remaining requests directly to Amazon Bedrock without additional safety controls.
Explanation
Incorrect. AWS WAF can help block some requests at the web layer, but it is not a comprehensive FM input safety system and is not designed to provide GenAI-specific controls like integrated PII masking for model inputs. Relying only on WAF also leaves gaps because prompt-injection and harmful content can be subtle and can bypass simple string matching.
Your answer is correct
Create an Amazon Bedrock guardrail that uses topic and word filtering (including profanity) and input PII masking. Configure the application’s Bedrock model invocation to apply the guardrail and return a standard blocked message when the guardrail intervenes.
Explanation
Correct. Amazon Bedrock Guardrails provides managed, real-time filtering for prompts (inputs) and can mask PII directly as part of the model invocation. Topic and word filters can block harmful categories (such as harassment and profanity) and common prompt-injection patterns. Because the guardrail is applied by Bedrock during inference, this approach minimizes custom code and operational burden while enforcing consistent policy behavior.
Overall explanation
The most effective low-ops approach is to enforce safety controls at the FM boundary by using Amazon Bedrock Guardrails. Guardrails can filter harmful user prompts with topic and word/profanity filters and can mask PII as part of the same real-time inference call, ensuring consistent enforcement without building and operating a separate moderation pipeline. Batch-oriented sensitive-data discovery and human-in-the-loop review introduce unacceptable latency for real-time chat, and generic web filtering alone does not provide comprehensive GenAI-focused protections such as integrated PII masking for model inputs. Further reading (AWS): - Detect and filter harmful content by using Amazon Bedrock Guardrails (AWS Documentation) - Remove PII from conversations by using sensitive information filters - Amazon Bedrock (AWS Documentation) - Guardrails for Amazon Bedrock now available with new safety filters and privacy controls (AWS Blog)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 61
Correct
A media company is building an internal GenAI assistant on Amazon Bedrock to help employees summarize meeting notes and answer policy questions. The application currently sends every user request to a high-capability (and higher-cost) model. Usage has grown, and most requests are simple summaries, while a smaller portion requires deeper reasoning and higher response quality. The team wants to reduce ongoing inference costs while preserving high-quality answers for complex requests, with minimal custom routing logic to maintain. Which solution will meet these requirements MOST cost-effectively?
Purchase provisioned throughput for the higher-capability model so the application can handle growth more efficiently and reduce cost per request.
Explanation
Incorrect. Provisioned throughput is primarily a capacity and performance control mechanism; it does not implement query-based model selection. It can also increase costs if provisioned capacity is underutilized, and it does not address the goal of using cheaper models for simple requests.
Your answer is correct
Configure Amazon Bedrock Intelligent Prompt Routing to route requests between a lower-cost model for simple requests and a higher-capability model for complex requests based on prompt complexity, and monitor token usage in Amazon CloudWatch.
Explanation
Correct. A tiered model selection framework that routes simple requests to cheaper models and reserves more expensive models for complex requests is a direct way to reduce overall inference spend while maintaining quality where it matters. Using a managed routing feature minimizes custom logic and operational burden compared to building and maintaining your own classifier and routing service. CloudWatch token metrics help validate savings and support ongoing price-to-performance tuning.
Implement an AWS Lambda router that invokes the lower-cost model first for every request and then invokes the higher-capability model only if the first response appears incomplete. Store responses in Amazon DynamoDB for future reuse.
Explanation
Incorrect. Calling a lower-cost model first for every request often increases total cost and latency because many requests will incur two model invocations. Additionally, determining whether a response is “incomplete” requires extra evaluation logic and ongoing tuning, which increases operational overhead. While DynamoDB can store conversation history or cached results, it does not solve cost-effective model selection by query complexity on its own.
Enable Amazon Bedrock prompt caching for the application’s system prompt and few-shot examples and continue using the same higher-capability model for all requests.
Explanation
Incorrect. Prompt caching can reduce cost and latency related to repeated prompt prefixes, but it does not implement tiered model usage. The higher-capability model is still invoked for all requests, so the primary cost driver (using an expensive model for simple work) remains.
Overall explanation
The most cost-effective approach is to implement a tiered model selection strategy so that routine requests (like basic summaries) use a cheaper model while complex requests use a higher-capability model. This directly improves the price-to-performance ratio by matching model cost to task complexity. A managed routing capability minimizes the amount of custom logic the team must build and operate, while CloudWatch token metrics provide the telemetry needed to validate savings and iteratively tune the routing and model choices over time. Other approaches either continue to rely on the expensive model for all requests or risk increasing costs by adding extra model invocations and custom decision logic. Further reading (AWS): - Understanding intelligent prompt routing in Amazon Bedrock - Amazon Bedrock (AWS Documentation) - Use Amazon Bedrock Intelligent Prompt Routing for cost and latency benefits | Artificial Intelligence (AWS Blog) - How tokens are counted in Amazon Bedrock - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 4: Operational Efficiency and Optimization fo
Question 62
Correct
A financial services firm wants to allow its internal developers to build proof-of-concept applications that call Amazon Bedrock directly from local Python scripts. The firm uses Okta for workforce identity and requires single sign-on with short-lived credentials (no long-term access keys). Security requirements state that developers must be able to perform inference against approved FMs but must not be able to manage models, agents, or knowledge bases. Which solution meets these requirements with the MOST secure, least-privilege access model?
Create one shared IAM role that has permissions to invoke Amazon Bedrock models. Distribute the role credentials to all developers and rotate the credentials quarterly.
Explanation
Incorrect. Sharing credentials prevents per-user accountability and violates the requirement to avoid long-term credentials. It also increases security risk because access is not tied to federated user identity and is difficult to govern and audit at the individual level.
Configure an Amazon Cognito user pool federated with Okta. Have developers authenticate to Cognito and pass the Cognito JWT token directly to the Amazon Bedrock Runtime API for authorization.
Explanation
Incorrect. Amazon Bedrock API requests require AWS SigV4 authentication with AWS credentials and IAM authorization, not a Cognito JWT presented directly to the Bedrock API. This approach does not provide a valid authorization mechanism for invoking Bedrock.
Create an IAM user for each developer and generate access keys. Store the access keys in AWS Secrets Manager and attach the AmazonBedrockFullAccess managed policy to each user for simplicity.
Explanation
Incorrect. This uses long-term access keys (explicitly disallowed) and grants overly broad permissions. AmazonBedrockFullAccess allows more than inference-only access and does not enforce least privilege.
Your answer is correct
Configure IAM Identity Center with Okta as the identity provider. Create a permission set for the developer group with a custom IAM policy that allows only Amazon Bedrock Runtime actions (for example, InvokeModel and Converse) for approved models. Have developers use IAM Identity Center authentication to obtain temporary credentials for AWS SDK calls.
Explanation
Correct. IAM Identity Center provides identity federation with the enterprise IdP and issues short-lived credentials (no long-term access keys). A custom, least-privilege policy can allow only inference actions through the Bedrock Runtime endpoint while excluding management capabilities such as model, agent, or knowledge base management.
Overall explanation
The most secure approach is to federate workforce identities from the enterprise identity provider into AWS and issue temporary credentials, then apply role-based access control with least-privilege IAM policies. Using IAM Identity Center with Okta satisfies the federation and short-lived credential requirements, while a custom permission set policy can restrict access to only Bedrock Runtime inference operations (such as InvokeModel or Converse) for approved models. Approaches that rely on long-term access keys, shared credentials, or unsupported token types either violate the security requirements or cannot authorize requests to Amazon Bedrock. Further reading (AWS): - Configure SAML and SCIM with Okta and IAM Identity Center (AWS Documentation) - IAM Identity Center credential provider - AWS SDKs and Tools (AWS Documentation) - Identity-based policy examples for Amazon Bedrock (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 63
Correct
A financial services company runs a customer-support chatbot that calls an Amazon Bedrock text FM through the bedrock-runtime Converse API from an AWS Lambda function in us-east-1. During occasional regional service disruptions and quota spikes, the chatbot experiences timeouts and cannot respond to users. The company must keep the workload running even if the primary Region is impaired, and the company must keep inference within the United States for data residency requirements. Which solution will meet these requirements with the LEAST operational overhead?
Deploy the same FM behind two separate Amazon SageMaker AI real-time endpoints in us-east-1 and us-west-2, and use Amazon Route 53 failover routing to direct traffic to the healthy endpoint during outages.
Explanation
Incorrect. This introduces significant operational overhead (multi-Region endpoint deployment, scaling, and model hosting management). It also changes the deployment approach away from managed Bedrock model invocation, which is unnecessary when the goal is resilient Bedrock inference.
Your answer is correct
Configure Amazon Bedrock Cross-Region Inference by creating a geographic inference profile limited to the United States, and update the application to invoke the inference profile so Bedrock can automatically route requests to an available Region within that geography.
Explanation
Correct. A geographic inference profile enables Bedrock to distribute and fail over inference requests across multiple AWS Regions within the allowed geography (United States). This provides resilience during regional disruptions or regional capacity constraints with minimal additional infrastructure to manage and only a small change to how the model is addressed (invoking the inference profile).
Use Amazon Bedrock provisioned throughput for the FM in us-east-1 to eliminate throttling and timeouts, and keep all chatbot inference in the primary Region.
Explanation
Incorrect. Provisioned throughput can help with steady high-volume capacity in a Region, but it does not provide cross-Region resilience when a Region is impaired. The requirement is continued operation during regional disruptions, which requires multi-Region inference capability.
Orchestrate the Lambda invocation in AWS Step Functions and add a circuit breaker that stops retries after repeated Bedrock failures, returning a generic error message to the user.
Explanation
Incorrect. A circuit breaker can prevent runaway retries and can support graceful degradation, but it does not provide a way to continue successful inference when the primary Region is unavailable. Without cross-Region inference or a fallback model/Region, the chatbot will still be unable to respond during a regional impairment.
Overall explanation
Geographic Cross-Region Inference in Amazon Bedrock is designed to keep applications operating when a specific Region is disrupted or temporarily constrained by routing requests to another Region within an approved geography. Using an inference profile keeps the routing logic managed by Bedrock, which reduces the need to build and operate custom multi-Region failover infrastructure. Alternatives that rely only on capacity provisioning address throttling but not regional outages, and approaches that add orchestration or DNS failover typically increase operational burden and are not required for resilient Bedrock inference. Further reading (AWS): - Geographic cross-Region inference - Amazon Bedrock (AWS Documentation) - Increase throughput with cross-Region inference - Amazon Bedrock (AWS Documentation) - Converse - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 64
Correct
A digital banking team is building a customer-facing chat assistant that uses an Amazon Bedrock text model through AWS Lambda and Amazon API Gateway. Users frequently abandon sessions when they do not see any output quickly, but the team wants to avoid the added cost of provisioning dedicated capacity because traffic is bursty. The team needs the chat UI to start displaying the model’s answer as soon as possible while still allowing the backend team to benchmark latency improvements. Which solution will provide the LOWEST perceived latency for end users with minimal additional cost?
Your answer is correct
Update the application to use Amazon Bedrock streaming responses and stream tokens to the client (for example, by using API Gateway WebSockets or server-sent events). Enable Amazon Bedrock latency-optimized inference for the model and use Amazon CloudWatch metrics to benchmark time to first token and end-to-end latency.
Explanation
Correct. Streaming responses allow the UI to render partial output immediately (minimizing perceived latency), and latency-optimized inference is designed to improve responsiveness metrics such as time to first token. This approach keeps the system on-demand for bursty traffic while still enabling benchmarking through CloudWatch.
Purchase Amazon Bedrock provisioned throughput for the model and invoke the model synchronously from AWS Lambda so the full completion is returned in a single response.
Explanation
Incorrect. Provisioned throughput can improve consistency and throughput, but it adds steady cost that conflicts with bursty traffic and does not specifically minimize perceived latency because users still wait for the full response to be generated before anything is displayed.
Write prompts to Amazon S3 and use Amazon Bedrock batch inference to generate responses asynchronously, then have the client poll for completion and download the final result.
Explanation
Incorrect. Batch inference is intended for offline/asynchronous processing where users can wait. Polling for results increases perceived latency and provides a poorer interactive chat experience than streaming.
Orchestrate parallel model invocations by using AWS Step Functions and combine the outputs into a single final response before returning it to the client application.
Explanation
Incorrect. Parallel model calls increase cost and coordination overhead and still require waiting for all parallel branches to complete before returning a combined response. This does not minimize time to first visible output for the user.
Overall explanation
For interactive chat experiences, the key user-perceived metric is how quickly the application can start showing a response. Streaming responses from Amazon Bedrock let the client render output incrementally instead of waiting for the full completion, which significantly improves perceived responsiveness without requiring dedicated capacity. Enabling latency-optimized inference targets faster responsiveness (such as improved time to first token) for time-sensitive interactions, and CloudWatch metrics provide a way to benchmark improvements. Alternatives that rely on batch processing or multi-step orchestration add delay or cost and are not optimized for immediate user feedback. Further reading (AWS): - InvokeModelWithResponseStream - Amazon Bedrock (AWS Documentation) - Optimize model inference for latency - Amazon Bedrock (AWS Documentation) - Model Invocations - Amazon CloudWatch (AWS Documentation)
Domain
Content Domain 4: Operational Efficiency and Optimization fo
Question 65
Incorrect
A media analytics company is building a RAG assistant on AWS by using Amazon Bedrock for text generation. For the retrieval layer, some workloads use Amazon OpenSearch Service for vector search, while other workloads use Amazon Aurora PostgreSQL with the pgvector extension for advanced metadata filtering. The GenAI team wants a single, consistent retrieval interface that Bedrock-based applications can use without being rewritten when the underlying vector store changes. Which solution will provide the MOST seamless integration mechanism for retrieval augmentation across these vector stores?
Correct answer
Create a stateless Model Context Protocol (MCP) server (for example, on AWS Lambda) that exposes a single tool such as "vector_search" with a stable JSON input/output contract. Use an MCP client library in the application/agent runtime to call this tool, and route the request internally to OpenSearch or Aurora as needed.
Explanation
Correct. An MCP server plus an MCP client provides a standardized tool interface for vector queries. The stable contract abstracts away vendor- or engine-specific query details and lets the backend implementation change (OpenSearch vs Aurora pgvector) without changing how the FM-integrated application invokes retrieval.
Your answer is incorrect
Store embeddings and document chunks directly in Amazon S3 objects and retrieve relevant chunks by using S3 prefix filters and object metadata filters. Pass the retrieved objects as context to the model.
Explanation
Incorrect. S3 prefix and metadata filtering is not a semantic (vector) retrieval mechanism and does not provide k-nearest-neighbor style similarity search. This does not meet the requirement for a consistent vector retrieval interface across vector stores.
Create two Amazon API Gateway endpoints: one backed by an AWS Lambda function that queries OpenSearch and another backed by a different Lambda function that queries Aurora. Instruct the foundation model in the prompt to call the appropriate endpoint based on the user’s request.
Explanation
Incorrect. Exposing multiple endpoints still results in inconsistent access patterns for the model and application. Relying on prompt instructions for endpoint selection is brittle and increases the chance of incorrect routing or malformed requests.
Implement separate retrieval modules in the application code: one that queries OpenSearch using its vector search APIs and another that queries Aurora using SQL with pgvector. Add conditional logic in each application to select the correct module.
Explanation
Incorrect. This approach duplicates integration logic across applications and creates multiple access patterns. Switching vector store choices requires code changes and redeployments, which conflicts with the goal of a single, consistent retrieval interface.
Overall explanation
A consistent access mechanism for retrieval augmentation is best achieved by placing a stable, tool-like contract in front of the retrieval layer and letting applications or agents invoke that contract in the same way regardless of which vector store is used. Using an MCP server to expose a single vector-query tool, combined with an MCP client in the runtime, standardizes how the FM-integrated system performs retrieval and returns normalized results (for example, chunks plus metadata). This approach prevents tight coupling to OpenSearch-specific or Aurora/SQL-specific query logic and avoids rewriting application integrations when the organization changes vector stores. Further reading (AWS): - Deploy MCP servers in AgentCore Runtime - Amazon Bedrock AgentCore (AWS Documentation) - Vector search - Amazon OpenSearch Service (AWS Documentation) - Using Aurora PostgreSQL as a Knowledge Base for Amazon Bedrock - Amazon Aurora (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 66
Incorrect
A logistics software provider runs an order-tracking platform on AWS that integrates with multiple internal microservices. A shipping partner sends signed HTTPS webhooks whenever a delivery exception occurs. The provider wants to add GenAI functionality that uses an Amazon Bedrock FM to generate a short, customer-ready message and then deliver the generated message to both a case-management service and a notification service. The webhook endpoint must acknowledge requests within 2 seconds, and the provider must be able to add additional downstream consumers later without changing the webhook handler code. Which solution will meet these requirements with the LEAST operational overhead?
Use Amazon API Gateway to start an AWS Step Functions workflow that validates the webhook signature, invokes Amazon Bedrock, and calls both microservice APIs. Configure the workflow to return the result to API Gateway after all steps complete.
Explanation
Incorrect. Returning only after the workflow completes couples the webhook acknowledgement to FM invocation and downstream API calls, making it difficult to reliably meet the 2-second acknowledgement requirement. Step Functions is useful for orchestrating multi-step workflows, but it adds unnecessary orchestration complexity for a simple event-driven fan-out integration.
Use Amazon API Gateway to receive the webhook and invoke an AWS Lambda function that validates the webhook signature and sends the payload to an Amazon SQS queue. Use a Lambda consumer to read messages from the queue, call Amazon Bedrock, and then call the case-management and notification microservice APIs.
Explanation
Incorrect. SQS provides durable buffering, but it is not optimized for content-based routing and multi-consumer fan-out without additional design (for example, multiple queues or custom fan-out logic). Adding new downstream consumers typically requires additional queues and changes to producers/consumers, increasing integration overhead compared to EventBridge rules.
Your answer is incorrect
Use Amazon API Gateway to receive the webhook and invoke a single AWS Lambda function that validates the webhook signature, calls Amazon Bedrock, calls the case-management and notification microservice APIs, and then returns a response to the webhook sender.
Explanation
Incorrect. This tightly couples webhook acknowledgement to FM inference and downstream service latency, which can exceed the 2-second acknowledgement requirement. It also makes future fan-out harder because new consumers require modifying the same Lambda function.
Correct answer
Use Amazon API Gateway to receive the webhook and invoke an AWS Lambda function that validates the webhook signature and publishes a custom event to Amazon EventBridge. Use EventBridge rules to invoke a second Lambda function that calls Amazon Bedrock and publishes a new event containing the generated message. Use additional EventBridge rules to invoke Lambda functions that call the case-management and notification microservice APIs.
Explanation
Correct. API Gateway provides a managed HTTPS endpoint, Lambda performs custom signature validation and normalization, and EventBridge decouples producers from consumers with managed, event-driven fan-out. The webhook handler can respond immediately after publishing the event, meeting the 2-second acknowledgement requirement. Adding new downstream consumers later is done by creating new EventBridge rules/targets without modifying the webhook handler.
Overall explanation
The key design requirement is to enhance an existing application by integrating GenAI while keeping the inbound webhook path fast and keeping downstream integrations loosely coupled. A managed webhook endpoint can be implemented with Amazon API Gateway, while AWS Lambda is appropriate for webhook handling tasks like HMAC signature validation and request normalization. Publishing the validated event to Amazon EventBridge decouples the webhook handler from downstream processing. EventBridge rules can then trigger GenAI inference (invoking an Amazon Bedrock model from Lambda) and fan out the resulting message to multiple existing microservices. This pattern minimizes operational overhead because routing, fan-out, and future consumer additions are handled by EventBridge configuration rather than custom integration code changes. Further reading (AWS): - Event buses in Amazon EventBridge - Amazon EventBridge (AWS Documentation) - Event bus targets in Amazon EventBridge - Amazon EventBridge (AWS Documentation) - Submit prompts and generate responses using the API - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 2: Implementation and Integration
Question 67
Correct
A product team is building an internal chat assistant for customer support agents by using Amazon Bedrock. After launch, the team needs a user-centered way to continuously improve response quality by collecting per-response ratings and optional written feedback, and to later analyze feedback by model and prompt version. The team wants a solution that is serverless and requires the LEAST operational overhead. Which solution will meet these requirements?
Create an Amazon SageMaker Ground Truth labeling job that sends every assistant response to a human workforce for scoring and annotation. Store the labeled results in Amazon S3 and use the labels to decide which model to use.
Explanation
Incorrect. While it can produce high-quality labels, routing every response through a labeling workflow adds significant operational overhead and cost and does not leverage direct in-app user feedback. It is not the least operationally complex way to continuously incorporate user experience signals.
Enable Amazon Bedrock model invocation logging and send the logs to Amazon CloudWatch Logs. Use CloudWatch Logs Insights queries to infer user satisfaction by counting follow-up questions and re-prompts, and store the query results as dashboards.
Explanation
Incorrect. This approach does not collect explicit user ratings or written feedback, and it attempts to infer satisfaction indirectly from behavior. It also makes it difficult to reliably associate feedback with a specific response and capture user-provided corrections, which are key user-centered evaluation mechanisms.
Use AWS CloudTrail to record Amazon Bedrock InvokeModel API calls and export the events to Amazon S3. Use Amazon Athena queries to compute daily satisfaction scores per model version.
Explanation
Incorrect. CloudTrail captures API activity for governance and auditing, not end-user satisfaction. It will not provide per-response ratings or user annotations, so it does not meet the requirement for a user-centered feedback mechanism.
Your answer is correct
Add a “Rate this answer” feature in the web UI that calls an Amazon API Gateway REST endpoint. Use an AWS Lambda function to validate the request and store the rating, free-text feedback, and metadata (model ID and prompt version) in an Amazon DynamoDB table for later analysis.
Explanation
Correct. An API Gateway + Lambda + DynamoDB pattern provides a simple serverless feedback interface that can capture explicit user ratings and annotations at scale. DynamoDB is well-suited to store structured feedback records (including model ID and prompt version) so the team can later analyze user satisfaction and trends per configuration and use that information to iterate on prompts or model selection.
Overall explanation
A user-centered evaluation mechanism requires collecting explicit feedback from the people using the application (for example, thumbs up/down, star ratings, and optional comments or corrections) and persisting that feedback in a form that can be analyzed over time. A serverless feedback endpoint implemented with Amazon API Gateway and AWS Lambda can receive feedback events from the application UI and perform basic validation and enrichment (such as attaching the model ID and prompt version). Storing these records in Amazon DynamoDB keeps the solution fully managed and scalable with minimal operational burden, while enabling later analysis and continuous improvement cycles based on real user experience. Auditing and logging services can help troubleshoot and govern usage, but they do not replace direct user feedback, and a full human labeling pipeline for every response is typically excessive for ongoing, in-product iteration. Further reading (AWS): - Tutorial: Using Lambda with API Gateway (AWS Documentation) - Monitor model invocation using CloudWatch Logs and Amazon S3 - Amazon Bedrock (AWS Documentation) - Monitor Amazon Bedrock API calls using CloudTrail - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 5: Testing, Validation, and Troubleshooting
Question 68
Correct
A digital claims team is building a GenAI workflow on AWS that produces a single, structured JSON “case packet” for an Amazon Bedrock multimodal model. Each claim includes a PDF form (with tables), several JPEG photos, an MP3 voicemail recording, and a CSV export of claim metadata from a legacy system. The team needs an AWS-managed processing workflow that can handle these mixed modalities and prepare model-ready JSON at scale with the LEAST operational overhead. Which solution will meet these requirements?
Use Amazon Textract to extract text and tables from the PDFs and to OCR the JPEGs. Use AWS Lambda to parse the Textract output and to merge it with the CSV data. Use Amazon Bedrock to directly ingest the MP3 voicemail as part of a multimodal prompt and generate the final JSON packet.
Explanation
Incorrect. While Textract can extract text and tables from documents and images, directly passing MP3 audio to an LLM for transcription is not a reliable or standard processing step in this architecture. This option also requires significant custom parsing and merging logic in Lambda for multiple output formats (Textract JSON + CSV).
Use Amazon SageMaker Data Wrangler in SageMaker Studio to import the PDFs, JPEGs, MP3 files, and CSV files. Apply transformations to standardize the data and export a single JSON file to Amazon S3 for the Bedrock model to consume.
Explanation
Incorrect. Data Wrangler is oriented toward interactive data preparation workflows (primarily tabular and some image transformations) and requires operating within SageMaker Studio. It is not a purpose-built service for automated audio transcription and multimodal document-to-structured extraction at scale with low operational overhead.
Store the PDFs, JPEGs, MP3 files, and CSV files in Amazon S3. Configure Amazon Bedrock Knowledge Bases to ingest all files automatically and return a single structured JSON object that combines the extracted information from every file type for direct FM consumption.
Explanation
Incorrect. Knowledge Bases are designed for retrieval augmentation over ingested content, not as a general-purpose multimodal ETL system that guarantees a single consolidated, structured JSON output across PDFs, images, audio, and tabular CSV. This option does not provide the required specialized processing steps (for example, dedicated audio transcription and deterministic tabular normalization into a case packet).
Your answer is correct
Use Amazon Bedrock Data Automation to asynchronously process the PDFs and JPEGs into structured JSON and to process the MP3 voicemails into transcripts and summaries. Use an AWS Glue ETL job to transform the CSV metadata into JSON. Store all outputs in Amazon S3, and use an AWS Lambda function to assemble a single JSON case packet for the Amazon Bedrock multimodal model.
Explanation
Correct. Bedrock Data Automation is designed to extract structured information from documents and images and to process audio into transcripts/summaries, producing model-ready JSON with minimal custom extraction logic. AWS Glue can reliably transform tabular CSV into normalized JSON. Lambda can then perform lightweight orchestration to assemble the final case packet for downstream Bedrock inference.
Overall explanation
The key requirement is a scalable, low-operations pipeline that can transform mixed modalities into a structured, model-ready JSON payload. Amazon Bedrock Data Automation provides managed extraction for documents and images and managed audio processing (for transcripts/summaries) without requiring the team to build and maintain custom parsers for each format. AWS Glue is well suited for transforming CSV tabular data into a normalized JSON structure. Storing intermediate artifacts in Amazon S3 and using a small Lambda step to assemble the final “case packet” keeps the architecture serverless, modular, and operationally simple while producing an input format that a Bedrock multimodal model can consume consistently. Further reading (AWS): - Using the Bedrock Data Automation API (AWS Documentation) - Data format options for inputs and outputs in AWS Glue for Spark (AWS Documentation) - Process Amazon S3 event notifications with Lambda (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 69
Correct
A retail bank is building an internal GenAI assistant that helps loan officers draft customer-facing email responses. The bank must ensure the assistant follows an internal lending communications policy that prohibits the assistant from implying that a customer is approved or denied, and requires a standard disclaimer in every response. The bank must also document the model’s intended use and known limitations for governance review. Which solution will meet these requirements with the LEAST operational overhead?
Create an Amazon Bedrock Knowledge Base that contains the lending communications policy and product guidelines. Use RetrieveAndGenerate so the assistant can cite the policy in responses, and reject any response that does not include citations.
Explanation
Incorrect. Grounding responses with a knowledge base can improve factuality and provide citations, but it does not ensure the model will always follow the policy or include required disclaimers. Requiring citations does not prevent prohibited statements (such as implying approval/denial) and is not a substitute for explicit safety and compliance controls.
Create a fine-tuned Amazon Bedrock custom model by training on historical compliant email examples. Deploy the model with provisioned throughput to ensure consistent behavior, and rely on user feedback to detect policy violations over time.
Explanation
Incorrect. Fine-tuning and provisioned throughput can improve domain style and performance consistency, but they do not provide strong, explicit policy enforcement. Relying on user feedback is reactive and allows policy violations to reach users. This also does not address automated compliance checks at inference time.
Your answer is correct
Create an Amazon Bedrock guardrail that implements the lending communications policy, including an automated reasoning policy derived from the policy document. Invoke the model through an AWS Lambda function that performs a final compliance check (for example, verifies the required disclaimer is present and blocks responses that violate policy). Document intended use and limitations in a model card.
Explanation
Correct. Amazon Bedrock guardrails are designed to enforce policy-based constraints on model inputs and outputs, and automated reasoning checks can apply structured policy logic derived from a policy document. A lightweight Lambda pre/post-processing layer can enforce deterministic checks (such as ensuring a required disclaimer is present and blocking noncompliant outputs) before returning content to users. Maintaining a model card addresses governance needs by documenting intended use, limitations, and constraints in a standardized way.
Store an approved system prompt in Amazon Bedrock Prompt Management that includes the full policy text and the required disclaimer. Require all applications to use only this prompt and enable AWS CloudTrail logging for auditing.
Explanation
Incorrect. Prompt governance and audit logging help with consistency and traceability but do not reliably enforce policy compliance because a model can still produce disallowed content, and prompts can be influenced by user input (prompt injection). This option also does not provide an automated enforcement layer to block or remediate noncompliant outputs.
Overall explanation
A policy-compliant GenAI system needs explicit enforcement mechanisms, not just guidance. Amazon Bedrock guardrails provide built-in controls to block or filter content according to policy requirements, and automated reasoning checks can apply structured logic derived from a policy document for complex compliance rules. Adding a small Lambda layer for deterministic validation (such as checking for mandatory disclaimers and blocking disallowed phrases) creates a final compliance gate with minimal added complexity. A model card complements these controls by documenting intended use and known limitations to support governance and audits. Approaches that rely only on prompt text, citations, or post-hoc user feedback do not reliably prevent policy violations from reaching end users. Further reading (AWS): - Detect and filter harmful content by using Amazon Bedrock Guardrails - Amazon Bedrock (AWS Documentation) - Improve accuracy by adding Automated Reasoning checks in Amazon Bedrock Guardrails - Amazon Bedrock (AWS Documentation) - Amazon SageMaker Model Cards - Amazon SageMaker AI (AWS Documentation)
Domain
Content Domain 3: AI Safety, Security, and Governance
Question 70
Correct
A healthcare platform team is enabling multiple product groups to build GenAI applications on AWS by using Amazon Bedrock. The applications will be deployed in different AWS accounts and environments (dev, test, and production). The platform team must provide a consistent, repeatable way for teams to implement GenAI architectures that align with organizational best practices across security, reliability, and cost. Which approach will achieve this with the LEAST operational overhead?
Use AWS Organizations service control policies (SCPs) to restrict which AWS Regions and services can be used, and require all teams to use the same foundation model for every GenAI workload.
Explanation
Incorrect. SCPs can help with governance controls, but they do not provide standardized GenAI architectural components or a repeatable best-practices implementation approach. Enforcing a single model choice also does not ensure consistent architecture across deployments.
Create a custom internal architecture review system by using AWS Lambda, Amazon DynamoDB, and Amazon API Gateway to enforce a proprietary checklist for every GenAI deployment.
Explanation
Incorrect. Although it could standardize reviews, building and maintaining a custom governance and validation system increases operational overhead compared to using established Well-Architected mechanisms and a managed review workflow.
Your answer is correct
Use the AWS Well-Architected Tool with the AWS Well-Architected Generative AI Lens to define and review a standard GenAI architecture. Provide approved reusable infrastructure-as-code components (for example, AWS CDK or AWS CloudFormation templates) that teams can deploy across accounts and environments.
Explanation
Correct. The Generative AI Lens provides a consistent set of best practices aligned to the Well-Architected Framework pillars. Using the Well-Architected Tool establishes a repeatable review mechanism, and publishing reusable IaC components creates standardized technical building blocks that teams can apply across multiple deployment scenarios with minimal ongoing manual effort.
Grant each product group permissions to use the Amazon Bedrock console playground to prototype their solution, and use AWS CloudTrail logs after deployment to verify that teams followed organizational standards.
Explanation
Incorrect. Prototyping in the playground does not create standardized components or consistent implementation patterns. Auditing after deployment is reactive and does not provide a repeatable design and implementation standard for multiple teams and environments.
Overall explanation
The most effective low-overhead way to standardize GenAI implementations across many teams and deployment scenarios is to combine a repeatable architecture review process with reusable building blocks. The AWS Well-Architected Tool and the Generative AI Lens provide a consistent best-practices framework aligned to the Well-Architected pillars, and publishing approved infrastructure-as-code templates or constructs enables teams to deploy the same vetted patterns across accounts and environments. Other approaches either rely on post-hoc auditing, add significant custom tooling overhead, or focus on restrictive governance controls without standardizing how solutions are designed and implemented. Further reading (AWS): - Generative AI Lens - AWS Well-Architected Framework (AWS Documentation) - What is AWS Well-Architected Framework? - AWS Well-Architected Tool (AWS Documentation) - Best practices for developing and deploying cloud infrastructure with the AWS CDK - AWS Cloud Development Kit (AWS CDK) v2 (AWS Documentation)
Domain
Content Domain 1: Foundation Model Integration, Data Managem
Question 71
Correct
A SaaS provider runs a customer-support assistant that uses an Amazon Bedrock text model and a prompt template stored in Amazon Bedrock Prompt Management. The team wants to release an updated prompt version and occasionally switch to a newer model version. Before promoting changes, the team must automatically validate that answers remain consistent with previously accepted behavior and that hallucinations or semantic drift do not increase. Which solution will meet these requirements with the LEAST operational overhead?
Create an Amazon CloudWatch Synthetics canary that invokes the assistant with a fixed set of prompts and fails if the generated responses do not exactly match a stored set of expected responses.
Explanation
Incorrect. Exact string matching is not a reliable validation approach for GenAI because responses can be correct but vary in wording across runs. This approach will produce false failures and does not measure hallucination or semantic drift in a robust way.
Enable Amazon Bedrock Model Invocation Logs and use Amazon CloudWatch Logs Insights queries to review a sample of production prompts and responses after deployment. Roll back if support engineers notice problems in the logs.
Explanation
Incorrect. This is reactive and manual. It does not provide an automated pre-deployment validation gate, and it does not quantify semantic drift or hallucination rates against a controlled baseline before promoting the update.
Your answer is correct
Create an Amazon Bedrock Model Evaluations job that runs a prompt dataset (synthetic user workflows) stored in Amazon S3 against the candidate prompt/model and a baseline. Use an LLM-as-a-judge evaluation to score quality and AI-specific metrics, and use the results as an automated quality gate in the release pipeline before promoting the update.
Explanation
Correct. Bedrock Model Evaluations can run repeatable synthetic workflows from a dataset and use an evaluator model to score responses for quality and AI-specific risks such as groundedness/faithfulness and response consistency (semantic drift). Integrating the evaluation results as a pipeline gate provides automated deployment validation with minimal custom implementation.
Route all responses from the updated prompt/model to Amazon Augmented AI (A2I) for human review during a canary period and promote the update only after reviewers approve a statistically significant sample.
Explanation
Incorrect. Human review can work, but sending responses for ongoing human evaluation adds substantial operational overhead and cost. It is not the least-effort approach for automated validation during frequent FM or prompt updates.
Overall explanation
A low-overhead deployment validation system for GenAI updates should run repeatable synthetic user workflows and automatically score the candidate release for AI-specific regressions such as hallucinations and semantic drift. Using Amazon Bedrock Model Evaluations with a prompt dataset in Amazon S3 provides a managed way to evaluate candidate prompt/model changes against a baseline by using an evaluator model, producing objective scores that can be enforced as a release gate. Log analysis is post-deployment and manual, exact string matching is brittle for non-deterministic outputs, and human-in-the-loop reviews are higher operational overhead than managed automated evaluation. Further reading (AWS): - Evaluate model performance using another LLM as a judge - Amazon Bedrock (AWS Documentation) - Create a model evaluation job that uses an LLM as a judge - Amazon Bedrock (AWS Documentation) - Compare versions of a prompt in Prompt management - Amazon Bedrock (AWS Documentation)
Domain
Content Domain 5: Testing, Validation, and Troubleshooting
